# 發現之前｜CH2-CR-013｜三列預測與押注讀回

**日期**：2026-08-09  
**狀態**：ACCEPTED／`DESIGN GATE: PASS`（2026-08-09）  
**真相模式**：TO-BE  
**風險**：R2（場景節點、具名旗標、後文讀回；不改 engine2／schema／rep）  
**上游**：ADR-014、ADR-016、GOV-ADR-001、Fable 四章 branch map v0.1、Sol 可施工性複核

**總監簽核**：`DESIGN GATE: PASS — CH2-CR-013 + CH4-CR-013 + CH5-CR-003 + CH6-CR-002`

## 一、問題與裁決

`B2-4/q1` 的三種事前預測目前都無狀態地進 q2。新增
`ch2DropPrediction = late | unresolved | abstain`，讓三列資料揭曉後由世界讀回玩家押注。
誠實預測錯誤不扣 rep；現行 q4.b「替耳朵補聲音」仍是另一種越界行為，原地反駁不變。

## 二、核准拓撲

1. q1 三選項各寫 `ch2DropPrediction`，全部進 Fable `c_commit`，再進現行 q2。
2. q2、np1、q3、np3 與總門閂結構逐字不動。
3. q4.b 仍走現行 nb1→q4；不得觸發押注讀回。
4. q4.a 依旗標顯示 `r_late`／`r_unresolved`／`r_abstain`，再進現行 na1。
5. B3-1/n3 後只有 `late` 路顯示 `b3_recall`；其他路與缺旗標 legacy 存檔直接略過。

`r_late` 採 Sol 校正版，避免把未測得延遲寫成絕對同時：

> 伽利略：（把你的木籌從「晚到」那格推到三列紙旁）這一注，三列紙沒有替它作證。可它敢押。（頓）籌碼不退——下次，讓它壓在紙寫得出的地方。

其餘台詞依 Fable v0.1.1；本替代句已獲 Fable 逐字確認。

## 三、狀態與相容

- 新狀態只在 `state.flags.ch2DropPrediction`；值採字串，不用裸計數。
- 舊存檔缺旗標時不補故事、不捏造押注，沿 neutral 路徑。
- 線性離場使讀回天然 exactly-once，不加 `seen` flag。
- scenes2 JSON 是 canonical；施工後重建 scenes2.js。

## 四、不動條款

- GB-ADR-022 的一次總門閂、三列逐列確認與 B2-5 三紙結構不動。
- q4.a／q4.b 現行文字、nb1、na1、F2 取得與 engine2 數據不動。
- 不把錯袋做成真封存，不新增 evidence／rep／schema。

## 五、預定施工面與驗收

預定只改 `greybox/data/scenes2.json`、生成鏡像與 `greybox/tests/run-node.mjs`。

測試至少涵蓋：三種押注、q4.b 重試不提前讀回、late 後文讀回、legacy 無旗標、rep 零變化、
F2 與 B2-5 既有契約不變、全套測試與瀏覽器鍵盤／讀屏路徑。

## 六、Gate

與 CH4-CR-013、CH5-CR-003、CH6-CR-002 同批裁。核准句見合併複核報告。
