# 發現之前｜CH4-CR-013｜切線錯紙與印刷讀回

**日期**：2026-08-09  
**狀態**：ACCEPTED／`DESIGN GATE: PASS`（2026-08-09）  
**真相模式**：TO-BE  
**風險**：R2（場景節點與既有事件守衛；不改 engine4／schema／rep）  
**上游**：ADR-014、ADR-016、ENGINE-CR-033、Fable 四章 branch map v0.1、Sol 可施工性複核

**總監簽核**：`DESIGN GATE: PASS — CH2-CR-013 + CH4-CR-013 + CH5-CR-003 + CH6-CR-002`

## 一、問題與裁決

D1-1 的錯選已被 engine4 append-only 記錄，但後文人物沒有翻回這張紙。保留當場幾何反駁，
並在 D4-2／DE-1 讀回；錯誤的幾何可立即裁，這張錯紙對整章的意義延後裁。

## 二、事實來源修正

錯選不會寫入 `sourceLab.tangentPrediction.choice`；它只存在於 `sourceLab.attempts` 與
`eventLog`。本 CR 不擴充 require grammar，使用現行 `require.event/all/not`：

1. 曾選 curve：顯示 `d42_recall_curve`。
2. 未選 curve、曾選 inward：顯示 `d42_recall_inward`。
3. curve／inward 都未選、曾選 tangent：顯示 `d42_recall_tangent`。
4. 缺事件的 legacy 存檔：全部略過，不反推故事。

玩家若兩種錯路都試過，僅演 curve 讀回；兩筆原始 attempt 仍保留。這是呈現優先序，不是
刪除歷史。

## 三、核准拓撲與文字

- w1 後加 Fable 的 curve stage 拍，再回 c1。
- w3 後加 Fable 的 inward stage 拍，再回 c1。
- D4-2 在印刷前串接互斥讀回；三條台詞依 Fable v0.1。
- DE-1 使用中性收束，避免 direct-tangent 玩家被寫成走過兩條錯路：

> stage：第一晚留下的切線紙、後來量成的短差，以及一路退下的錯稿，疊進同一只木匣。蠟油封上時，沒有一張曾真正出現的紙被抽走。

本替代句已獲 Fable v0.1.1 逐字確認。

## 四、不動條款

- D1-1 c1/c2、w1–w5 既有台詞除接線外不改；三個 `sealTangentPrediction` action 不改。
- K1–K5 數值、借條 append-only、印刷窗口、stampAttempts 與校樣代價不動。
- 不新增旗標、evidence、rep、schema 或 engine4 能力。

## 五、預定施工面與驗收

預定只改 `greybox/data/scenes4.json`、生成鏡像與 `greybox/tests/run-node.mjs`。

測試至少涵蓋：direct、curve→tangent、inward→tangent、curve＋inward→tangent、legacy 缺
event、attempt 全保留、K0/K1–K5／migration 既有契約與全套測試；瀏覽器驗 D4-2 不連播
互斥讀回。

## 六、Gate

與 CH2-CR-013、CH5-CR-003、CH6-CR-002 同批裁。核准句見合併複核報告。
