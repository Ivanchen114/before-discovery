# 發現之前｜CH5-CR-003｜尺度斷言進辯論與沉默後果

**日期**：2026-08-09  
**狀態**：ACCEPTED／`DESIGN GATE: PASS`（2026-08-09）  
**真相模式**：TO-BE  
**風險**：R2（場景包裹、具名枚舉與辯論前後讀回；不改 engine5／debate5／schema／rep）  
**上游**：ADR-014、ADR-016、GOV-ADR-001、Fable 四章 branch map v0.1、Sol 可施工性複核

**總監簽核**：`DESIGN GATE: PASS — CH2-CR-013 + CH4-CR-013 + CH5-CR-003 + CH6-CR-002`

## 一、問題與裁決

E2-3/c_scale 的兩個過度斷言目前被立即退回重選。改為保留原句、直接前進，在 E3-1 由
杜佩拿同批資料反擊。玩家可以縮句，也可以沉默；沉默不被 NPC 代撤回，原句在 E3-2 仍
記在玩家名下。這避免用「必須改成正解」重造舊答題閘門。

## 二、單一具名狀態

`state.flags.ch5ScaleClaim` 使用封閉枚舉：

- `measurement-only`
- `vis-viva-open`／`vis-viva-shrunk`／`vis-viva-silent`
- `momentum-false-open`／`momentum-false-shrunk`／`momentum-false-silent`

一個欄位同時保留原立場與後續處理；不另加 repaired／seen 計數。legacy 缺旗標時不顯示
先手拍或痕跡讀回。

## 三、核准拓撲

1. c_scale 三選項都寫入枚舉；measurement-only 保留既有 `ch5ScaleBoundary`。
2. 兩錯項走現行 scale_bad_world／scale_bad_momentum，再接 Fable 承諾拍並匯入 n1a，
   不回 c_scale。
3. E3-1/x4 後只有 `*-open` 進先手拍；measurement-only／legacy 直接進 debate。
4. 玩家縮句：轉 `*-shrunk`，演 e_shrunk，再進現行 debate。
5. 玩家堅持：杜佩再壓一拍；其後縮句或沉默。沉默轉 `*-silent`，演 e_silent，再進 debate。
6. E3-2：shrunk 用 Fable e32_trace；silent 用 e32_trace_silent；measurement-only／legacy 略過。

## 四、機制事實校正版台詞

`duel_v2`：

> 杜佩院士：（翻開 J2 那頁，推到桌心）同一批碰撞。你的油灰坑量過碰撞後兩邊的速度嗎？量過方向嗎？（頓）沒有。那它憑什麼，審一本記方向的帳？

`e_silent`：

> 杜夏特萊：（把下午那句原話留在帳頁邊欄，沒有替你劃線）沉默不算撤回。上桌吧——它還是你的。

`e32_trace_silent`：

> 杜夏特萊：（把帳頁邊欄翻開給你看——下午那句原話旁，今晚仍是一片空白）你沒有收回。那它就繼續算在你名下。

其餘台詞依 Fable v0.1.1；三句與「沉默可持續前進」均已獲 Fable 逐字確認。

## 五、不動條款

- collision fixture、J1/J2/J3、debate5、支柱順序與 E3-2 x2–x5 不動。
- 理論押注與沉默不新增 rep；現行權威／研究失信事件不動。
- 不把兩本帳改成二選一真理，不由 NPC 代玩家縮句。

## 六、預定施工面與驗收

預定只改 `greybox/data/scenes5.json`、生成鏡像與 `greybox/tests/run-node.mjs`。

測試至少涵蓋三種初始立場、立即縮句、堅持後縮句、堅持後沉默、E3-2 差異讀回、legacy、
rep 零新增、debate5 狀態深等與全套測試；瀏覽器驗辯論先手不重播、沉默不死鎖。

## 七、Gate

與 CH2-CR-013、CH4-CR-013、CH6-CR-002 同批裁。核准句見合併複核報告。
