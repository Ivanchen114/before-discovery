# 發現之前｜CH6-CR-003｜稽核板原紙逐問首欄選取

**日期**：2026-08-14
**狀態**：APPROVED／IMPLEMENTED／AUTOMATED CONFORMANCE PASS
**上游**：《工作台與辯論架構・一二章實證規格》§4-1
**總監授權**：總監要求各章真正的「選數據」介面對齊第一章，不在題目下以卡片或下拉重複資料。

## 問題與定案

`model-audit-board` 原先把五個來源問題各做成一張卡，卡內都重列 T1–T4 下拉。現改為一次只處理一個尚未完成的來源問題；T1–T4 只在一張表出現一次，玩家於首欄正好勾一張，再放回目前這一格。

完成的配對收進上方摘要；下一問換題時清除 UI 草稿。T4 仍可分別被炮身與水箱兩題引用，因為這是同一張長時段原紙回答兩個具名來源主張，不是重複產生原紙。

## 施工與保護線

選取存在 `heat6AuditDraft`，不進存檔。送出仍逐題呼叫既有 `placeAuditEvidence({slot,evidence})`；engine6 的對位、T1–T5、共同頁、schema、sanitizer、場景與信譽均不變。

## 驗收

1. 畫面一次只顯示當前來源問題與一張 T1–T4 表；沒有五份重複下拉。
2. 正好勾一張才送出；錯配由引擎拒絕且 state 不變。
3. 換到下一題時不沿用上一題的暫選；T4 可合法引用兩次。
4. 全章走查、存檔淨化與既有有限來源負向契約維持綠燈。

## 施工回執

- `model-audit-board` 已改為一次一個來源問題＋單一 T1–T4 原紙表，首欄正好勾一張；換題即清除暫選，T4 仍可在不同來源題各引用一次。
- `placeAuditEvidence`、engine6、T1–T5、schema、sanitizer、場景與信譽零修改。
- 2026-08-14 全套 `npm test` PASS：主契約 184／0，第六章全章走查、有限來源負向、認知回饋與美術 suites 全綠。
- 瀏覽器與玩家體感仍待總監實走；自動通過不冒充人工驗收。
