# Claude 任務書：第一章 M3 外科修正

**日期**：2026-07-19  
**目標**：修正審查阻擋並交付可凍結基線；保留現有 M1–M3 架構，不重寫。

## 先讀

1. `05_審核/發現之前_劇本v0.2.1審稿_Sol_20260719.md`
2. `05_審核/發現之前_第一章功能規格v0.1審稿_Sol_20260719.md`
3. `05_審核/發現之前_M1-M3_程式對抗審查_Sol_20260719.md`
4. `05_審核/發現之前_M1-M3_瀏覽器煙霧測試_Sol_20260719.md`

## 交付順序

### 1. 先修上游單一來源

- 劇本出 v0.2.2：探索模式實際做 E3.c；物理結論補介質／同形狀／同槽面條件；「稀釋重力」改為把沿槽加速變小；E4 改「不能只怪重量」；「空氣裡不分先後」改「近乎同落／只差一線」。
- 修 Delft 信件揭露與債務時間：保留戲劇功能，但史實頁明列信件／直接因果為虛構串接；刪「1599 還一半／1603 債清」精確宣稱。
- q2 要有可感知後果，否則刪除；1604 統一「四十年」；E4 不再標為可略過支線。
- 章規格出 v0.1.1：同步 E3.c 雙模式必經、lint enum／refutedBy、site 決定、信譽導流提示、壞檔提示與史實標籤 schema。

### 2. 再同步 M3 runtime

- 探索模式加入引導式變傾角複驗並取得 E3.c；學者保留自由配置。
- FR 與 `debatePresent` 在引擎層檢查 E3.c／證據所有權；UI 只列已取得子項。
- lint class 封閉為 `character-hypothesis`／`post-reveal`；未知 class 必敗；角色假說必填且驗證 `refutedBy`。
- 壞存檔顯示一次性非阻塞提示。
- 修 trap speaker 重複與 end 重複；不要改已通過的《物理學》道具動作。

### 3. 必補測試

1. 探索模式入辯論前 E3.c=true。
2. E3.c=false 不得開啟／完成 FR。
3. 未知 whitelist class、缺／無效 `refutedBy` 必敗。
4. 未持有 E1／E2／E4／S1／S2 或 E3 子項不得出示，且 state 不變。
5. trap 前說服力=1、信譽=1：lied → 同時中止／修復 → 再入 FR → honest → end。
6. 壞 JSON 與舊 schema 均備份且提示。

## 邊界與入庫規則

- 不碰 `art/source/proof/ch01/`、美術附錄與 Sol 審核文件；美術工作樹目前有未入庫交付。
- 先寫逐條審稿回應至 `05_審核/`，再修 `04_劇本/`、`03_規格/`、`greybox/`。
- 跑 `npm test`，回報新測試總數與人工檢核路徑。
- 共用工作樹禁止 `git add -A`；只 stage 本任務明列路徑，避免把 Sol 美術與審核檔混入程式 commit。
- 完成後停在「等待 Sol verification pass」，不得自行宣告凍結或開始正式美術量產。
