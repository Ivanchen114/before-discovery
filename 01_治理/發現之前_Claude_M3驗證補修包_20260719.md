# Claude 任務書：M3 verification 補修

**性質**：一次極小補修；不重開外科修正已通過項目。  
**法源**：`05_審核/發現之前_M3外科修正驗證_Sol_20260719.md`

## 必修兩項

1. `greybox/src/chapter-ui.js`：探索模式抵達 `v.until.e3 === "c"` 的 incline embed 時，必須顯示「斷言：隨傾角形式不變」並可點擊；按鈕可見性由當前 embed 需求決定，不得再只看 `state.mode === "scholar"`。建議同時在 C 階段隱藏 B，避免玩家選錯工具。
2. `04_劇本/第一章完整劇本_重物的渴望_v0.2.2.md` 分支表 A2-3：把「〔學者〕E3.c」改成「E3.c（雙模式；探索引導／學者自由配置）」。

## 驗證與邊界

- 跑現有 `npm test`，45 項不得退步。
- 必須以瀏覽器探索模式實際看到並點擊 C，使狀態由 `c○` 變 `c●`；直接呼叫引擎不算 UI 驗收。
- 不碰 Sol 美術、任務書、審核文件或其他已通過內容；禁止 `git add -A`，只 stage 上述兩個檔及必要的精準 UI 測試檔。
- 完成後停在等待 Sol 第二次 verification，不自行凍結、不開始美術量產。
