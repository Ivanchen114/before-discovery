# 第二章 M3b 交接 Sol(Claude,2026-07-21)

**現況**:M1/M2a/M2b/M3a 已 commit,`npm test` **72/72**(注意:驗證請看 exit code,勿只看管線輸出——本輪踩過管線吃退出碼的坑)。第二章**引擎層全章可通**:走查測試以三條路實走 B0-1→BE-2(探索誤選路/學者+ch1 certified 繼承路/over 反將路),F5 雙路、跨章投影、複盤與修復狀態機皆引擎驗證。灰盒 `chapter2.html` 可玩至三柱擊破;**FR 兩步判讀的面板未畫,玩家點不到**——這是 M3b 第一項。

## M3b 工作清單(依序)

1. **chapter-ui FR 面板**:`debateView()` 新相位 `enemy`(`v.enemy={step,card,prompt,options}`)與 ch2 版 `trap`(claim options)——ch1 `renderDebate` 已處理 pillars/fr/trap/won,補 enemy 分支即可。疊線圖=SVG 由 `card.x/y` 程式繪(**點陣禁承載物理資訊**);`card.a11y` 全文給讀屏;探索模式標首末斜率(card.mFirst/mLast),學者不標(R-DEB2-05)。
2. **歸零路測試**(run-node.mjs 補):①說服力歸零→B3-D suspendNext→B3-F debrief→reenter→已破柱+enemySlopeRead/Classified 保留;②信譽歸零→SC-R1(彈射版 repairRun=乾淨 profile series 讀值數超基線,untilMet 已支援)→return 回原節點;③雙歸零=先 SC-R1 後複盤,回 fr_honest 不重打三柱(R-DEB2-06)。
3. **XCH 瀏覽器接線**:chapter2.html 開局讀 `bd_ch1_save` → `N.projectCh1(raw)`(已實作,**結構版**——sanitize 整合待補,見函式註解)→ `initialState(mode, {ch1: projection})`(注意投影要包在 `{ch1:...}`,測試踩過)。書信碼 envelope v1(R-SAV2-01)+七路徑測試(規格 §五表)。
4. 完成後:M2a~M3b 整批對抗審查(fixture 獨立重算入口:`E2._judgeRaw`/`_FIXTURE`)→ 舞台接線(首頁 `data-chapter="ch02"` 路由+場景 BGM/肖像掛點,沿你首頁 v2 契約)。

## 檔案地圖

`src/engine2.js`(彈射引擎)|`data/scenes2.*`(18 場景+lint 三軌 entries×12)|`data/debate2.*`(三柱+FR)|`data/histfacts2.*`|`chapter2.html`(重指 scenes/engine/debate/histfacts+bd_ch2_save)|測試=run-node.mjs 尾段五個「第二章」區塊。

## 鐵律速記

改 `src/stage/*.part.js` 後必跑 `node tools/build-stage.mjs`;commit 前 npm test 看 exit code;只 stage 明列路徑;凍結基線(劇本 v0.1.3/規格 v0.1.1)動字走正式紀錄;敵方卡與一切數據=SVG。

祝順手。——Claude
