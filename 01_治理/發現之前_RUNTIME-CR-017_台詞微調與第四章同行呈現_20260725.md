# RUNTIME-CR-017｜台詞微調與第四章「和牛頓同行」呈現

**日期**：2026-07-25
**狀態**：已實作、已通過自動測試與關鍵瀏覽器走查
**規範來源**：`greybox/data/scenes*.json`；`*.js` 僅為生成鏡像

## 一、跨章台詞微調

本批不改證據判定與角色立場，只修 runtime 中仍存在的口語漂移。

| 章／節點 | 原句要點 | 新句要點 | 性質 |
|---|---|---|---|
| CH1 `A1-5/n12` | 筆記「重重寫下」 | 筆記「寫下」 | 系統敘述去掉編劇腔 |
| CH2 `B1-3/s1` | 筆記「重重寫下」 | 筆記「寫下」 | 系統敘述去掉編劇腔 |
| CH2 `B2-4/n3` | 「若它要等……」 | 「如果它要等……」 | 伽利略白話聲線一致 |
| CH2 `B3-6/n5` | 「諸位都看見」 | 「在場所有人都看見」 | 旁白不挪用辛普里奧老派語域 |
| CH3 `C0-2/n3b` | 「丟掉這份前行」 | 「鬆手之後，它還會繼續往前」 | 名詞化改成動作 |
| CH3 `C2-4/n2` | 「把已有的前行歸零」 | 「石頭會立刻停下來」 | 名詞化改成可見動作 |
| CH3 `publicDemo/stable-window` | 「任何人都能重新判斷」 | 「你們自己照著查一次」 | 艾蒂安由抽象宣告改成現場邀請 |
| CH4 `D2-1/n7` | 「替數量作證」 | 「數量還沒算出來」 | 牛頓回到短、冷、數字導向 |
| CH4 `D2-1/n9` | 「證明還沒閉合」 | 「還沒算完」 | 旅人心聲去法庭／論文腔 |

`CH3 C2-4/q1` 的正解字串保留不動，避免口語微調不必要地進入判定敏感區。

## 二、第四章呈現重整

上游：

- `02_設計/發現之前_第四章呈現重新思考_與牛頓同行_Claude_20260725.md`
- `02_設計/發現之前_第四章節點級草稿_1665兩拍_Claude_20260725.md`

### 1. 去掉「五個模擬器待辦」外觀

- HUD 不再顯示「改向○／縮放○／預測○／反驗○／邊界○」。
- 只顯示旅人已實際取得的紙；未取得的未來證據不預告。
- 各 phase 改用當下人物與物件命名：1665 紙桌、未決紙、哈雷木匣、彗星星圖、印刷排程、倫敦印刷台。
- 工作區標題去除一至九的清單編號，改成當下真正要回答的問題。

### 2. 首次互動不再被整章備忘卡攔截

- `D1-2/e1` 仍保留明確交棒，但按鈕改成「和牛頓把規則寫死」。
- 首次進場與讀檔直落第四章互動時，不自動彈出六條整章備忘。
- 完整說明仍可由 `?`／備忘入口主動查閱。

### 3. 牛頓對玩家這一次的結果說話

- `runConsequence` 依 `tangent/outward/impact/unstable` 回應可見後果。
- `runOrbitRule` 依 `parabola/wrong-center/outer-band/inner-band/near-circle` 回應路徑。
- 再依 `predictionMatched` 補一句：區分「料中失敗」「意外成功」「預測與成功相合」。
- 回話只描述這張紙做了什麼，不直接告訴玩家下一個正確設定。

### 4. 1665 年誠實失敗

`D1-3` 在 K1 後新增第一次月球比較：

1. 手邊的地球尺度換算後，地表落下與月球偏折沒有碰上。
2. 玩家可選擇硬換數字、先宣布半個答案，或記下差距與借來的量尺。
3. 前兩項由牛頓駁回並返回原題；第三項把紙標成「未決」收入抽屜。
4. `D2-1` 於 1679 年把未決紙重新拿回；`D2-2` 才正式用當時採用的地球尺度重做。

史實邊界：本段採牛頓晚年相關備忘錄與後世研究的重建，只演出早期月球檢驗不足以支持公開宣布，不把 1665 年演成萬有引力理論完成之年，也不預先洩漏反平方律。

## 三、同步與驗證

- 已由 JSON 重建：`scenes.js`、`scenes2.js`、`scenes3.js`、`scenes4.js`、`assets.js`。
- 已由 12 個 part 重建：`greybox/src/stage-ui.js`。
- `npm test`：112 通過、0 失敗；第四章美術與系列首頁檢查通過。
- 瀏覽器走查：
  - D1-2 對話完整收尾後才出現交棒。
  - 進入互動不再自動顯示長備忘卡。
  - 固定方向的拋物線、成功但預測失配，皆顯示對應牛頓回話。
  - 1665 年硬湊數字會返回選擇；「未決」路徑可正常翻到 1679。

## 四、文件回寫說明

第一至第四章 Markdown 劇本目前另有平行編修且工作樹未清。本批不覆寫那些既有改動；本文件保存節點級回寫清單。下一次整理小說／完整劇本時，以 runtime JSON 與本 CR 為準合併，避免反向覆蓋。
