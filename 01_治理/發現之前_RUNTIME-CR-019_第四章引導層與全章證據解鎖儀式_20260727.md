# RUNTIME-CR-019｜第四章引導層＋全章證據解鎖儀式

**日期**：2026-07-27
**授權**：總監原話「你直接改」＋「只有第一章才有這個解鎖的畫面與聲音，幫我把整個遊戲拿到證據都要有這個機制」。
**施工**：Claude
**提交**：**暫不 commit**——工作樹混有 Sol 未提交的 ch5／ch3 施工（`engine3.js`＋447 行、`narrative.js`、`sanitize.js`、全部 `data/*.json`）。本 CR 的改動（`chapter-ui.js`、`stage/04-typewriter.part.js`、`stage-ui.js` 重建、`tests/run-node.mjs`）**由 Sol 於其收工 commit 時一併明列提交**，訊息註明 RUNTIME-CR-019。
**範圍**：`greybox/src/chapter-ui.js`、`greybox/src/stage-ui.js`。**不碰任何 `data/*.json`**（Sol 正在編輯、未提交）。
**規格依據**：`03_規格/第四章工作台與終局規格_對齊一二章骨架 v0.2` §3-1／3-2／3-3。

## 一、變更內容

### A. 第四章目的卡（v0.2 §3-2）

`orbit4Mission()` 由 `[標題, 副標]` 擴為 `[標題, 副標, 目的, 台詞]`，十個相位各補「這一輪要回答什麼」＋牛頓一句 in-world 台詞；`renderOrbit()` 於標題區渲染。**vectors 的目的卡引用玩家在第三章末頁親手寫下的問題**（「月亮為什麼沒有沿直線離開？」）。

### B. vectors 相位文案替換（v0.2 §3-3）

「局部幾何改向尺」→ 紙桌視角；「替月亮修路」→「中途改規則」；「短箭／中箭／長箭」→「改向箭長 N 格（短／中／長）」；「提示不會告訴你」→「紙不會告訴你」。

### C. 證據 chips 升級（v0.2 §3-1 最小版）

頂欄 `0/5` 由「只列已取得」改為**五張全列**：`○`（未取得，tooltip＝取得條件）／`✓`（已取得）。K1–K5 名稱與條件依 v0.2 §3-1 表。

### D. 全章證據解鎖儀式（總監新指示）

原施工只把 `gainHit` 正規式擴寫為更多章節文案。2026-07-27 Codex 實查發現：第五章 J1–J3 由實驗引擎直接入卷，沒有取得系統行，仍不會命中文案正規式；因此文案契約不能保證「每份證據」。

修正後以 `state.evidence` 新增為主契約：有取得台詞走結構化 `item.evidence`，無取得台詞走 `bd:evidence`；兩路都呼叫同一個 `playEvidenceGain()`，播放證據圖、金色脈動與雙音。舊正規式只保留給尚未結構化的斷言／筆記解鎖句。

## 二、不改的東西

- `data/*.json`（Sol 未提交，鐵律）；資料層的取得句若需改寫以命中契約，**由 Sol 隨其資料收工一併處理**。
- 引擎judgment、fixture、相位順序、`proofAudit`。
- v0.2 §3-4／3-5（proof 洩答與空槽標籤）**本 CR 不含**——那兩項涉及選項與 SVG 語意，依原排序待 ch3 試玩後施工。

## 三、驗收

- [x] 五章 31 個 evidence code 全量盤點；第五章 S6、S7、J1–J4 六張缺圖已補齊並接入 `assets.evidenceVisual`。
- [x] 契約測試改為從五章 `evidenceNames` 自動盤點，並鎖住有／無取得台詞兩條解鎖路徑。
- [x] 施工中被兩條既有測試擋下並依測試修正：①`stage-ui.js` 是 part 檔生成物 → 改 `04-typewriter.part.js` 後以 `build-stage.mjs` 重建（GB-ADR-015）②「第四章 HUD 不得把未來證據演成打勾清單」→ chips 改為**已取得 ✓＋「這一輪追：X」**（僅當前目標＋條件 tooltip），未來不列。**若總監要五張全列，須先修改該治理測試（總監裁）。**
- [ ] 真人走查：ch4 每相位入場可見目的卡；vectors 無設計語言；chips 五張全列。
- [ ] 真機人工驗收：各章各取得一份證據，確認開聲時每份只響一次、關聲時無聲，且低高度畫面不遮操作。

## 四、法源同步

- 《工作台與辯論架構・一二章實證規格》升 v0.1.3：§3-2b 改為狀態契約，新增 §3-2d「每份證據都必須有圖」。
- repo 內 skill 鏡像 §三已同步狀態契約與全量配圖守門；Claude 帳號生效版仍需由 Claude 自行同步。
