# 總監裁決紀錄:滿版舞台殼與逐字演出(2026-07-19)

**性質**:裁決紀錄+實作交付說明(decisions.md 現有 Sol 未提交編輯,故另檔記錄;待其提交後由任一方把裁決條目併回 ADR 清單)
**裁決者**:總監|**實作**:Claude

---

## 一、裁決內容

1. **滿版舞台殼現在就搭**(不等 Sol 首批圖定版面)——正式呈現=逆轉裁判式:場景背景鋪滿、立繪站位、底部對話框+人物名牌。佔位色塊先行,Sol 的 runtime WebP 落地即自動換裝。
2. **逐字演出範圍:對話逐字、旁白與系統行整句淡入**。一律支援點擊跳完、再點翻頁;`prefers-reduced-motion` 全顯不動畫。

法源對齊:美術規格 v0.2 §2.1 本就把「對話框、人物名牌、選項、提示」列為全域資產;本裁決把呈現形式定案並提前實作表現層。**不改任何凍結基線**(劇本 v0.2.2/章規格 v0.1.1/灰盒規格 v0.1.2):引擎、資料、玩法、文字零變動,純表現層新增。

## 二、實作架構(雙殼一心)

```
chapter.html(灰盒殼,Sol 驗證基準,永不重構)──┐
                                             ├── chapter-ui.js(唯一遊戲 UI 邏輯)── narrative.js/engine.js/data(唯一真相)
stage.html(舞台殼,候選正式呈現)+ stage-ui.js ─┘
```

- **chapter-ui.js 只加了事件廣播**(`bd:line`/`bd:scene`/`bd:view`/`bd:start`,CustomEvent 單向發佈):chapter.html 無訂閱者=灰盒行為零差異。
- **stage-ui.js = 純表現層**:只訂閱事件+唯讀 scenes/assets;禁碰引擎與存檔(測試把關,見下)。打字機 30ms/字;換景淡入吃 tokens 的 `--motion-*`;立繪支援 ART-ADR-001 分層合成;玩家發言時對方立繪壓暗。
- **回顧抽屜**=原「紀錄流」降級安置(旅人筆記:證據列表+完整逐字稿),等於逆轉裁判的 backlog;螢幕閱讀器由 log 的 aria-live 播報一次,打字機視覺層 aria-hidden(不重複播報)。
- **存檔互通**:兩殼同一把 `bd_ch1_save`——同一份進度,雙殼隨時互換。

## 三、驗證

- `npm test` 49/49(新增「舞台殼 DOM 契約」:chapter-ui 引用之全部 id 兩殼皆備;載入順序;stage-ui 純度=出現 `GB.Narrative|GB.Engine|localStorage` 即紅;四個事件掛點兩端對齊)。
- jsdom 無頭煙霧(驗證器不入 repo):新遊戲→開場句即顯→逐字 held→跳完→選項→回顧抽屜 24 行→全過。抓到並修復一蟲:**全新開局 transcript 為空,對話框空白**——表現層於開局代按一次繼續(走 DOM 按鈕,不觸引擎),讀檔則即顯停留句。
- 真機(瀏覽器)檢查項已補進 `greybox/docs/browser-checklist.md` G 節,待總監實機簽核。

## 四、與 Sol 的接口(不變+一項提醒)

資產接口完全不變:填 `assets.js/.json` 的 path 即上圖。**提醒**:背景現以 `object-fit: cover` 滿版裁切,16:9 構圖之外的邊緣會被裁——關鍵資訊請留在安全框內(異議或需改留白策略,開 C 級意見即可)。

## 五、遺留(非阻塞)

音效/BGM 未定(§美術規格音訊預算內);打字速度分角色差異(辛普里奧慢半拍?)候補;辯論「異議!」式 FX 候補——皆屬加分項,等第一章候選版體驗後由總監排優先序。

## 六、第一輪視覺修正(同日總監試玩後裁決;Sol 設計批評+Claude 實作)

**裁決**:①旅人筆記改**全畫面筆記本模式**(桌機雙頁:左=證據+實驗快照/右=完整紀錄;低高度視窗=分頁;Esc+焦點歸還;44px 觸控;舞台暫停壓暗;`bg_notebook` 資產掛點留給 Sol)。②普通對話肖像移**對話框左側半身像**(桌機 170–220px;現行 600×600 筆記頭像僅為柔邊遮罩 fallback,**根因承認:先前誤將筆記頭像放大充當舞台立繪**;新接口鏈 `speakerDialoguePortrait`→`speakerPortrait`;禁 CSS 鏡像;大型立繪留高潮演出)。③字級全面放大(對話 clamp 24–34px、選項≥44px 高、手機橫屏 media query、HUD 收斂)+雙字體軌(對話=思源宋系 serif 落地棧/UI=系統黑體棧;不下載 webfont,WOFF2 子集留正式版評估)。④打字機 40ms 基速+標點停頓(逗短/句長);**超過約三行=表現層分頁,對話框禁捲軸**(不改劇本文字);句完後 Enter/Space 觸發唯一繼續(表單/選項/筆記不誤觸)。⑤預載改**場景範圍**(當前+下一場景+對話肖像),禁全 manifest(首屏 3MB 預算)。

**驗證**:npm test 50/50(新增視覺修正契約:筆記 modal 語義/遮罩 fallback/禁鏡像/clamp 下限/分頁存在/禁捲軸/idleAdvance/charDelay/禁 preloadAll);jsdom 煙霧 17 項全過(含 idle Enter 推進、筆記焦點進出歸還、半身像 has-bust、書信碼正反例)。實機驗收=checklist G2 節(1280×720+844×390+200% 文字放大),待總監/Sol 簽核。

**給 Sol 的肖像框規格(正式半身像據此輸出)**:顯示高桌機 170–220 CSS px/手機橫屏 110–150(底部對齊對話框下緣、向上突出;左緣 16px);建議來源=透明背景 WebP、高 ≥660px(2× 裕度)、自然寬高比(約 3:4);去背+alpha 清邊由 Sol 母版重生;**程式端永不鏡像**。筆記本底圖掛點=資產槽 `bg_notebook`(攤開雙頁、文字區留白,文字仍為 HTML)。
