# 《發現之前》第一章功能規格書(全章增補)

**版本**:章規格 v0.1.1
**狀態**:🔒 **已凍結**(2026-07-19——Sol 第二次 verification 通過,見 `05_審核/發現之前_M3第二次verification_Sol_20260719.md`;後續改動須走正式變更紀錄)
**變更紀錄**:CR-001(2026-07-19,總監核准)——§7 增補 R-SAV-05 書信碼;既有條文零改動。詳 `03_規格/發現之前_章規格變更紀錄_CR-001_書信碼_20260719.md`
**作者**:Claude|**審稿**:Sol|**定稿**:總監
**日期**:2026-07-19
**法源**:架構計劃書 v0.3、劇本 **v0.2.2**(場景 ID 之唯一來源)、灰盒功能規格書 v0.1.2 🔒(**全部 R-ID 原文繼承,本文件不重述、不修改**)、ADR-005/006/007/**008/009**。

> **v0.1 → v0.1.1 變更摘要(逐條處置審稿)**:①【A-1】E3.c 改雙模式必經(R-HOOK-02/R-MODE-02/R-DEB-12 同步);FR 引擎入口驗證 E3.c。②【A-2】R-NAR-05 白名單 class 封閉列舉(character-hypothesis/post-reveal),未知值必敗;character-hypothesis 須 refutedBy。③【B-1】R-TWR 刪 q2 場地假選擇。④【B-2】R-REP-02 定為「立即導流+一次性系統行提示」。⑤【B-3】R-SAV-02 壞檔備份+標題頁非阻塞提示,載入結果四分類可測。⑥【B-4】R-END-02 史實標籤 enum 擴充。⑦【程式審查 B-1】新增 R-DEB-15 證據所有權單一語義。⑧上游裁定 4.1/4.2 明文化。⑨【C-1】法源版本更新。
**撰寫原則**:僅描述外部行為、狀態與驗收準則;新增規範編列新 R-ID;與切片重疊處一律引用切片 R-ID。

---

## 0. 範圍

**含**:序幕→尾聲之全章可玩流程(劇本 v0.2 全部場景)、敘事引擎、信譽系統、高塔實驗、E4 對照實驗、三支柱+最後反撲之完整辯論、探索/學者模式分層、localStorage 存檔、章末史實頁。
**不含**:美術音效(灰盒紀律仍適用,R-ENV-04)、AI 功能、行動裝置、成就系統、多語、第二章。
**互動顆粒度原則**:斜面實驗沿用灰盒實驗台(R-EXP 全套);其餘實驗(高塔、E4)為**敘事內嵌之低顆粒互動**(選擇+固定結果),不做數值模擬——依憲章一,它們是證據取得與變因教學,不是核心實驗迴圈;核心迴圈唯一歸斜面。

## 1. 敘事引擎(R-NAR)

- **R-NAR-01 資料驅動**:全章敘事以場景圖定義於 `/data/scenes.js`(+`scenes.json` 鏡像,R-DATA-05 適用)。場景 ID 與節點標籤**逐字對齊劇本 v0.2**(如 `P0-2`、`A2-3.a`)。引擎通用,內容即資料。
- **R-NAR-02 節點類型**:`line`(台詞/演出)、`choice`(選項)、`system`(量表/證據事件顯示)、`embed`(內嵌子系統:實驗台/辯論廳/高塔/E4/修復事件)、`goto`。效果原語:`rep±n`、`gainEvidence(id)`、`setFlag(k,v)`、`gotoScene(id)`。
- **R-NAR-03 守衛與匯流**:選項與節點可帶 `require`(旗標/證據/信譽條件);不滿足之選項**隱藏**(非灰化,避免暗示)。分支必以顯式 `next` 匯流;全章分支結構須與劇本 v0.2 附錄 A 一致。
- **R-NAR-04 模式標記**:節點可標 `mode ∈ {all, explore, scholar}`;引擎依模式過濾。
- **R-NAR-05 結論詞彙 lint(刪台詞測試機器化)**(修訂|A-2):資料層維護**禁詞表**與**白名單**;白名單 `class` 為**封閉列舉** `{character-hypothesis, post-reveal}`,由資料檔 `lint.classes` 宣告且不得超出此二值——**未知 class、錯字、擅增類別,測試一律必敗**。
  - `character-hypothesis`(劇中人自己的錯誤/前期理論):必填 `reason` 與 `refutedBy`(指向**存在且為玩家互動節點**(embed/choice)之座標);不得用於 system/stage 之正典結論。
  - `post-reveal`(玩家揭曉後之覆述):必填 `afterPlayerNode`,且該節點存在並位於白名單節點**之前**。
  - lint 為**防回歸工具,不取代人工語義審稿**;白名單變更須記 changelog。負向測試(未知 class/缺 refutedBy/無效目標/缺 afterPlayerNode)常駐。
- **R-NAR-06 埋點事件**:`gainEvidence`、`rep 變動`、死路 A/B 抵達、辯論中止、章末,寫入 session 內事件log(記憶體+存檔),供形成性試玩回放觀察;不外傳(零資料蒐集不變)。

## 2. 信譽系統(R-REP)

- **R-REP-01**:信譽整數 0–5,起始 3,顯示於狀態列。變動事件以劇本 v0.2 附錄 A 為準(資料驅動,不寫死於程式)。
- **R-REP-02 歸零軟鎖**(修訂|B-2):信譽=0 → 標記 `repLocked` → **立即導流**至修復事件【SC-R1】,導流前顯示一次性系統行「(信譽歸零——關鍵人物暫時拒絕與你交談。)」。此提示為 R-NAR-03「不滿足即隱藏」原則之明文例外(鎖定原因必須可見)。外部行為唯一:不採「留在原頁顯示鎖定選項」方案。
- **R-REP-03 修復事件**:SC-R1=完成任一次斜面實驗並「出示紀錄」→ 信譽回復至 1、解鎖,返回原節點。可重複觸發。
- **R-REP-04**:信譽在辯論中之扣減(A3-3.a、A3-5.trap)與說服力扣減**獨立記帳**,兩表各自顯示。

## 3. 高塔實驗(R-TWR)——場景 A1-3/A1-4

- **R-TWR-01 兩選單**(修訂|B-1):丟什麼(`q1`)/找誰看(`q3`,設 `w ∈ {student, assistant, none}`)。場地由敘事決定(大教堂鐘樓,劇本 v0.2.2)——原 `q2` 為無後果假選擇,依規範 2 防偽條款刪除;不得再引入寫入後不消費之旗標。
- **R-TWR-02 單變因勸阻守衛**:`q1=B(鉛+木)`→ 進勸阻節點(伽利略台詞)→ 返回 `q1` 且選項 B 標記「已勸阻」不可再選。筆記寫入「一次只動一個變因」。
- **R-TWR-03 執行與結果**:選項齊備 → 塔上演出(固定結果文本:兩聲近乎重疊、重者先一線)→ `gainEvidence(E1)`;E1 為**觀察卡**(含 w 註記),非數據 run,不進判定選集。
- **R-TWR-04**:w 影響 A1-5 之插入台詞與 P1 辯論之 E1 特殊回應措辭(僅文本差異,無數值差異)。

## 4. E4 對照實驗(R-E4)——場景 A2-4

- **R-E4-01 紙實驗(只動形狀)**:三個操作按鈕:攤平放手/揉團放手/上天平比對。三者皆執行(順序自由)→ 紙結論節點解鎖(玩家自選結論選項:「變的是形狀」/「變的是重量」——後者被天平結果駁回,重選)。
- **R-E4-02 球實驗(只動介質)**:兩個操作:空氣中放手(銅大+銅小)/水缸中放手。皆執行 → 球結論節點(玩家自選:「球變了」/「介質變了」)。
- **R-E4-03**:兩實驗皆完成且結論正確 → `gainEvidence(E4)`。E4 卡面記載兩組「單一變因差」摘要。
- **R-E4-04 誠實條款**:結果文本不得使用「完全同時」表述空氣中之銅球對照;一律「近乎同落/差一線」。真空表述必附條件(沿架構憲章四)。

## 5. 辯論系統全章擴充(R-DEB-09 起;R-DEB-01~08 原文適用於每根支柱)

- **R-DEB-09 支柱序列**:辯論資料新增 `pillars: [P1, P2, P3, FR]`,依序進行;每根支柱結構同 §6.1(三句證詞、弱點、追問文),weakTo:**P1=(E4→證詞1-2)、P2=(E2→證詞2-2)、P3=(E3.b→證詞s2,沿切片)**。前一支柱破裂才進下一支柱。
- **R-DEB-10 特殊回應表**:資料層支援 `special` 條目——三元組→(回應文,量表效果,旗標)。初值一條:P1 之 (E1,-,證詞1-2) → 「又是幾乎」回應,**量表不扣、支柱不動**,筆記提示解鎖。此表資料驅動,可擴充。
- **R-DEB-11 追問內嵌選項**:證詞之 `press` 得攜帶 `choice`(如 A3-3 追問 2-3 之「憑誰立論」):選項效果沿 R-NAR-02 原語(A 選項:rep−1、persuasion−1、返回選項)。
- **R-DEB-12 最後反撲(FR/E5)**(修訂|A-1):P1–P3 全破後觸發。**引擎入口守衛:E3.c=false 時一律拒絕推進 FR(不改 state);負向測試必備**——探索兩步與學者組鏈皆為玩家已驗事實之覆述,不得引用未取得證據。
  - 〔探索〕兩步引導選項(`A3-5.e1/e2`),答錯僅重選,不扣量表;
  - 〔學者〕三槽組鏈:槽位=①各傾角皆成立②增陡不改形式③垂直為極限方向;干擾項三條(空話/次數/訴諸權威),**選入干擾項該槽退回且說服力 −1**;
  - 組鏈完成 → **trap 抉擇節點**(`A3-5.trap/honest`):「量過」→ 說服力 −2、信譽 −1、強制轉 honest;「沒有」→ 直接 honest;
  - honest 完成 → `gainEvidence(E5)`、FR 標記 resolved。
- **R-DEB-13 勝利與中止**:勝利=P1∧P2∧P3 broken ∧ FR resolved → 判定演出(A3-6)→ 尾聲。說服力歸零(任一階段)→ 中止(R-DEB-07 語意)→ 返回第二幕,**全部支柱與證詞狀態保留**;再入辯論從第一根未破支柱續,量表重置 5,已破支柱以一行帶過(不重打)。
- **R-DEB-14**:E3 出示時之子項展開、E3.b 守衛,沿切片 R-DEB-03/04/05 不變。
- **R-DEB-15 證據所有權**(新增|程式審查 B-1):單一語義函式 `ownsEvidence(state, evidence, subitem)`——E3 依子項查實驗台狀態,其餘查章節證據;**引擎與 UI 共用**。未持有之證據出示一律回錯且不改 state;UI 僅列已取得之證據與 E3 子項。竄改存檔不得繞過。

## 6. E3 敘事掛鉤(R-HOOK)——場景 A2-3

- **R-HOOK-01**:`E3.a 首次點亮`事件 → 觸發場景 A2-3(伽利略模仿橋段)。場景內選項 `b(吵渴望)`被駁回重選;選項 `a(換球)`→ `embed` 實驗台(預選銅小,玩家仍可自由改配置——**不鎖自由度**,僅預選)。
- **R-HOOK-02**(修訂|A-1):`E3.b 點亮`→ A2-3 收尾台詞後**接 E3.c 複驗,雙模式必經**——探索=引導式(預選中傾角,玩家仍可自由改配置);學者=自由配置(含音格)。`E3.c 點亮`後才開放 A2-4 與 A2-5。終辯外推之一切「各傾角」前提,自此皆為玩家親手取得之證據。

## 7. 存檔(R-SAV)

- **R-SAV-01**:localStorage 單一鍵 `bd_ch1_save`,內容={schemaVersion, mode, 敘事游標(場景+節點), 三層狀態(R-STA-01~03 全量), rep, flags(w 等), 事件log, 章末兩題文本}。每場景轉換與每次 embed 子系統返回時自動存。
- **R-SAV-02 容錯**(修訂|B-3):載入結果四分類且可測——`empty/badjson/schema/cursor`(純函式 `loadSave`)。後三者:壞檔改鍵備份 `bd_ch1_save_corrupt`、移除原鍵,**標題頁顯示一次性非阻塞提示**(「舊進度無法讀取,已備份;請開新遊戲」),不得只讓「繼續」按鈕消失,不得用 modal 阻塞。
- **R-SAV-03**:標題畫面提供「繼續/新遊戲(需二次確認)」;新遊戲清空存檔。
- **R-SAV-04**:存檔往返測試:任意狀態 serialize→deserialize→深度相等,入自動測試。
- **R-SAV-05 書信碼**(CR-001 增補|總監核准 2026-07-19):標題畫面匯出(填欄+複製+下載 .txt,原文=serialize 結果,不加密)/貼碼續玩(唯一入口=`loadSave` 四分類;非法碼不得動本機存檔);介面明示「貼碼會覆蓋本機進度」。動機:共機互蓋與跨裝置。詳 CR-001。

## 8. 模式分層(R-MODE)

- **R-MODE-01**:新遊戲時選擇 探索/學者;存入存檔;v0 中途不可換。
- **R-MODE-02 差異表(規範)**:

| 環節 | 探索 | 學者 |
|---|---|---|
| 斜面配置 | 全自由(同灰盒) | 全自由+音格計時解鎖 |
| 死路 B 診斷 | 可複選診斷選項(A2-2) | 相同(診斷即教學,不因模式剝奪) |
| E3 | **a+b+c 必經**(c=引導式預選) | **a+b+c 必經**(c=自由配置) |
| 最後反撲 | 兩步引導 | 三槽組鏈+干擾項 |
| 綁縛悖論加問 | 無 | A1-7 加問(「矛盾何時消失」) |

  註:音格於探索模式**不出現於選單**(非灰化);切片通過矩陣不受影響(矩陣以配置空間全集定義,測試不分模式)。
- **R-MODE-03**:模式差異一律資料標記(R-NAR-04/資料欄位),禁止程式內 if 散落(單一過濾機制)。

## 9. 章末與史實頁(R-END)

- **R-END-01**:A3-6 判定 → 尾聲(E-1、E-2)→ 兩題自由輸入(存入存檔,不評分,R-UI-02 語意)→ 史實與虛構頁。
- **R-END-02**(修訂|B-4):史實頁資料驅動(`/data/histfacts.js`+json 鏡像),條目=劇本 v0.2.2 史實頁表全文;每條含 `label`,取值限定於資料檔宣告之 enum:**史實/傳說/戲劇改編/教學代理/合理重建/考據性重建/概略化/史實記載但解釋有爭議**;擴充須經規格變更並記 changelog;自動測試驗證每列 label ∈ enum,不得把不確定性壓扁成「史實」。
- **R-END-03**:章末顯示總天數與(若曾)辯論中止次數——與 R-UI-02 併版面。

## 10. 資料格式(規範性骨架,新檔)

```json
// data/scenes.json(節錄骨架)
{ "startScene": "P0-1",
  "scenes": [
    { "id": "P0-2", "nodes": [
        { "id": "n1", "type": "line", "speaker": "galileo", "text": "…", "next": "q1" },
        { "id": "q1", "type": "choice", "options": [
            { "id": "a", "text": "其實——所有東西落得一樣快。", "effects": [{"rep": -1}], "next": "n2" },
            { "id": "b", "text": "(先聽他說完)", "next": "n3" } ] },
        { "id": "n9", "type": "goto", "scene": "P0-3" } ] },
    { "id": "A2-2", "nodes": [
        { "id": "lab", "type": "embed", "system": "incline", "next": "…" } ] } ],
  "lint": { "bannedWords": ["平方", "1:3:5:7", "奇數", "與重量無關", "一樣快"],
            "whitelist": [ {"scene": "A2-2", "node": "…", "afterPlayerNode": "…"} ] } }
```
```json
// data/debate.json 擴充(舊欄位不刪改,R-DATA-04)
{ "pillar": "P3", "persuasion": 5, "statements": ["…沿用…"],
  "pillars": [
    { "id": "P1", "statements": [ { "id": "p1s2", "text": "…", "weakTo": {"evidence": "E4"} } ],
      "special": [ { "evidence": "E1", "target": "p1s2", "reply": "…", "persuasionDelta": 0 } ] },
    { "id": "P2", "…": "…" },
    { "id": "P3", "useLegacy": true },
    { "id": "FR", "type": "finalRebuttal",
      "explore": { "steps": ["…"] },
      "scholar": { "slots": 3, "correct": ["…"], "distractors": ["…"] },
      "trap": { "penalty": { "persuasion": -2, "rep": -1 } } } ] }
```
- **R-DATA-06**:scenes/histfacts/debate 擴充皆雙載體+鏡像測試(R-DATA-05 延伸);切片 debate 舊欄位與切片測試**不動**。

## 11. 驗收準則對照

灰盒四問:①高塔+斜面配置自由(R-TWR-01/R-EXP-01)②死路 B 診斷+R-JUD-04③預測制+lint(R-NAR-05)④三元組指認(R-DEB 全套)。六門檻=四問+兩題訪談(ADR-004;R-END-01 掛點)。自動測試面(v0.1.1 擴充):lint 封閉列舉含負向、存檔往返+loadSave 四分類、支柱序列狀態機、E4/高塔守衛、**E3.c 負向(FR 拒絕+入辯論前斷言)、證據所有權負向、trap 雙歸零(說服力=1 且信譽=1 時 lied→中止+修復→再入 FR→honest→完章)**。瀏覽器人工清單:全章鍵盤通關+存檔斷點續玩。

## 12. 依賴與里程碑切分(實作序)

M1 敘事引擎+序幕+第一幕(高塔、死路 A、E2)——可玩到幕間|M2 幕間+E4+E3 掛鉤+存檔——第二幕完整|M3 辯論全章+尾聲+史實頁+lint 全綠|每一 M 結束跑全部自動測試+更新瀏覽器清單。程式審查(Sol)每 M 一輪(ADR-002/003)。

## 上游裁定明文化(審稿 §4)

辯論再入自**第一根未破支柱**續戰(已破支柱一行帶過,不重打);死路 B 診斷為**兩模式共有**(模式差異在配置自由度與論證組裝,不剝奪必要回述);壞檔備份保留+非阻塞提示(R-SAV-02)。

## 作者自檢聲明

刪台詞:結論詞彙 lint 把測試機器化(R-NAR-05),白名單初值僅含玩家操作後節點——通過。因果回述:死路 A/B、E4 結論自選、FR trap 皆有回述掛點;事件log 供試玩觀察(R-NAR-06)——支持。憲章三問:認知主體(E4 結論由玩家選、FR 逐步由玩家組)、證據因果(單變因守衛 R-TWR-02/R-E4 結構)、誠實(R-E4-04、trap 機制本身即「不得謊報證據邊界」的機制化)——通過。

## 上游問題清單(呈報,不擅改)

1. 劇本 A3-F 之「再入從未破支柱續」為本規格細化(劇本未明言);若 Sol 認為應全部重打,呈總監裁決。
2. R-MODE-02 將「死路 B 診斷」設為兩模式共有(設計文件 v0.1 模式表為探索專屬引導)——依 ADR-007 優先序,以本規格為準,請審稿確認。
3. 存檔鍵名與 schema 為新資產;R-SAV-02 之壞檔備份策略請審稿確認必要性(可降級為直接重開)。
