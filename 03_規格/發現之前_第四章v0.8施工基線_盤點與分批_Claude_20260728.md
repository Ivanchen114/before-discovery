# 第四章 v0.8 施工基線:runtime 盤點與分批計畫

**日期**:2026-07-28|**作者**:Claude
**授權**:總監 2026-07-28「第三章第四章都做完,再一次請 Sol 審,審完我再試玩」。
**地位**:ch4 進 runtime 的**唯一施工起點**。所有數據為當日實撈,133/0→134/0 綠燈狀態。

---

## 一、好消息:機制層比預想近很多(實撈 engine4.js)

| v0.8 契約 | engine4 現況 | 差距 |
|---|---|---|
| **A-5|預測先於演示** | ✅ **已成立**。`runOrbitRule(config, prediction)` 要求方向+速度+箭長+形狀預測**一次提交**,模擬在其後 | 無 |
| **A-5|3×3 多解** | ✅ **已成立**。`ORBIT_SPEEDS {0.82, 1, 1.18}` × `ORBIT_STRENGTHS {0.67, 1, 1.39}` **數值就是照 v² 配的**:慢×短、中×中、快×長 → near-circle | 驗收時跑一次數值確認判準帶寬 |
| 切線預測紙 | ⚠ 半成立。`o.tangentRecord` 會自動生成(「在操作前封存的無作用切線預測」),但**不是玩家操作、也不是證據** | **K0 要新增為證據**,由玩家在 D1-1 親手封存 |
| **A-3|借條四狀態** | ❌ **未成立**。`modelOutcome` 表格把補丁寫死(`planets: patches:2`、`comet: patches:2`)——玩家沒貼過借條,結果照樣報 2 張。**系統替玩家生成操作**,Sol A-3 的原始指控 | engine4 改造:`slipPlanet`/`slipComet` 由玩家動作設定;四狀態結論;證據包借條數=實貼數。spike `spike_ch4_d31_ledger.html` 已有 CANONICAL 參考實作 |
| A-4|雷恩 | 場景層台詞問題 | 隨場景層一起改 |

## 二、場景層:重排對映(實撈現行 14 場)

現行:D0-1(13)/D0-2(21)/D1-1(17)/D1-2(12,orbit)/D1-3(19,orbit)/D2-1(15)/D2-2(10,orbit)/D2-3(19,orbit)/D3-1(12,orbit×2)/D3-2(10,orbit)/D3-3(20,orbit)/D3-4(8)/DE-1(17,orbit)/DE-2(12)=**205 節點,證據 K1–K5(無 K0)**。

v0.8 重排(遷移表 `03_規格/發現之前_第四章v0.8存檔遷移表與契約測試` 已載,此處為施工序):

| 步 | 動作 | 危險度 |
|---|---|---|
| 1 | D1-2/D1-3(1665 作圖)整段搬到 D2-1 之後(1679)——**embed 閘門 `until:{orbit:…}` 跟著搬,閘門順序=章能不能走完** | 高 |
| 2 | D2-2(同尺紙)搬到 D1-1 之後(1665),補 A-1 兩張紙拆開的台詞 | 高 |
| 3 | 新增 D2-2′(1680-01-06 虎克平方反比信・牛頓沉默)——純 line 場,無 embed | 低 |
| 4 | 新增 INT-1(幕間十四年) | 低 |
| 5 | K0 證據:evidenceNames/Summary + D1-1 效果 + 引擎 assert | 中 |
| 6 | 全場台詞照 v0.8 稿(含 A-4 雷恩「一本值四十先令的書、兩個月」、誘答鍵拆兩顆的台詞) | 大宗但低險 |
| 7 | schema 1→2 + 節點級遷移(**唯一回收證據的遷移:舊 D1-2/D1-3 游標 → K1 回收+可讀訊息**) | 高 |
| 8 | 18 條契約測試(遷移表 §三) | 中 |

## 三、分批(為什麼不一口氣)

ch3 的轉入是**純增拍**(不刪不改 id,免遷移),一段做完可行。ch4 是**結構重排**:場序、embed 閘門、證據 ID、存檔 schema 全動。這種改動做一半斷在中間,章會直接走不完——所以必須一批做完**並在同一批裡跑完 18 條契約**,不能切碎。

**批次 4A(下一工作段,一次做完)**:上表 1–6+新台詞全量。
**批次 4B(緊接)**:7–8(遷移+契約)+ A-3 借條改造 + K0 assert。
兩批之間 **不 commit、不交審**——4A 完 4B 完、測試全綠,才連同 ch3 一起交 Sol。

## 四、給下一工作段的錨點

- v0.8 稿:`04_劇本/第四章台詞稿_v0.8_Claude_20260728.md`(現行線,五項 A 級已修)
- 遷移表+18 契約:`03_規格/發現之前_第四章v0.8存檔遷移表與契約測試_Claude_20260728.md`
- CANONICAL spikes:`spike_workshop/README_ch4_spike索引.md`(D2-1 v3 / D3-1 v2 / d31_ledger)
- 對話框化:ch4 的 orbit 教練句 17 句(WB-CR-025b)可在 4A 一併入橋——格式照 `講者：（動作）「台詞」` 寫,動態契約自動涵蓋
- 跨章規格:ch4→ch5 接縫斷裂記錄在 §3(ch5 展開時處理,**不在 4A/4B 範圍**)
