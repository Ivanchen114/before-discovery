# B1／B2 修正・五提交乾淨工作樹|Claude 定點複審回覆

**審查**:Claude|**日期**:2026-07-31|**真相模式**:CONFORMANCE|**lane**:R0
**主審**:`d170103`／`365599c`／`f3d8165`／`4894856`／`9765020` + B-1／B-2 修正
**判定:A=0、B=0、C=1 —— 通過。**兩條 B 都真修,五個提交分包乾淨,候選沒有被偷升 active。

---

## 一、B-1|風的重複支付:修得比我要求的完整

我上輪要求「辯論不再重問 + 舊存檔仍補問」。實查是**雙層守衛**:

**engine 層**(`engine3.js` cabin 步):

```js
db.p1.cabin = true; db.p1.concept = true; db.p1.steady = true;
if (d.designCommitments.cabinWind === "close-cabin") {
  db.p1.wind = true; db.pillars.p1 = true; db.current = "p2";
  db.lastReply = "…槳手:「你在動手前已把話寫小:這組只回答關艙後的比較。好,下一問,船長那張舊紙。」";
  dossierOS(d, "p1-done");
} else {
  db.lastReply = "…槳手:「好。可這只隔開了甲板風——你最多敢把結論寫到哪裡?」";
}
```

**UI 層**(`chapter-ui.js:4336`):

```js
} else if (!db.p1.wind && !(d.designCommitments &&
    d.designCommitments.cabinWind === "close-cabin")) {
```

**兩件我沒要求但做對的事**:

1. **NPC 台詞改成引用玩家先前那句**——「你在動手前已把話寫小」。我上輪只寫了「改為引用」四個字,Sol 把它寫成了真的台詞,而且是槳手的口吻。
2. **雙層都擋**。只改 UI 會留下 engine 仍可被舊入口呼叫的縫;只改 engine 會讓 UI 畫出一個立刻自動完成的按鈕。兩層一致才是對的。

舊存檔路徑保留完整補問,`else` 分支的台詞維持原樣。

---

## 二、B-2|ch1 ERROR:修法比我建議的好

我建議「修 `P0-2/nA3` 去掉引號」。Sol 沒有刪引號,而是**把引號移進句中當引述**:

| | |
|---|---|
| **修改前** | 「你說的『大家都知道』,正是……」(整句以「開頭) |
| **修改後** | `你說的「大家都知道」,正是亞里斯多德最厚的一面牆。他那邊的「大家」,已經知道了一千九百年。(轉回書桌)在比薩,斷言不用錢。證據才值錢。` |

外層引號拿掉,**內層兩處引述保留**——原句的力量(把「大家都知道」當成被檢驗的對象)完全沒有損失。這比我建議的「去掉引號」精確。

**豁免品質**:ch1 `A3-D`、ch2 `B3-D` 各逐場逐節點(`e1`、`gF`、`gW`)登記,理由具體到「本場由辯論引擎承載完整可見攻防;scenes 層只保留 embed 與成功／中止出口,**不應為介面對稱硬塞台詞**」。**沒有用 pattern 全免**,符合我在規範 §七 訂的條件。

**四個 R-TXT-5 warning 誠實留著**(`A1-4/n5`、`A1-7/m3`、`B0-2/s1`、`B1-2/s1`),沒有為了全綠去改兩個凍結章的舊台詞。這個判斷我同意——那四處是既有證據簽收的診斷,應該獨立排程,不該搭這班車。

---

## 三、五組數字與五個提交

| 宣稱 | 實測 |
|---|---|
| `git status --short` 空白 | ✅ **0 行** |
| runtime 138/0 | ✅ **138 通過、0 失敗** |
| skill 64/64 | ✅ **Ran 64 tests — OK** |
| validate 0 errors／20 warnings | ✅ PASS |
| activation 6 errors | ✅ FAIL 6 errors,逐條吻合:`ch3.cr-026`／`027`／`028`／`029`、`ch5.cr-002`、`registry_status` |
| ch1/ch2 各 0 error／2 warning | ✅ 精確吻合 |
| ch3–ch5 全 0／0 | ✅ |

**分包精確性**(我最在意的一項):

| 提交 | 檔數 | 範圍檢查 |
|---|---|---|
| `d170103` | 2 | 治理文件 |
| `365599c` | 145 | 設計/劇本/CR/審核/spike;**只有 1 個 greybox 檔**(`docs/playtest-record-template.md`,是文件不是 runtime)✅ |
| `f3d8165` | 166 | 美術原檔與授權;greybox 側只帶美術接線(`assets.*`、`stage-ui.js`、`03-focus-visual.part.js`、`run-ch4-art.mjs`)✅ |
| `4894856` | 17 | **路徑全部落在 `greybox/{data,src,tests}`**,零文件混入 ✅ |
| `9765020` | 7 | skill 原子包 ✅ |

**候選未被偷升 active**——這是我最想確認的一件事。`registry` 從 `6cb02d6` 到 HEAD 的狀態欄位 diff:

```
7 × +"status": "candidate"
4 × -"status": "missing"
```

**零筆 candidate → active。**4 筆 `missing → candidate` 是因為那些檔案終於進了 Git,屬合法狀態推進。分 5 包也證明沒有 `git add -A`。

---

## 四、C-1|反向控制我沒跑完(沙盒磁碟耗盡)

我在 /tmp 副本注入變異(拔掉 UI 承諾守衛)後測試從 122/16 → **120/18**,**新增兩條失敗**——方向正確,守衛確實被契約鎖住。但副本基線是 122/16 而非 138/0(沙盒複製造成的環境失敗),所以我**無法宣稱這是一次乾淨的反向控制**;而且我還沒跑完「刪掉 ch1 豁免必須轉紅」那一項時,沙盒磁碟就滿了。

**我不把它算成守住,也不算成缺口。**Sol 的報告 §八 列了反向驗證入口,測試數 64 也比上輪多 1(合理對應新增的 NAR-08 豁免正反測試)。**建議由 Sol 補一次乾淨環境的反向控制截圖或輸出**,或在下一輪交付時附上;這是唯一一項我這輪沒能獨立確認的。

---

## 五、放行建議

**A=0、B=0。可以進 activation 原子切換。**

切換範圍是六項一起:`ch3.cr-026`／`027`／`028`／`029`、`ch5.cr-002`、`registry_status`。其中 **`ch3.cr-027` 我仍未做過獨立定點覆核**——它的內容已被 CR-028、CR-029 兩輪疊過並經我審查,現行 HEAD 行為我背書;若您要求形式完備,我可補一份;若接受「以後續輪次涵蓋」,請在 cr-027 檔頭註記。

**一件現在才成立的事**:我在「最終乾淨鏈」那輪提的 B-1 是「審的 HEAD ≠ 玩的版本 ≠ registry 指向」。**五個提交把這三者對齊了**——工作樹空白、runtime 全綠、registry 指向 HEAD 內容。您現在試玩的版本,就是我審過的版本。這是這幾輪治理工作真正的產出。

**仍須您真人判定**:桌機與 844×390 橫屏、風三選一的誘惑力、第五章展開後的節奏、四張年代圖的年份感受、桅頂熱點好不好按、杜夏特萊 47 句 vs 旅人 25 句的比例感。

---

## 六、我的誤判紀錄

本輪我又一次直接偽造 `assertions.A2` 去驗 cabin 步,得到 `dossier-evidence-missing`——**同一個坑的第七次變體**,而且是我兩輪前才記進記憶的那一條(A2 由 `blind.records` 衍生)。改用讀碼 + 查契約才得到正確結論。

七次的共同點都是**我假設了工具或資料的形狀而沒先看一眼**。這個頻率對一個審查者來說太高,您在採信我的報告時值得知道。本輪的正面是:七次全部在寫進報告前被自己攔下,沒有一次讓錯誤結論出門。
