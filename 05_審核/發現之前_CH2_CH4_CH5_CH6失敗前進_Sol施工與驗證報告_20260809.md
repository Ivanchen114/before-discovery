# 《發現之前》CH2／CH4／CH5／CH6 失敗前進：Sol 施工與驗證報告

- 日期：2026-08-09
- Gate：`DESIGN GATE: PASS — CH2-CR-013 + CH4-CR-013 + CH5-CR-003 + CH6-CR-002`
- 施工狀態：**IMPLEMENTED／自動測試與瀏覽器互動驗證通過**
- 下一棒：Fable 5 敘事忠實度 CONFORMANCE → 總監六章連續實玩
- 版本控制：**未 stage、未 commit、未 push、未 deploy**

## 一、施工結果

四章已依 v0.1.1 核准台詞與四張 CR 寫入 runtime；沒有新增信譽懲罰，也沒有修改四章工作台的物理計算。

### CH2 — 三列預測與押注讀回

- `B2-4/q1` 三種回答不再以重選推進，改記錄 `ch2DropPrediction = late | unresolved | abstain`，共同進入 `c_commit`。
- `q4.a` 依原押注顯示 `r_late／r_unresolved／r_abstain`；`q4.b` 的越界行為仍先處理，不被失敗分支洗掉。
- `B3-1` 只對曾押「晚到」的玩家讀回一次。
- 核准修正句已逐字實裝：`三列紙沒有替它作證。`

### CH4 — 錯線留紙與二十年後讀回

- `D1-1` 的圓弧／向心錯線仍由牛頓以當下可得幾何立即反駁，但錯紙不被抹除。
- `D4-2` 依事件紀錄讀回，優先序固定為 `curve > inward > tangent`；舊存檔無事件者不捏造往事。
- `DE-1` 加入中性檔案句：`曾真正出現的紙`，不把錯誤升格成正面證據。

### CH5 — 尺度斷言進辯論與沉默後果

- 新增 `ch5ScaleClaim`，區分測量限定、兩種越界主張，以及各自的 `open／shrunk／silent` 狀態。
- 玩家越界後不再被強迫重選；可縮句，也可沉默，原句照樣進入辯論並仍算在玩家名下。
- 測量限定路與 legacy 路不被誤加失敗經歷。
- 核准台詞逐字實裝，包括：`油灰坑量過碰撞後兩邊的速度嗎？`、`沉默不算撤回。上桌吧——它還是你的。`
- `debate5` 契約、碰撞工作台與信譽事件均未修改。

### CH6 — 乾式越界宣言與延後追債

- `H1-3` 越界宣言保留原有一次性信譽後果，新增承諾拍後進入真正的乾式證據判定。
- 新 `t2_overreach` 仍呼叫 `judgeDryStrip({ concept: "observed-range-only" })`；T2 不是白送，既有證據門檻不變。
- `H3-1` 由「熱仍進、溫度線卻停住」追債；玩家可承認，也可先替模型辯護，再縮回主張邊界。
- `HE-1` 補入章末回聲；模型可保留，越界勝利宣言不能被洗白。

## 二、額外修復：CH6 合法存檔被 sanitizer 誤判

施工驗證發現既有缺陷：第六章本來已有四個合法信譽扣分來源，但 `sanitizeReputationLifecycle` 未登記 CH6 reason／first-time flag。玩家合法走過既有扣分選項後，重載可能被當成損壞存檔。

本次只補現行規則的 sanitizer 白名單與一致性檢查，沒有改變狀態 schema 或設計含義。瀏覽器已實測 `H1-3` 越界路扣分、取得 T2、重載後仍可「繼續第六章」。

## 三、驗證證據

### 自動測試

在 `greybox/` 執行 `npm test`：

- **172 passed／0 failed**（施工前基線 168／0）
- 新增四組 CR 路徑測試，涵蓋正路、錯路、沉默、縮句、legacy、優先序、防重播、信譽與 sanitizer。
- ch4 migration：19 groups／205 legacy cursors。
- cognitive feedback、chapter art、series home、design governance 全數通過。
- 鏡像與 stage bundle 已重建；舞台 cache key 更新為 `asset-83ea88ff3268`。

### 瀏覽器互動驗證

- CH2：實際選「晚到」，信譽維持 3，顯示 `c_commit`，後續可繼續。
- CH4：實際選圓弧錯線，先見牛頓反駁，再見錯紙被壓到紙角保留。
- CH5：以 `vis-viva-open` 進辯論，走堅持 → 沉默，顯示「仍是你的」，信譽維持 3。
- CH6：實際走乾式越界，信譽 3 → 2；承諾拍後由證據判定取得 T2；重載存檔成功。
- 六章入口均能載入正確章名，瀏覽器 console 無 JS error。

**視覺驗收邊界：**本次互動 smoke server 從 `greybox/` 啟動，因而無法供應上層 `/public/assets/`；互動與 fallback 正常、console 無錯，但不把這次瀏覽器操作宣稱為「正式素材逐張視覺驗收」。素材契約已有自動 art tests 通過，正式視覺仍留給六章整體實玩確認。

## 四、實際施工檔案

Canonical runtime：

- `greybox/data/scenes2.json`
- `greybox/data/scenes4.json`
- `greybox/data/scenes5.json`
- `greybox/data/scenes6.json`

同步鏡像：

- `greybox/data/scenes2.js`
- `greybox/data/scenes4.js`
- `greybox/data/scenes5.js`
- `greybox/data/scenes6.js`

共用驗證／產物：

- `greybox/src/sanitize.js`
- `greybox/tests/run-node.mjs`
- build-stage 產生的 `greybox/src/stage-ui.js` 與入口 cache-key 更新

## 五、尚未越權宣稱的事項

1. **Fable CONFORMANCE 尚未執行：**本報告證明可執行、狀態與測試契約成立，不代替 Fable 對台詞、節奏與戲劇目的的獨立驗收。
2. **總監六章連續實玩尚未執行：**這是最終體驗裁決，重點不是找正解，而是看錯誤是否真的在世界中留下可感後果。
3. **版本控制與發佈尚未授權：**目前沒有 stage／commit／push／deploy；共享工作樹中的其他既有修改與未追蹤檔均原樣保留。

## 六、給 Fable 5 的驗收焦點

- CH2：`r_late` 是否只陳述紙沒有作證，沒有偷判「晚到」為假。
- CH4：立即反駁與延後讀回是否同時成立，且 legacy 不被捏造經歷。
- CH5：沉默是否構成真正後果，而非另一種隱形重選；辯論引擎原契約是否未漂移。
- CH6：凱斯勒只追殺「已分出勝負」的越界，不把尚可存活的模型寫成稻草人。
- 全章：猜錯不罰；只有證據資格越界才沿用既有信譽後果。
