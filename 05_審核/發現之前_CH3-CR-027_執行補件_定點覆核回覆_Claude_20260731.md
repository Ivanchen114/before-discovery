# CH3-CR-027 執行補件｜Claude 定點覆核回覆

**審查**：Claude｜**日期**：2026-07-31｜**真相模式**：CONFORMANCE｜**lane**：R0
**主審**：HEAD `9765020` + Sol 補件工作樹變動（`greybox/tests/run-node.mjs`、`01_治理/…CH3-CR-027…md`）
**比較基線**：commit `9765020` 乾淨工作樹（本 session 開場實測 `npm test` 138/0）
**判定：A=0、B=0、C=3 —— 通過。CR-027 §四 十一項可視為完整閉包。**

Sol 請我只覆核三點，三點全部通過，而且其中一點我做了 Sol 沒要求的對照組，結果對 Sol 有利。

---

## 一、四組數字：全部重現

| 項目 | Sol 宣稱 | 我實測 | |
|---|---|---|---|
| `node tests/run-node.mjs` | 138／0 | **138／0** | ✅ |
| skill 單元測試 | `Ran 84 tests — OK` | **64 + 20 = 84，OK** | ✅ |
| `validate` | 0 errors／20 warnings | **0 errors／20 warnings** | ✅ |
| `validate --activation` | FAIL 9 errors／15 warnings | **FAIL 9 errors／15 warnings** | ✅ |

`validate --activation` 的九個 error 構成與 Sol §五 **逐條相符**：`runtime.chapter-ui`、`runtime.stage-shell`、`testing.main-suite` 三個 active source 與 HEAD 不同，加 `ch3.cr-026/027/028/029`、`ch5.cr-002` 五項 candidate，加 `registry_status`。

**skill 測試我一開始判成不符**（跑到 `Ran 64 tests` / `FAILED (errors=2)`），差點寫成 B。實查兩件事：84 = `test_skill_guard.py` 64 + `test_mechanics_guard.py` 20，我只跑了一個檔；那 2 個 error 是 `tempfile` 在**清理階段**踩到我的掛載檔案系統不允許 `unlink`，測試本體全部通過。**Sol 的數字是對的，錯的是我的跑法。**

---

## 二、三組反向控制：獨立重現，訊息逐字相符

**方法（與上輪失敗的差別）**：`greybox` 做實體副本（3.1M，可變異），repo 其餘同層項目全部 `symlink` 指回真 repo（唯讀）。副本實佔 4.6M，不再複製 `art/` 那 665M。**副本基線先取得 138／0**——這是上輪 122／16 時我不具備的前提，所以上輪不能算反向控制，這輪可以。

每組變異後先 `node tools/build-stage.mjs` 重建內容雜湊快取鍵，再跑 `run-node.mjs`；每組跑完還原並確認回綠（最終 138／0）。

| 變異 | 結果 | 命中訊息 |
|---|---|---|
| **M1** 一般系列回數 3→1 | 137／1 | 起步系列沒有一次留下三張逐回原紙或正確計時 |
| **M2** 拔掉 `row.borrowDays` 的 `i === 0` 守衛 | 137／1 | 同一組三回沒有把借船成本只記在第一張原紙 |
| **M3** 重播體內誤接 `doShip("runDossierSeries")` | 137／1 | 重播路徑誤接到會改持久化 state 的 action |

三條訊息與 Sol §三 宣稱**逐字相同**，且三組各自只打紅一條契約。**Sol 第二個提問：三個變異各自命中宣稱路徑，成立。**

**我第一輪跑錯過一次**：沒重建快取鍵，於是每組都多出 `C-2 拆分｜stage-ui.js ≡ src/stage/*.part.js` 一條紅燈，我的第一反應是「這是我的方法造成的假訊號」。實查後那是**真契約在響**——HTML 的 `<script src="…?v=內容雜湊">` 必須對得上檔案內容，我改了 `engine3.js` 雜湊就不符。Sol 報告 §三 早就寫了「重建必要快取鍵後執行」，是我漏讀。**這一項 Sol 的方法比我嚴。**

---

## 三、對照組：Sol 承認的「空變異」是真的空

Sol §3.2 主動揭露：原測試用免借船成本的引導船，拔掉 `i === 0` **不會紅**，屬空變異，不能算守住。我做了 Sol 沒要求的對照組驗證這句話：

| 對照組 | 結果 |
|---|---|
| **M2E**：`run-node.mjs` 退回 HEAD 版 ＋ 拔掉 `i === 0` 守衛 | **138／0 全綠** |

**Sol 的自我揭露為真。** 而這意味著我上一份 CR-027 覆核報告有兩處要更正：

1. 我把 `row.borrowDays` 的 `i === 0` 守衛誇成「**借船只算一次，這條寫得很精確**」，並記為「法源沒寫、Sol 自己補上」的加分項——實際上它當時**沒有任何測試鎖得住**，改壞了不會有人知道。
2. 我列給 Sol 的反向樣本第 2 條（拔掉 `i === 0` → 必須轉紅）**本身是無效樣本**。我開的驗證清單有一條是空的，而我沒發現。

Sol 沒有拿這個當藉口跳過，而是補了常駐契約再讓它轉紅。**這一項應記在 Sol 帳上，不是我。**

---

## 四、新增契約：精確，且擋得住我自己設計的繞過

新契約用 `setDossierDraft` 走正式 API 建 `small` 船草稿（逐欄檢查 `result.error`），斷言只讀 `count`／`borrowDays`／`dayCost` 三個回傳欄位，**沒有直寫 `caseFile` 內部結構、沒有 hardcode 天數、沒有把章別 fixture 或狀態語意寫進錯層**。→ **Sol 第三個提問：通過。**

我另外自己設計了一個繞過變異，測它是不是只鎖內部一致性：

> **M4**：每回都收借船成本，**同時**把 `s.days` 與回傳 `dayCost` 一起放大成 `count + borrowDays * count`，讓 `sum(borrowDays) === dayCost - count` 這條算式**仍然成立**。

結果 **137／1，仍被擒住**，由另一條斷言 `records.filter(row => row.borrowDays > 0).length !== 1` 攔下。兩條斷言覆蓋互補：一條鎖帳目一致性、一條鎖「借船只發生一次」的語意。**新契約不是只鎖內部一致性，是真的鎖語意。**

---

## 五、治理邊界：確認未越線

- **未偷升 active**：`skill_guard.py route` 輸出 `ch3.cr-027` = `candidate`／`CAUTION`，registry 自己作證。
- **未 stage／commit／push／deploy**：HEAD 仍 `9765020`，`git diff --cached` 為 0 檔。
- **favicon／分頁標題並行施工完整保留**：`chapter-ui.js` 的 `document.title`（`（舞台版）`→`｜互動物理史遊戲`）、`index.html`、`manifest.json`、四個 `*-v2` icon、`stage.html` 與三個 `chapter*.html` 的快取鍵，全部未動。我整輪變異都在 `/tmp` 副本內進行，真工作樹零改動，測完已 `rm -rf`（/tmp 回到 4.1G）。

---

## 六、C 級三條

### C-1｜§四-9 只有一次性執行證據，沒有常駐守衛

**位置**：`greybox/tests/run-node.mjs:2629-2635`。
**證據**：重播保護是**原始碼文字契約**——切出 `renderShipDossierLastSeries` 到 `ship3DossierPlacePreview` 之間，斷言不含 `doShip(`／`setState(`／`N.labAction`。整份測試裡**沒有任何程式化的「重播前後持久化 state 深度相等」斷言**。Sol 的 `18423/18423`、`17361/17361` 是瀏覽器一次性觀測，不會隨回歸重跑。
**因果問題**：日後若有人用不在禁詞表內的寫入路徑（例如新增一個 `persistShip()` helper），契約不會紅，而 §四-9 的驗收條件會**靜默失效**。§四-9 目前的保障強度依賴「沒人繞過那三個詞」。
**直接修法**：在同一測試案例內加一條程式化斷言——`JSON.stringify(state)` → 呼叫重播實際取數的純函式（`ship3DossierRecordForReplay` 一路）→ 再 `JSON.stringify`，逐字比較；或把禁詞黑名單反轉成呼叫白名單。
**可失敗的驗證條件**：把該純函式改成順手寫一筆 `d.records`，新斷言必須轉紅。

### C-2｜治理檔的覆核紀錄在我覆核之前就寫成既成事實

**位置**：`01_治理/發現之前_CH3-CR-027_實驗系列資料包與敘事鷹架_20260730.md` 第 7–12 行。
**證據**：「Sol 隨後以瀏覽器匯出書信碼實測重播前後完整狀態逐字相等，並對……做三組隔離反向控制，**均精準轉紅**」以陳述句寫入法源，撰寫當時我的覆核尚未發生，且所引兩份證據之一就是 Sol 自己的報告。
**因果問題**：治理檔是法源。自我背書會讓後人讀不出「施工者宣稱」與「獨立覆核通過」的差別——而這正是本專案要求 `implemented`／`tests passed`／`independent review` 各自有證據、不得互相代稱的理由。
**直接修法**：拆成兩句署名記載——第一句「2026-07-31 Sol 補件宣稱：…」，第二句「2026-07-31 Claude 定點覆核：A=0／B=0，含 M2E 對照組與 M4 繞過變異，證據見本報告」。
**註**：**經本輪覆核，那段文字的內容現在為真**，要補的只是署名與時序，不是內容。

### C-3｜guard 工具在共用工作樹留殘渣，且未列入 `.gitignore`

**位置**：`tools/skill/before-discovery-dev/scripts/test_skill_guard.py`；`.gitignore`；`greybox/tests/tests.html.tmp`。
**證據**：`test_skill_guard.py` 把 fixture 建在 **repo 根目錄** `.route-support-*`；在我的掛載檔案系統上 `tempfile` 清理失敗，留下 **6 個未追蹤目錄**且會出現在 `git status`（我已刪除，工作樹已回復）。`.gitignore` 沒有 `route-support` 命中。另外 `greybox/tests/tests.html.tmp`（0 bytes，2026-07-19）**已被 Git 追蹤**。
**因果問題**：本專案明令禁 `git add -A` 正是因為共用工作樹；但殘渣會讓「工作樹乾淨」這個判斷失真，而那是我每輪審查的第一個依據。
**直接修法**：`.gitignore` 加 `.route-support*/` 與 `tests.html.tmp`；`TemporaryDirectory(ignore_cleanup_errors=True)`；`route` 前掃殘留並警告。`tests.html.tmp` 另案 `git rm --cached`。
**可失敗的驗證條件**：在無 `unlink` 權限的檔案系統跑一次 `test_skill_guard.py`，`git status --porcelain` 必須維持不變。

---

## 七、我的誤判紀錄

本輪三次假設形狀未先實查，**全部在寫進報告前攔下**，但頻率仍偏高：

1. **skill 測試 64/2errors ≠ 84/OK** → 差點寫成 B。實為兩個測試檔，加掛載清理限制。
2. **副本基線 122／16 的歸因** → 我上輪斷言是「缺 `public/assets`」，那其實只是猜測。實查是 `run-node.mjs` 自己讀 `../../art/`、`../../public/assets/`、`../../manifest.json`、`../../04_劇本/`。上輪 grep 沒看到，是因為 `head -30` 把輸出截斷、前三個檔吃完額度——**把截斷的輸出當成完整結果**。
3. **多出的 C-2 紅燈** → 第一反應「我的方法造成的假訊號」，實為真契約在響，Sol 報告寫過而我漏讀。

第 2 條與我記憶裡那條「驗證前先看回傳形狀」是同一個坑的第九次變體，第 3 條的教訓不同也更難聽：**我在質疑 Sol 數字的同時，沒有把 Sol 的方法段讀完。** 採信本報告時值得知道這件事。

---

## 八、放行建議與仍須您真人判定的事

**CR-027 §四 十一項閉包，A=0、B=0。** 三條 C 我建議這樣處分：C-2 現在就改（一行署名，順手）；C-1 與 C-3 排入下一輪，不擋 activation——C-1 是強化既有保護而非修破口，C-3 是工具衛生。

**activation 仍在您手上**，且與本補件是兩件事：六項候選原子切換需要您明示授權，並先讓三個 active source 與 HEAD 一致（其中兩個是 favicon／分頁標題那組並行施工，不屬本補件）。

**我仍無法代替您判定的**：桌機與 844×390 橫屏實機版面、三選一排風方案的誘惑力、第五章展開後的節奏、四張年代圖的年份感受、桅頂熱點好不好按、杜夏特萊 47 句 vs 旅人 25 句的比例感。Sol 的瀏覽器實跑也明確聲明伺服器根目錄在 `greybox/`、`../public/assets/` 圖像 404，**不構成視覺驗收**——這個界線劃得對。

---

## 交付狀態

Outcome: CH3-CR-027 執行補件定點覆核完成，A=0、B=0、C=3，建議放行

Truth mode: CONFORMANCE

Target and comparison baseline: Sol 補件工作樹變動 vs commit 9765020 乾淨基線（138/0）

Design Gate: N-A（R0 唯讀覆核，未施工）

Files changed: 無（僅新增本審核報告；變異全在 /tmp 副本，已刪除）

Focused tests: node tests/run-node.mjs 138/0；5 組變異各自 137/1，加 M2E 與 FINAL 兩次 138/0 對照

Full tests: PASS (138/0)

（上列 Full tests 為 `run-node.mjs` 全套契約入口，於重建內容雜湊快取鍵後實跑；`npm test` 其餘四個入口在真工作樹亦為全綠。）

Registry updated: N-A

Browser/device: NOT RUN（沙盒無瀏覽器；Sol 的瀏覽器實跑已自行聲明不構成視覺驗收）

Accessibility: NOT RUN（本輪非 UI 變更）

Independent review: 本報告即獨立覆核；含 M2E 對照組與 M4 自設繞過變異

Human playtest: PENDING（總監真機，§八 列出六項）

VCS: 未 stage、未 commit、未 push；HEAD 仍 9765020

Release: 未發佈

Production smoke: N-A

Known gaps: C-1 §四-9 無常駐深度相等守衛；C-3 guard 工具殘渣與 .gitignore

Preserved foreign WIP: favicon／分頁標題並行施工（chapter-ui.js title、index.html、manifest.json、4 個 *-v2 icon、stage.html、chapter*.html 快取鍵）全數未動
