# CH3-CR-028 碼頭辯論節奏與存檔回饋|Claude 對抗審回覆

**審查**:Claude|**日期**:2026-07-31|**真相模式**:CONFORMANCE|**lane**:R0
**主審**:`1dbcec8` + `6cb02d6`(乾淨 clone 檢出,`git status` 空)
**併補**:CR-027 與 v3.4.5 從未經我審查,本報告一併涵蓋(見 §五)
**判定:A=0、B=1、C=3。**

---

## 一、數字復現與一處對不上

| 宣稱 | 實測 |
|---|---|
| runtime 137/0 | ✅ 乾淨 clone **137 通過、0 失敗** |
| skill guard 0 errors | ✅ PASS |
| skill guard 18 warnings | ⚠ 我量到 **16**(見 C-3) |
| activation 仍 candidate | ⚠ **4 errors** 不是 2:`ch3.cr-026`、`ch3.cr-027`、`ch3.cr-028` 三個 CR + registry(見 C-1) |
| 八步黃金路徑 | ✅ 測試序列逐行對:p1(source,cabin)→p2(source,boundary,scope)→p3(source,align,transform) |
| v3.4.5 的 B-1 修法 | ✅ **59 條 active 法源,worktree≠HEAD 漂移 = 0**(我上輪抓的 11 條全平) |
| C-2 預印答案 | ✅ `"○ " + ship3DossierAssertionText` 已從候選卡移除 |

---

## 二、A 級(0 條)

**A-1 三個物理認知**:p2、p3 明確由玩家完成——`same-pattern-not-proof`(過度宣稱/過度丟棄/正確保留,兩邊都是坑)、`align`+`transform` 是真操作。p1 見 B-1。
**A-2 排除風**:`cabin` 步要求 `A2`,錯選會被「這張是在甲板上畫的」擋回,功能保留 ✅
**A-3 舊紙船況不明**:`boundary` 守住,錯選一律回「這一格的數字在哪裡」✅
**A-4 先對齊再轉換**:`align`(same-beats)→`transform`(subtract-each-beat) 順序強制 ✅
**A-5 舊存檔續玩**:實跑舊路徑 `p1/wind`、`p1/concept` **仍可呼叫且能完成 p1 並推進到 p2** ✅

**移除的五項我逐一比對過**,四項正是我在 DEB-CR-026 點名的稻草人(石頭自己追桅杆、只有風推、先選同一時刻、起點不同)。**用刪除代替改寫,是我 CR §四備選方案的合法路徑**,ch3 這條線我認帳。

---

## 三、B 級(1 條)

### B-1|第一柱是三柱中唯一沒有玩家主張的一柱,而它扛的是本章核心概念

**位置**:`engine3.js` p1 `source`/`cabin` 分支;`chapter-ui.js` 新 UI 不再呼叫 `p1/wind`。

**可重現證據**:

```js
/* p1 source 成功後自動設定 */   db.p1.steady = true;  db.p1.concept = true;
/* p1 cabin 成功後自動設定 */    db.p1.concept = true; db.p1.steady = true; db.p1.wind = true;
                                 db.pillars.p1 = true;
```

玩家在第一柱只做兩次**選紙**;「共同前行」與「甲板風只是非必要條件」兩句都由 NPC 說出:

> 維達爾船長:「共同前行這句,我收。」
> 槳手:「**你只說到紙能說的地方**。這一問過了。」

那句「你只說到紙能說的地方」正是**限定主張**——它應該是玩家講的。

**因果問題**:依報告 §二-2 自訂的四種認知支付(選證據/做新推論/限定主張/親手轉換),逐柱盤點:

| 柱 | 選證據 | 推論 | 限定主張 | 轉換 |
|---|---|---|---|---|
| 第一柱 | ✅×2 | — | — | — |
| 第二柱 | ✅ | ✅ boundary | ✅ scope | — |
| 第三柱 | ✅ | — | — | ✅×2 |

**第一柱四格全空只剩選證據**,而它承載的「石頭鬆手前已和船一起走」是整章、甚至整個系列往慣性走的那顆種子。玩家把最重要的一句交給 NPC 講,和 CR-028 想解決的「玩家不清楚自己在做什麼」是同一個病灶的另一面:上一版是**問太多**,這一版在第一柱**問太少**。

**修法很小,而且材料現成**:`p1/wind` 步在 engine 完整保留且**正是本專案 §3.7 認可的可否證形式**——三個選項都在講風,只差斷言強度:

| | 選項 |
|---|---|
| 正 | 甲板那一趟的風影響多少,我還不能說;但把甲板風隔開後,石頭仍不一定落後 |
| 誤 | 甲板風在這類落石裡不會改變結果(過度否定) |
| 誤 | 這兩趟足以排除風的影響(過度宣稱) |

沒有稻草人,兩個錯項都必須讀 A2 的「船艙隔開的是甲板風」才能否掉。**在 `cabin` 之後把它接回來,八步變九步,第一柱補上「限定主張」。**多一次點擊,換回本章核心概念的作者權。

**會失敗的驗證條件**:新增契約——每一柱至少要有一個非「選證據」的必按動作;今天 p1 會紅。反向:拔掉 `p1/wind` 的接回,該契約必須再紅。

---

## 四、C 級(3 條)

**C-1|activation 是 4 errors 不是 2**:`ch3.cr-026`、`ch3.cr-027` 兩個 CR 也還是 candidate。報告 §六只提 cr-028,會讓人以為只差一張票。**cr-027 我從未審過**(其審查包至今仍是 untracked `??`),cr-026 則等我上輪放行後未翻。翻 active 時三個 CR 必須同一原子切換,漏一個 activation 就不會綠。

**C-2|CR-027 未經獨立審查就被 CR-028 疊在上面**:`1dbcec8` 同時含 CR-027 與 CR-028 的產物,兩者共用同一批檔。本報告的 A/B 判定涵蓋**現行 HEAD 行為**,但我沒有針對 CR-027 的原始交付主張(重播不加天數/證據、筆記證據圖、斷言成立對話)逐條做 CONFORMANCE。若您要 cr-027 也翻 active,請說一聲,我補一份定點;或接受「以 CR-028 現況審查涵蓋之」並在 cr-027 檔頭註記。

**C-3|warnings 16 vs 報告 18**:數字不符,可能是我與 Sol 跑的時點差兩個 commit。不影響判定,但既然 warnings 被當成「已知非阻斷」清單在交接,數量對不上就該對齊一次。

---

## 五、併補:v3.4.5 的 B-1/C-2 驗收

我上輪的兩條都真修:

- **B-1(active 法源 HEAD 綁定)**:掃 59 條 active 來源,worktree≠HEAD **= 0**;我上輪列的 11 條(scenes/scenes2/scenes4/run-node/README/decisions…)全部平掉。
- **C-2(ch3 提交前印答案)**:候選卡的 `○ 完整斷言` 已改中性問題,「提示不會預先標出答案」不再自打臉——**您明天試玩不會再撞到這一條**。
- **保留節點 ID**:ch4 未為此改動 id,schema 2 存檔不斷鏈 ✅

**B-3(ch1 回標題)**:`chapter-ui.js:7148-7153` + `stage.html:2903` 三段提示齊備,首頁依 `currentSave` 顯示「有未完成進度」。診斷正確——原本就有存,是回饋失真。真機續入原局仍需您確認。

**B-4(ch4/ch5 不施工)**:同意。ch4 用「軌道與出版備忘」、ch5 用「兩本帳辯論備忘」,`stage-ui.js:908/929` 逐章分流,沒有硬套三柱。ch5 的 `fr.claim` 我前輪查過是四選一的範圍階梯(含量綱幾乎成立的 `same-thing`),不是 bridge。**沒有為了看起來一致而亂改,是對的。**

---

## 六、我的誤判紀錄

實跑新八步時我一度得到 `p1/cabin` 失敗(`dossier-evidence-missing`),差點寫成 A 級。查證後是**我的探針無效**:我直接把 `assertions.A2` 設成 true,但該旗標由底層 `blind.records` 重算,偽造值在下一步被正確重置。官方測試走真實遊玩路徑是綠的。

**這是本輪第二次靠「先查探針再下結論」擋下假發現**(另一次是遷移的 `bad-v2-seed`)。規則已寫進我的記憶:偽造中間狀態前,先確認該欄位不是衍生值。

---

## 七、回覆格式

```text
判定:A=0、B=1、C=3

B-1:
- 位置:engine3.js p1 source/cabin 自動設定 concept/steady/wind;UI 不再呼叫 p1/wind
- 證據:第一柱四種認知支付僅有「選證據」;限定主張由槳手代講
- 最小修法:cabin 之後接回既有 p1/wind 步(八步→九步)
- 修後驗證:每柱至少一個非選證據動作的契約;拔掉接回必須轉紅

B=0 之後:
- ch3.cr-026 + cr-027 + cr-028 + registry 四者同一原子切換:同意(見 C-1)
- 同步正式生效 skill:同意;Claude 生效版由我 save_skill 並 read-back
- 仍須總監真人試玩:桌機/844×390 橫屏、第一章回標題續入原局、
  九步後的辯論節奏體感、證據卡分層是否夠選、旅人筆記證據圖可讀性
```

repo 零修改(本報告除外);探針全在 `/tmp/c2`(HEAD 的獨立 clone),即棄。
