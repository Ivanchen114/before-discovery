# CH3／CH4 整合狀態＋第四章四處台詞｜交 Claude 唯讀對抗審

**發出**：Sol／Codex｜**日期**：2026-08-01｜**基線**：主 repo HEAD `01ebbeb`＋共享工作樹
**審查模式**：唯讀；不得修檔、stage、commit、push 或發布

這份文件取代分散在兩條對話裡的口頭交接。先講現況：第三章已知兩個 B、第四章
文字護欄的一個 C 都已修；本輪只請審第四章四處台詞施工與其護欄，不重審已閉包的
第三章接縫，也不要把共享工作樹其他美術、favicon 或章節施工算進本包。

## 一、目前判定

| 範圍 | 工程判定 | 仍未驗 |
|---|---|---|
| CH3 斷言離台／雙視角 | A=0、B=0 | 接縫中途讀檔 C-1；真機連點、Enter、重播定位、844×390、讀屏與降低動態 |
| CH4 Opus 四項修復 | A=0、B=0 | 選項是否仍能靠語氣猜答；D2-1 揭露新鮮感 |
| CH4 本輪四處台詞 | Sol 複審 A=0、B=0、C=2；兩條 C 已修 | 修後兩句由 Sol 動過，仍須 Claude 獨立唯讀審；另欠真機閱讀節奏與讀屏重新作答 |

自動綠燈不是發布判定。以上皆未做完整真人通關，未 stage、未 commit、未 push、未部署。

## 二、本輪已完成的施工與護欄

### 2.1 四處台詞

1. `D0-2/w1、w2`、`D1-1/w1、w3`、`DE-1/w1、w2`：刪除角色口中的
   「重選／重收」，保留角色對錯誤本身的具體反應。
2. `D-INT-1/n2、n3`：把 1667／1669 年表壓成一句；`n3` 改為
   `line/stage`，加入旅人伸手穿過時間頁縫卻碰不到書桌的動作。Sol 再把
   「接下盧卡斯講座」收斂為「接任盧卡斯數學教授」。
3. `D0-2/n14`：「抬手止住」改成「把蘋果換到另一隻手」，使搶答後收回與一開始
   就守住證據邊界的兩條分支都不被錯誤訓斥；n10→n14→n16 的蘋果動作仍連續。
4. `D4-2/n2`：無前文的「兩個窗口」改寫；Sol 複審再把仍偏現代術語的
   「兩次上機」改成「兩個能開印的空檔」。

### 2.2 新增三層契約

- `ch4MetaTerms` 已從只掃 D2-1／D2-2 改為掃描第四章所有節點與選項；把
  「正解卡」放在 D0-2 選項，必須轉紅。
- 所有非 `stage/system` 角色台詞不得含「重選、重收、重來、請選擇、點選」。
- `D-INT-1/n3` 必須以 `line/stage` 顯示，且同版存檔 serialize→deserialize 後
  仍能從 n3 前進到 n4。

三組獨立副本反向控制均為 **137 通過／1 失敗**，訊息分別精準命中：

- `第四章仍有場次把製作端的答案分類洩漏給玩家`
- `第四章角色台詞混入『重選／重收』等介面動詞`
- `D-INT-1/n3 沒有走可續讀的舞台旁白渲染路徑`

還原後完整 `npm test` 為 **138／0**；第四章遷移 **19 組／205 個舊游標**通過；
`git diff --check` 通過。

## 三、Sol 已做的獨立檢查

### 3.1 `D-INT-1/n3` 型別與存檔

- 故事游標只存 `scene + node`，不存節點型別；ID `D-INT-1/n3` 未變。
- narrative 的 `view` 同時接受 `line` 與 `system`；`speaker:stage` 會走無肖像旁白。
- `advance` 只對 `system` 套 effects，而 n3 原本與現在皆無 effects，沒有遺失狀態。
- 205 個 schema 1 游標依場次遷移，不靠 n3 原型別；完整 migration 已重跑通過。
- `narrativeExemptions` 只列 D2-2 與 D4-2，與 D-INT-1 無交集。

### 3.2 分支相容

- `future-answer → n12a → n12b → n13 → n14`：新 n14 讀作牛頓接受玩家收回。
- `evidence-boundary → n12d → n14`：新 n14 讀作牛頓同意玩家的證據分寸。
- 兩條路都不再出現「答對者被制止」；沒有新增節點或改變信譽效果。

### 3.3 序列化器是否誤改 v0.9

以外部 Opus 送審包的 `scenes4.json`（SHA-256
`130f5441569435222d0ee1bfe32e9cdb461fa4d99b83191b69d89b3668e1fd5f`）為施工前錨點，
逐欄解析比較現行 JSON：共 **22 個語意欄位差異**。它們完整分成：

- Opus 修復：D0-2 兩個回答、兩張「參考卡」、DE-1 三個選項、SC4-R1 三個選項；
- 本輪台詞：n14 一句、六句刪 UI 動詞、D-INT-1 四個欄位、D4-2 一句。

除此之外零語意差異；scene／node／option ID、連線與數量均未漂移。這證明大規模格式
往返沒有偷改 v0.9 內容，但請 Claude 仍獨立重現，不要採信本段結論。

## 四、請 Claude 實攻五點

### A. n14 兩分支與動作線

逐拍讀兩條分支，確認「把蘋果換到另一隻手」在錯答收回與正解直達時都自然，並檢查
n10、n14、n16 的蘋果位置是否矛盾。若判問題，須指出是哪一條路、前後各哪一句造成。

### B. D-INT-1 型別變更

獨立驗 `system → line/stage` 的 view、advance、transcript、同版存檔往返、schema 1→2
遷移、舞台無肖像呈現、讀屏全文與降低動態。不得只用「205 cursor 綠」代替渲染判定。

### C. 刪除「重選／重收」後的重新作答

在 D0-2、D1-1、DE-1 各選錯一次，確認角色回應完會回到原 choice；滑鼠、Enter、Tab
與讀屏使用者都能知道仍需再選。另攻新 regex 是否誤殺角色合理使用「重來」的劇情語句，
以及是否有其他 UI 指令詞漏列。

### D. 序列化與資料邊界

用上述外部送審檔重做語意 diff。若不是 22 個差異，列出多出或缺少的 path。確認
`scenes4.js` 是由 JSON 生成，五入口 cache key 已更新，沒有把格式重排誤報成內容修改。

### E. 真正的台詞閱讀

不要量平均字數；逐句看問題、回答、動作與下一個玩家操作。特別攻：

- D-INT-1 的時間蒙太奇是否終於有場面，而非仍像年表；
- 「接任盧卡斯數學教授」與「兩個能開印的空檔」是否第一次就懂；
- D2-1 牛頓連說五句是否過悶；
- DE-1/ok1 的 92 字歸檔說明是否在情感高點變成說明書。

後兩項不在本輪四處施工內；若仍有問題判 C，除非能證明會阻斷理解或操作。

## 五、最低反向控制

請至少獨立做三組，使用副本，不污染共享工作樹：

1. D0-2 任一 option 加「正解卡」→ 137／1，命中全章製作術語訊息。
2. D0-2/w1 加回「重選。」→ 137／1，命中角色 UI 動詞訊息。
3. D-INT-1/n3 改回 `system/system` → 137／1，命中舞台旁白路徑訊息。

每組先更新副本的 JSON／JS 鏡像與 HTML cache key，避免用鏡像漂移或 cache mismatch
冒充目標紅燈；須回報失敗訊息與乾淨對照組。

## 六、回覆格式與放行門檻

請回覆 A／B／C；每條附檔案位置、重現路徑、玩家後果、最小修法與可失敗的通過條件。
最後分開標示：自動測試、瀏覽器、無障礙、真人試玩、VCS 與發布狀態。

本台詞包放行門檻為 **A=0、B=0**。瀏覽器或真人節奏做不到時明列「未驗」，不可用
138／0 代替。
