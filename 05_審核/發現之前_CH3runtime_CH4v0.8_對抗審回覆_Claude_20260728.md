# CH3 runtime + CH4 v0.8 對抗審回覆(Claude → Sol)

**審查**:Claude|**日期**:2026-07-28|**對象**:Sol 交接報告 + 檔案包(23 檔)
**方法**:不信 129/0;依報告第七節七面逐一實攻。所有攻擊腳本在 `/tmp/audit/` 執行,**未修改 repo 任何檔案**。
**判定**:**A=0、B=2(+1 條歸屬待裁)、C=2** —— **依「A=0、B=0 才放行」,本輪不放行;但兩條 B 都是小修,預估一輪內可清**。

---

## 〇、審查前提確認

- 檔案包 vs 工作樹:10 個核心檔 md5 **全部一致**——審的就是現行 runtime。
- `npm test` 自跑:**129 通過/0 失敗**;`run-ch4-migration.mjs`:19 groups/205 cursors ✓。
- **測試數 134→129 的五條去向**:我上輪的契約(工作台對話框化、E-2b osFired、ch3 場數、接縫逐字、講者白名單)逐一 grep **全部存在**——合併屬實,不是刪除。此項通過。

---

## 一、B 級(2 條 + 1 條歸屬待裁)

### B-1|1665 年的牛頓引用了 1673 年才出版的數字

- **位置**:`data/scenes4.json` D1-2(1665 同尺紙)節點 `q2`——牛頓:「這本寫 60⅖;托勒密寫 59;**惠更斯寫 60**。」
- **證據**:惠更斯的月距/重力比較工作發表於《Horologium Oscillatorium》(**1673**)。托勒密 59 ✓(古代)、Streete 60⅖ ✓(《Astronomia Carolina》1661,1665 可引),但**惠更斯 60 在 1665 年還不存在**。這條台詞在 Sol 本輪修正的月距紅線(1687/1726 兩版並列——那部分修得對)正中央,史實頁對了、1665 台詞漏了。
- **修法**:`q2` 的「惠更斯寫 60」改為「**溫德林(Wendelin)寫 60**」——Wendelin 的月距工作在 1640s,且 Principia 1687 初版清單就列 Wendelin 60(Sol 自己的史實頁已載),1665 引用完全合法。或直接刪去第三個來源。
- **驗證條件**:D1-2 場內 grep「惠更斯」=0;histfacts4 的兩版清單**不動**(1726 三版列 Huygens 60 是對的)。

### B-2|A-5 的 3×3 多解沒有任何測試在鎖

- **位置**:`src/engine4.js` `ORBIT_SPEEDS`/`ORBIT_STRENGTHS`/`orbitRuleOutcome`;`tests/` 全目錄。
- **證據**:我抽出引擎內部函式做九宮格數值驗證——**現行行為完全正確**:
  ```
  slow×short   near-circle  r∈[1.00,1.01]     slow×medium  inner-band
  medium×medium near-circle r∈[0.99,1.01]     medium×short outer-band
  fast×long    near-circle  r∈[0.99,1.01]     fast×short   outer-band …
  ```
  對角三組=近正圓、錯配各有可讀後果,A-5 契約成立。**但** `near-circle` 在 `tests/run-node.mjs` 命中 0 次,migration 測試只用單一 fixture——**未來任何人動這五個常數,3×3 多解會靜默壞掉**,而它是 Sol 對抗審 A-5 的核心修正、也是「中間選項不是唯一正解」的唯一保證。
- **修法**:加一條契約測試:九種組合跑 `simulateOrbitRule`+`orbitRuleOutcome`,斷言對角=`near-circle` 且六種錯配≠`near-circle`。
- **驗證條件**:把 `short: 0.67` 改成 `0.8` → 測試必須轉紅;還原 → 綠。

### B-3(歸屬待裁)|ch4 工作台台詞未入立繪對話框橋

- **位置**:`src/chapter-ui.js` `DEBATE_SAY_ACTIONS`(僅含 ch3 七個辯論動作);orbit 動作與 17 句牛頓教練句不在其列。
- **證據**:總監的原需求是「實驗室**或工作台**中的對話也要用對話框顯示」。現況 ch3 辯論有立繪對話框、**ch4 全部工作台沒有**——試玩從 ch3 走到 ch4 第一時間可見的不一致。
- **歸屬**:公平說,這是 **WB-CR-025b 既定待辦**(我的施工基線列「可一併入橋」,但 Sol 的交接範圍未承諾此項)——**不是本輪回歸**,不屬「史實誤導/操作代做/存檔洗白」三不可降級。**是否本輪修、或降 C 列後續,請總監裁。**
- **修法**(若本輪做):ch4 的 orbit 回饋改走 `sayIntoDialogue` 等價路徑;台詞照 `講者：（動作）「台詞」` 格式,動態契約擴到 ch4 走查。

## 二、C 級(2 條)

- **C-1|對話框動態格式契約只掛在 ch3 走查**。ch4 台詞若未來入橋,現有契約不會自動涵蓋。隨 B-3 一起處理即可。
- **C-2|魚書呈現位置請 Sol 指明**。報告第四節說魚書講法已收斂,`scenes4.json` 僅 1 處命中、`histfacts4.json` 0 處——不是問題,只要求在放行報告註明玩家在哪一場讀到它,供總監試玩對照。

## 三、守住的(逐面實攻結果)

| 第七節攻擊面 | 結果 |
|---|---|
| 1|schema 2 存檔 mutation 洗白 | ✅ 守住。`evidence.k5=true` 直改被 sanitizer 重算;`placeProofLink(slot,"FORGED")` 拒;`addModelLoan` 偽 caseId 拒;`declineModelLoan` 後無系統補借條(**A-3 改造屬實**:拒借不生成) |
| 2|schema 1→2 的 205 游標 | ✅ 守住。未知場游標→可讀錯誤不靜默重開;**偽 K1(有旗標無操作紀錄)整包被 `bad-evidence` 拒**——比回收更嚴;K3 未過里程碑不可攜;K4/K5 硬編碼禁攜(「schema 1 無等價操作,禁止合成假蓋章」註解屬實);偽 `originalCursor` 不被沿用;遷移器強制要求新引擎 v2 種子(`bad-v2-seed`),不自造狀態 |
| 3|雙重刪除 | ✅ 兩半皆守。刪操作:空狀態 `assertK1` 拒(守衛含 `firstStepAt > sealedAt` 時序契約);刪旅人:histfacts4 無「旅人發現/證明/教」任何居功句 |
| 4|1665 殘留 1679 框架 | ✅ 乾淨。「持續偏離/扳彎/每一拍/兩支箭」在 D0-1~D1-2 零命中(唯一命中「拍」=拍掉草屑,誤報);A-1 戲劇支付完整(「這個 4.9,不是我量的」+「兩張紙分開放,別混」);K0→封四項→落拍的**守序鏈**(`k0-source-required`)存在 |
| 5|人物預知/心聲答案詞/旅人語 | ✅ 乾淨。哈雷/虎克/佛蘭斯蒂德/雷恩全部他者先提;心聲禁詞 0 命中;旅人(你) 半截句 0 |
| 6|史實 | ⚠ 除 **B-1** 外乾淨:虎克兩信日期完整、雷恩「值四十先令的書/兩個月」(賞金×0、三個月×0)、月距兩版並列、球殼 LXX 命題在、1680 沉默場忠實 |
| 7|844×390 | 靜態層:讓位規則×3 在、任務卡無 nowrap 殘留。**真機層我無法執行**,此面依賴 Sol 的實測紀錄(報告第六節)+總監終驗,不計入放行判定 |

## 四、我的誤報(誠實列出,共 6 個)

審查中我先後誤報 6 次,全部是**我的斷言方式錯**、不是缺陷:`assertK1` 回傳協定(`ok:false` 非 `error`)、遷移器需 v2 種子×2、`_CREDIT_EXPECT` 用英文鍵(`direction:"Hooke"` 就在)、`sealOrbitRule` 的 `k0-source-required` 是守序鏈、「拍掉草屑」。**這些誤報反過來證明守衛的層數比我的第一直覺多。**

## 五、放行條件

1. **B-1**:一行台詞替換(惠更斯→溫德林),加一條 lint(D1-2 禁「惠更斯」)。
2. **B-2**:一條九宮格契約測試,反向驗證(動常數轉紅)。
3. **B-3**:總監裁——本輪修,或明列 WB-CR-025b 降 C 後續做。
4. C-1/C-2 列後續清單即可。

以上清完、`npm test` 綠、B-2 反向驗證截圖或輸出附上,**即放行總監試玩**。依角色互換條款,修正由 Sol 執行,我做第二輪覆核(只驗上列驗證條件,不重開全面審)。
