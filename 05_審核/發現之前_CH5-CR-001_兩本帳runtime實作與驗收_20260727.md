# CH5-CR-001｜第五章《兩本帳》runtime 實作與驗收

- 日期：2026-07-27
- 規格基準：小說稿 v0.1.4／工作台與辯論規格 v0.1.1／debate5 資料稿 v0.1.1
- 程式落點：`greybox`
- 狀態：runtime 與正式美術完成；自動測試與瀏覽器互動驗收通過

## 一、實作範圍

1. 新增第五章場景、辯論、史實頁三組 canonical JSON 與 file-compatible JS 鏡像。
2. 新增 `engine5.js`：
   - 輪一鋼頭／油灰頭各三筆；
   - 輪二沿用同一批紀錄，零次新實驗；
   - 4／8 油灰追一筆，`mv²` 由 144 降至 48，短少 96（三分之二）；
   - 黏土三速讀值依序為 0.5／2.1／4.4。
3. 新增兩頁式工作台、斷言卡、checkbox 選紙、已固定標示、提示與存檔淨化。
4. 新增第五章辯論：
   - 柱序固定為 P2→P1→P3；
   - 第一柱必須先承認院士的動量帳正確；
   - FR 四選項以 `reason` 資料分流；
   - 錯項不過濾，可重複提交；
   - 三柱均使用 `statements + playerCorrect`，未使用 `useLegacy`。
5. 接入系列首頁、第五章路由、HUD、章末回顧、史實卡與下一章鉤子。

## 二、對抗驗收後修正

### 1. 工作台焦點保留

瀏覽器實走發現：checkbox 狀態會保留，但下拉與 checkbox 在 DOM 重繪後會把焦點掉回 `body`。

已改為用 `data-c5-focus` 明確回復焦點。實測：

- 下拉重繪後：焦點仍在 `SELECT[data-c5-focus="clayHeight"]`；
- checkbox 重繪後：焦點仍在 `INPUT[data-c5-focus="record-8"]`；
- 十筆勾選狀態均保留。

### 2. 辯論柱軌順序

辯論引擎已依 P2→P1→P3 運作，但柱軌曾固定顯示 P1→P2→P3，造成介面與實際順序不一致。

已讓 `pillarSummary` 直接依 `chapter.order` 生成，並加回歸測試。

### 3. 未查證史實不得進 runtime

史實卡曾以「待查證／未採用」標籤顯示達朗貝爾未核定的版次措辭；即使是否定式展示，仍違反「不得寫進 runtime 文案」。

已整列移除，並把「達朗貝爾」「用詞之爭」加入第五章 runtime lint。's Gravesande 只保留黏土落球的歷史主線，不寫裝置規格。

## 三、自動測試

在 `greybox` 執行：

```text
npm test
131 通過，0 失敗（共 131）
```

第五章新增契約涵蓋：

- fixture 逐格驗算與誤差序列；
- 輪二零實驗與同批紀錄守衛；
- 4／8 追一筆可達；
- 三速黏土才可取得 J3；
- P2→P1→P3 與 P2 承認對手；
- FR `reason` 分流、錯項可重複；
- 完章走查、存檔白名單、鏡像與舞台接線；
- 證據越權及未查證措辭 lint。

## 四、瀏覽器驗收

桌面流程已由第五章開場完整操作至章末封存：

- 錯誤立約有可讀診斷並可重選；
- 輪一一次一筆，共完成鋼頭三筆與油灰三筆；
- 輪二沒有新實驗按鈕，天數維持 6；
- 4／8 油灰追一筆顯示 144→48、短少 96；
- 黏土三速顯示 0.5／2.1／4.4；
- 三柱依 P2→P1→P3 顯示；
- 同一個 `single-ledger` 錯項連續提交兩次，均保留且說服力各扣 1；
- 正確 FR 後完整顯示院士收尾：「我記了三十年的帳。今天才知道，我記的是哪一本。」；
- 章末史實卡不再出現未核定的達朗貝爾措辭；
- 瀏覽器 console error／warning：0。

手機橫向 667×375：

- 頁面 `scrollWidth = clientWidth = 667`，無整頁水平溢出；
- 工作台左右邊界均在 viewport 內；
- 主要操作按鈕高度 44px；
- 表格沒有把舞台撐出螢幕。

本輪後續已把第五章正式美術納入同一份 CR；詳見第七節。

## 五、史實邊界

以下兩項仍待核原典，但不阻擋 runtime：

1. 's Gravesande 裝置的確切規格；
2. 達朗貝爾對「用詞之爭」的版次與逐字措辭。

兩者均未作為第五章 runtime 的史實主張。

## 六、裁決

第五章 runtime 與正式美術可交付。機制、證據邊界、敘事主張與視覺資產一致；自動測試與桌面／手機瀏覽器驗收均通過。

## 七、正式美術補件與驗收

### 1. 資產範圍

第五章 runtime 現在引用 17 張獨立正式圖：

- 3 張場景背景：日間書房、晚間辯論、夜間尾聲；
- 3 張開場轉場：1687 倫敦、五十三年翻頁、約 1740 西雷莊園；
- 2 張透明角色：34 歲杜夏特萊、58 歲杜佩院士；
- 6 張證據圖：S6、S7、J1、J2、J3、J4；
- 3 張工作台功能圖：碰撞器材、黏土深度器材、4／8 油灰追問。

章節封面沿用日間書房，不另製重複圖。主生成提示、版本裁決與來源記在
`art/source/production/ch05/PROMPTS_RUNTIME_ART_20260727.md`。

### 2. 證據圖對抗審

另一工作對話交付的 S6、S7、J1–J4 已逐張檢查：

- S6、S7、J3 可直接採用；
- J1 v01 的裝飾刻痕形成可數的錯誤閉合暗示；
- J2 v01 的裝飾刻痕形成可數的錯誤短少暗示；
- J4 v01 的偽手寫、點陣與填滿表格會讓玩家誤認圖上已有另一組資料。

因此 J1、J2、J4 改用 v02：圖面只提供帳格、方向、兩本帳與「待對平」的
視覺結構，精確數值一律由 runtime 疊字提供。v01 保留在來源目錄作失敗
紀錄，但 `assets.json` 不再引用。

### 3. 自動與瀏覽器驗收

- `npm test`：131 通過，0 失敗；
- 新增第五章美術契約測試，鎖定背景場景、人物左右位、三幕轉場、證據版本、
  alt／caption、來源檔與 runtime 檔存在性；
- 桌面瀏覽器實走：章節封面、三幕轉場、日間書房、兩名角色、S6、S7 均成功載入，
  console error／warning 為 0；
- 手機橫向 667×375：`scrollWidth = clientWidth = 667`，證據卡完整顯示，
  無整頁水平溢出。

## 八、2026-07-27 工作台圖與首審修正

### 1. UI 文案

- HUD 移除 `J1／J2／J3` 內部代碼，改顯示目前這一輪的玩家語言；
- HUD title 只解釋當前目標，不預告後續路線；
- 實驗簿標題只寫當前相位，不再把後續四段一次列成路線圖；
- 每一輪新增兩行目的卡：「這一輪要回答」＋「做法」；
- 黏土相位補回杜夏特萊的在場引導；
- 未取得的斷言卡只顯示取得條件；取得後才顯示主張內容與引用紀錄。

### 2. 工作台功能圖

- 動量／活力相位：完整碰撞軌、可換鋼頭與油灰頭、砝碼、釋放架與計時器；
- 追一筆相位：不等重油灰配置與空白帳頁，文字層提出「總是少一半？」；
- 黏土相位：低、中、高三段垂直掛點、單一金屬球、平整黏土盤與深度尺。

圖像不燒入 fixture 數字、結果或完整去向；精確設定與讀值仍由 runtime
HTML 提供。黏土初稿因三個掛點同高而拒用，修正版才納入資產表。

### 3. 本輪驗收

- 最終 `npm test`：132 通過、0 失敗；
- 桌機實走：碰撞圖 → 同紙重算 → 4／8 追問圖 → 黏土圖 → J4 v02，
  圖像與相位、HUD、目的卡同步；
- 輪二天數不增加；4／8 實際留下 144→48、短少 96；
- J4 v02 已以「新證據」儀式實際取得，頁面無偽手寫與可數假資料；
- 手機橫向 667×375：`scrollWidth = clientWidth = 667`；
- 瀏覽器 console error／warning：0。
