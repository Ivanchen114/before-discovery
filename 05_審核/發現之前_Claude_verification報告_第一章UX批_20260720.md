# Claude verification 報告|第一章試玩 UX 批(Sol commit 8751351)

**結論**:**verification pass**(程式與資料層 12 項檢核全過;一項 B 級文件同步由 Claude 當場補記;G7 實機仍待真人)。

## 一、A 級門檻(全過)

1. **凍結語義**:debate.json=純加法(各證詞 `insight` 教練欄,無答案詞,分析對手前提不下判決,憲章一合格);scenes.json 改動限 A3-D/A3-F 失敗迴路四節點,**GB-ADR-009 完整記錄**(背景/裁決/不變邊界/驗證),總監事前授權(互換條款批次範圍明列)。
2. **判定本體零改動**:engine.js 未觸;12% 門檻、fixture、216 矩陣、扣值原樣——訊息=view 層重述,數字與規格一致。
3. **狀態機/存檔**:schema 3 不變;新欄位(`debate.mistakes`、證詞 `pressed` 狀態)全部防衛式讀取——**舊存檔載入抽測 ✓、書信碼匯入閘相容 ✓**(實測無 mistakes 欄之 state 通過 loadSave+sanitizeImport);雙歸零依序(說服力→複盤、信譽→SC-R1)有常駐測試。
4. **sanitize 不被繞過**:btnImport 路徑原樣;chapter-ui 無新 innerHTML 串接(機械測試綠)。
5. **測試不弱化**:57/57;舊「中止→回實驗台補證」測試之刪除=依 GB-ADR-009 改寫為 debrief 分流測試(語義變更有據),新增:失敗提示連續數、追問 insight+重複拒絕(不扣分)、A3-F debrief 分流、雙歸零單次 SC-R1、辯論桌 DOM 契約、非學段模式文案。

## 二、發現與處置

- **B-1|上層文件失同步**(已由 Claude 當場補記,非 Sol 程式缺陷):runtime A3-F 已改,但劇本 v0.2.2 與章規格 v0.1.1 正文仍述舊迴路。處置:兩檔頭部各補 GB-ADR-009 變更註記(正文原文保留,差異以註記為紀錄,俟第一章 v2 併入)。單一真相恢復。
- **C-1**:`rememberMistake` 上限 6 筆(shift 淘汰)——複盤長辯論時最早錯誤會被擠掉;可接受,建議 v2 時在複盤文案註明「僅回顧最近幾次」。不阻擋。
- **C-2**:追問後 `pressed` 狀態不入 UI 存檔提示(重讀檔後 insight 仍在筆記紀錄,可查)——可接受。

## 三、Claude 側自動驗證(本機重跑)

- `npm test`:**57/57**;`node --check`×4 全過;jsdom 煙霧(21 項)全過;舊檔/匯入閘相容抽測 ✓(指令與輸出見 git log)。

## 四、尚待真人(不冒充)

- **G7 全清單**:`file://` 真瀏覽器實點(對話播完才出現前往鈕/兩道門檻顯示/三段無重複進場閘/問到底→洞見→錯示至歸零→複盤→已破支柱保留/trap 信譽歸零→SC-R1/雙歸零依序/1280×720+844×390 辯論版面)。Claude 瀏覽器工具被安全政策擋 `file://`,與 Sol 相同,如實留白。

**簽核**:Claude(對抗驗證),2026-07-20。角色互換批次結束,檔案鎖解除;分工彈回 ADR-001。
