# Claude verification 檢核表|第一章試玩 UX 批(Sol 主責實作)

**性質**:對抗驗證的事前公告——Sol 開工前即知驗什麼;交付後 Claude 逐項核,✗ 附可驗證依據(A/B/C 級)。
**依據**:角色互換條款(01_治理/…追加事項_20260720 §二之三)。

## 一、凍結與語義(A 級門檻)

1. 劇本 v0.2.2 既有台詞**逐字不動**;新增伽利略提示=教練層附加,節點須可辨識(標記/註記),留變更註記。
2. 教練台詞守憲章一:**問句不判決**;不得出現答案詞(×9、奇數、1:3:5:7、「陡+水鐘救不回」這類直接判定)。
3. 判定規則本體(engine judge/TOL/判定表)零改動;訊息改寫=view 層重述,數字與規格表一致。
4. 章規格 R-ID 語義不變;若訊息文案涉及 R-SAV/R-DEB 描述,規格不必改(view 措辭自由),但不得矛盾。

## 二、狀態機與存檔(A 級門檻)

5. `serialize/loadSave` 格式不變(schema 3);舊書信碼與舊 localStorage 存檔可無損載入。
6. 雙歸零流程(說服力歸零+信譽歸零)路徑:中止→離場→SC-R1→再入,已破支柱保留、量表重置——與 narrative-suite 既有黃金路徑一致;若流程有感受層改動,引擎狀態轉移不變。
7. sanitize 匯入閘不被繞過:btnImport 仍先經 `GB.Sanitize.sanitizeImport`;新 UI 不引入新的 innerHTML 串接(機械測試仍須綠)。

## 三、測試(A 級門檻)

8. `npm test` 全綠;**不得刪除或弱化既有斷言**(改寫允許,語義等價須說明);新行為附新測試。
9. jsdom 煙霧可跑(Claude 會重跑);DOM 契約:chapter-ui 引用 id 兩殼同備。

## 四、無障礙與輸入(B 級)

10. srLine 主線播報不退化;新面板 modal 有焦點圍欄與歸還;確認閘鍵盤焦點=本批修目標,Tab/Enter 全程可走。
11. reduced-motion 全數尊重;觸控 44px;點觸即顯說明不退化。

## 五、體感(C 級,實機共驗)

12. 追問/出示新 UI 的行動句可讀;失敗訊息兩道門檻亮牌;計時器性格入選單;伽利略三層觸發時機(探索自動/學者按鈕)。

**通過條件**:一~三全綠+四無 A 級=verification pass;體感項留總監實機。
