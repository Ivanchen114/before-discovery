# 《發現之前》M1／M2 瀏覽器煙霧測試

**審查者**：Sol  
**日期**：2026-07-19  
**原定受審基線**：M1 commit `7ed6bf7`  
**完成時最新版**：M2 commit `84a2ecd`

## 結論

**內部里程碑條件通過，不阻擋後續實作。** M1 操作檢查期間程式基線前進至 M2，因此存檔斷點與最終自動測試改以最新版重驗；結果為 **36 通過、0 失敗**，瀏覽器點擊流程、分支、回標題、自動存檔與續玩落點皆正常，主控台無 error／warning。

本紀錄**不是最終真人實機簽核**：Codex 內建瀏覽器基於安全政策拒絕 `file://`，且其鍵盤自動化驅動未能以 Enter／Space 觸發已聚焦的原生按鈕。這兩項留待一般 Chrome／Firefox 依 `greybox/docs/browser-checklist.md` 實測；不得把工具限制寫成產品已通過。

## 檢核紀錄

1. 確認 M1 commit `7ed6bf7` 存在，並於 M1 畫面完成：標題頁、探索模式、開始新遊戲、序幕連續節點、兩組選項分支與狀態列顯示。
2. 檢查版面：1280×720 視窗無水平溢出；標題、遊戲區、信譽、模式、場景、證據筆記及控制按鈕均可辨識。
3. M1 測試途中 HEAD 前進至 M2 `84a2ecd`；為避免把不同 schema 的舊存檔誤判為故障，於 M2 開新局後重跑存檔路徑。
4. M2 實測：開始新遊戲 → 推進一節 → 回標題 → 出現「繼續上次進度」→ 續玩後回到 `P0-1` 已推進節點，文字紀錄與信譽 `3/5` 保留。
5. 讀取瀏覽器 error／warning log：空陣列。靜態伺服器僅有不影響遊戲的 `favicon.ico` 404。
6. 於 `greybox/` 執行 `npm test`：**36 通過、0 失敗**；含 216 判定點、敘事圖可達性、禁詞 lint、M2 門檻、E4／E3 黃金路徑、SC-R1、模式守衛、存檔往返與三份 JS／JSON 鏡像。
7. 鍵盤檢查：Tab 可把焦點移到第一個選項按鈕；內建瀏覽器驅動的 Enter／Space 未觸發 click，故只記「焦點可達」，不簽「鍵盤可啟動」。

## 待最終真人簽核

| 項目 | 現況 | 最終門檻 |
|---|---|---|
| 雙擊 `chapter.html` 離線開啟 | 內建瀏覽器封鎖 `file://`，未驗 | Chrome＋Firefox 各一次 |
| 全章只用鍵盤通關 | Tab 焦點可達；Enter／Space 未由自動化驅動證成 | M3 全章完成後真人走一輪 |
| 存檔跨關閉／重開 | 同一瀏覽器工作階段已通過 | 關閉瀏覽器後重開再驗一次 |
| 正式美術整合後版面 | 尚未進入整合版 | 候選美術版做最終簽核 |

## 判定

- **A 級：0**
- **B 級：0（尚未簽核的項目是測試待辦，不是已重現缺陷）**
- **C 級：1**：可補 `favicon.ico`，但不影響玩法或交付。

