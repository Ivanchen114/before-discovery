# 《發現之前》M3 第二次 verification pass

**審查者**：Sol
**日期**：2026-07-19
**受審基線**：commit `c688f52`
**前次紀錄**：`05_審核/發現之前_M3外科修正驗證_Sol_20260719.md`

## 結論

**通過。** 前次唯一 A 級（探索模式 E3.c 按鈕被 UI 隱藏）與 B 級（劇本分支表仍標成學者限定）均已依最小範圍修正；探索模式可在真實瀏覽器完成 E3.c，並可沿指定高風險路徑完成雙歸零、SC-R1 修復、離場再入、誠實作答與第一章結局。

依 ADR-009，本次通過使下列狀態即時生效：

- 第一章劇本 **v0.2.2 凍結**；
- 第一章功能規格 **v0.1.1 凍結**；
- 第一章正式美術量產 **解凍**；
- 第二章功能規格、程式與美術仍依 ADR-010 維持凍結。

本輪分級：**A 0、B 0、C 0**。不開第三輪審稿。

## 1. 變更範圍與自動驗證

1. `c688f52` 只修改三個檔案：`greybox/src/chapter-ui.js`、劇本 v0.2.2、`greybox/tests/narrative-suite.js`；未夾帶 Sol 的美術、審核與任務書工作樹。
2. UI 按鈕可見性改由當前 embed 的 `until.e3` 資料決定，不再另以模式判斷；到 E3.c 節點時 C 顯示、B 隱藏。
3. 劇本附錄已改為 E3.c 雙模式必經，與正文、規格及 runtime 一致。
4. `npm test`：**46 通過、0 失敗**；新增契約測試鎖定 E3.c 節點不得帶模式限制。
5. `git show --check c688f52` 無格式錯誤。

## 2. 瀏覽器實機路徑

以 HTTP 伺服器載入 `greybox/chapter.html`，在探索模式實際點擊，不以直接呼叫引擎代替 UI 驗收。

1. A2-3 完成第二傾角主張後，畫面只顯示「斷言：隨傾角形式不變」，不顯示 B；點擊後狀態由 `E3:a●b●c○` 正確變成 `E3:a●b●c●`，劇情可繼續。
2. 完成紙張形狀與空氣／水介質兩組 E4 對照實驗，正常進入第三幕。
3. 以三次錯誤出示及 P2「憑未來的物理學」錯答，共扣四點說服力，使最後反撲前狀態為：說服力 1、信譽 1。
4. 三根支柱依序擊破；FR 兩步外推可由 E3.c 證據正常完成。
5. 在「垂直，你量過嗎？」選「量過」，說服力與信譽同時歸零；系統立即進 SC-R1，沒有卡死或跳過代價。
6. 執行一次新實驗後信譽回復至 1；回到已中止辯論，離場進 A3-F，再入時說服力重置為 5，P1／P2／P3 保持已破，FR 陷阱仍保留。
7. 再入後選「沒有」，正常完成 A3-6 判定、運河尾聲、月球鎚羽、兩題反思與 `——第一章 終——`；本次存檔顯示總耗 8 天。

全程未出現可見 UI 例外或死路；測試頁籤與本機伺服器已於驗證後關閉。

## 3. 前次門檻文字更正

前次紀錄 §4 寫成「四次錯誤出示使說服力 1 → P2 錯答使信譽 1」，按量表算術會多扣一點並提前中止。正確的自然 UI 路徑是：

- 三次錯誤出示：`5 → 4 → 3 → 2`；
- P2 錯答：說服力 `2 → 1`、信譽 `2 → 1`；
- lied：說服力 `1 → 0`、信譽 `1 → 0`。

此為審查紀錄的文字筆誤，不是受審程式缺陷；本次已按正確狀態鏈實機覆蓋。

## 4. 交接

第一章內容與功能基線自此凍結。正式美術依 ADR-001／009 由 Sol 按場景 ID 分批量產；Claude 後續負責資產載入、程式動畫與整合。任何內容或功能改動須走正式變更紀錄，不得在美術量產期間偷改凍結基線。
