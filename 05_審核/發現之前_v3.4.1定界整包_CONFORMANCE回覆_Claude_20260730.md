# v3.4.1 candidate・五章承重・CR-031・開工門檻|Claude 定界整包 CONFORMANCE 回覆

**審查**:Claude|**日期**:2026-07-30|**真相模式**:CONFORMANCE|**lane**:R0
**主審對象**:repo 內 `before-discovery-dev v3.4.1-candidate`(297 行,直接讀 repo,未依賴我的生效版)、五章去台詞承重報告、全專案盤點與第六章開工門檻、NARRATIVE-CR-031
**範圍外**:285 個髒檔的逐一審查(依任務書明示排除)
**判定**:**A=0、B=0、C=4 —— candidate 包放行,可進總監 activation 裁決。**六條 runtime 紅線全數確認仍在、未被綠燈掩蓋,依 Sol 的分批表另立 CR,不阻擋本包。

---

## 一、五組數字獨立復現

| 宣稱 | 我的實測 | 判定 |
|---|---|---|
| runtime 136/0 | **136 通過、0 失敗**(exit 0) | ✅ |
| skill 48/48 | repo 內跑 **FAILED(errors=2)**;/tmp 完整副本 **48/48 OK** | ✅(見 C-1) |
| mechanics guard 20/20 | **OK**(exit 0) | ✅ |
| validate 0 errors/46 warnings | **PASS: 0 errors, 46 warnings**(23 未追蹤+8 candidate+7 GAP+8 其他) | ✅ |
| activation 預期失敗 41/17 | **FAIL: 41 errors, 17 warnings**(exit 1,乾淨取碼,無管線) | ✅ |
| 敘事診斷 1/1/0/5/6 | **1/1/0/5/6**(逐章 `--scenes` 實跑) | ✅ |
| 旅人心聲 17+28 | **總 45 = active 17 + legacyOnly 28** | ✅ |

skill 測試在 repo 內的 2 個 ERROR 是**我沙盒的 teardown 權限問題**(掛載資料夾禁止 unlink 暫存檔),斷言本身全過;不是 Sol 的缺陷,但暴露了一個測試設計問題(C-1)。我的測試殘留(`.route-support-*` 兩個目錄)已清除,工作樹回到 285 路徑。

我上輪抓的**治理鏡像落後已收掉**:`validate` 的鏡像 error 為 0。

---

## 二、A 級(0 條)

---

## 三、B 級(0 條)

我試過的攻擊面全部被擋下,列出以示不是沒打:

| 攻擊 | 結果 |
|---|---|
| ch6 `write` 無 brief/provenance | ✅ exit 2,兩條 ROUTE BLOCKER 訊息精確 |
| ch6 `implement` 無 spec | ✅ exit 2 |
| brief 的 `bd_chapter` 填 ch5 冒充 ch6 | ✅ exit 2「bd_chapter 必須是 ch6」 |
| 同一檔兼任 brief+spec | ✅ exit 2「bd_artifact 必須是 implementation-spec」+「bd_status 必須是 frozen」 |
| 拿 `package.json` 冒充 brief | ✅ exit 2「缺少 YAML front matter」 |
| 合法 brief+provenance 但 lane 報 R2 | ✅ exit 2「unregistered-chapter requires R3 or higher」——**我自己填錯 lane 被正確攔下**,改 R3 後 exit 0 |
| mechanics contract 填 `ch99` | ✅ exit 2 |
| contract 用假 anchor `ZZ-9/n1` | ✅ exit 2 |
| contract 零 segments | ✅ MEC-01 拒絕 |
| 純 `historical-inquiry` 是否誤載工作台 | ✅ `design.workbench` 0 命中;明示 `--task workbench` 才載入(B-4 修正確認) |
| CR-031 負向控制:active 心聲的 `osPurpose` 改成 `vibes` | ✅ **134/2 紅**;還原後 136/0 |

**B-4 的三處狀態矛盾已同步**:工作台檔頭現為「含實驗工作台/證據支付/辯論語法時的**現行法源**」,registry `design.workbench status=active`,SKILL.md §6 同語——三處一致。

---

## 四、C 級(4 條,不阻擋 activation)

### C-1|`test_skill_guard.py` 把暫存檔寫進 repo 根,且未列 .gitignore

**位置**:`test_skill_guard.py` 的 route 支援檔測試(在 repo 根建 `.route-support-*` 暫存目錄)。
**證據**:我在 repo 內跑測試,teardown 因沙盒權限失敗,留下 `.route-support-4kayi81a/`、`.route-support-negative-gpyn3ac_/` 兩個未追蹤目錄——工作樹瞬間從 285 變 287。任何中斷的測試跑動(Ctrl-C、當機)在一般機器上也會留同樣的垃圾,混進本來就有 212 個 untracked 的工作樹,正是 A-5 基線工程最怕的污染源。
**因果**:router 要求支援檔在 worktree 內,所以測試不得不寫進 repo——這個設計是對的,缺的只是隔離。
**修法**:`.gitignore` 加 `.route-support-*`;或暫存目錄集中到已忽略的固定子目錄。
**會失敗的驗證**:中斷測試後 `git status --short | wc -l` 不變。

### C-2|我自己的 DEB-CR-026 成了懸空的第二份真相

**位置**:`01_治理/發現之前_DEB-CR-026_誘答項可否證性法則_20260730.md`(我寫的)vs `02_設計/發現之前_實驗證據所有權與認知進階原則_v0.1.md` §3.7(Sol 施工的)。
**證據**:全 repo 無任何檔案引用 DEB-CR-026;decisions.md 無此案。同一條規則現有兩份文本,而 §3.7 的版本**更好**——`refutedBy.relation` 四分類(`contradicts/confounded/out_of_scope/insufficient`)比我的單一欄位精確,且已有 mechanics guard 機檢與 20/20 反向 fixture。
**修法**:DEB-CR-026 檔頭加「已被 §3.7 收編,僅存提案歷史」一行。我的檔案,總監點頭我就補;或 Sol 在下輪順手處理。
**會失敗的驗證**:grep「誘答項可否證」只有一個 active 出處。

### C-3|ch1 `A3-D`/ch2 `B3-D` 的 NAR-08 是結構性誤報,會變成常駐紅噪音

**位置**:`/tmp/nar1.txt`、`/tmp/nar2.txt` 各 1 條「0 visible text beat(s)」。
**證據**:兩場的節點構成都是 `{embed:1, goto:2}`——辯論場,台詞全在 debate.json/辯論引擎裡,scenes 層本來就不該有文字拍。這與 ch5 的空殼場**病因完全不同**(ch5 是真的沒內容)。
**因果**:診斷器看不到引擎側文字。這 2 條 warning 永遠不會歸零,三個月後大家就學會忽略 warnings——紅燈正常化,和「136/0 不代表沒問題」是同一種病。
**修法**:narrative guard 給「引擎承載場」開豁免宣告(contract 標注 `textCarrier: debate`),或讓它讀 debate.json。豁免必須逐場明示,不得用場名 pattern 全免。
**會失敗的驗證**:只豁免這兩場後 ch1/ch2 歸 0;把一個真空殼場(如 ch5 E1-2)標成豁免時,guard 必須拒絕(引擎側查無對應文字)。

### C-4|ch4/ch5 十一條 warnings 的人工判讀(任務書範圍 3 的交付)

**ch4(5 條)——兩真、一組保留、一組誤報:**

| warning | 判讀 |
|---|---|
| `D0-2/n3→n4` 連續心聲 | **真債,輕微**。n3 觀察(他先看草上凹痕)、n4 自我穩定(比薩第一課)是兩個拍,中間缺一個世界拍。插一句舞台動作即解 |
| `D2-2/n9→n10→n11` 三連心聲 | **建議保留,以 contract 豁免入檔**。「他一個字都沒回→課本上只寫一行→一行。這場沉默,佔一行。」是同一戲劇拍的三口氣,收尾是 ch4 最好的心聲時刻。拆開會變弱。這正是 guard 文件自己說的「人工邊界」該接手的案例 |
| `D1-2/n4+n7` 金句雙發 | **誤報**。兩句都是「不是我量的/是我估的」——資料歸屬更正,是 speech act 不是金句。NAR-03 的「不是…是」pattern 誤中。建議 NAR-03 對含數字+量測動詞的句子降權,或逐場豁免 |
| `D4-2/n10+n16` 金句雙發 | **真債,但可辯護**。n10(現在是。二十年前不是)在做因果工作,n16(寫清楚,不是寫大方)才是真金句。建議留 n16、把 n10 改為不觸發 pattern 的寫法;或作者欄高潮場正式豁免雙發。交 Sol 判斷 |

**ch5(6 條)——全部是真的,而且是同一件事:**

3.9 節點/場、E1-2/E2-2 純 `{embed+goto}` 包裝、EE-2 連結尾都是裸的(`{review+histfacts+end}`)、E3-2/j4 五節點跑道只有 2 個可見拍。加上 CR-031 的 AS-IS 表(ch5 公開發言 1 句、心聲 0 句),結論一致:**第五章缺的不是潤稿,是參與骨架**——旅人在這章幾乎不存在。CR-031 不變量 7 的判斷正確:「須另做參與骨架,不能靠補 OS」。這應該是下一個 narrative 工作包的主體,規模比照 ch3 的 E-2 展開,而非小修。

盤點報告 B-6 的自我修正(「不能宣稱五章台詞已全精修」)與以上判讀相符,誠實。

---

## 五、六條 runtime 紅線:全數仍在,未被綠燈掩蓋(範圍 4)

| # | 紅線 | 唯讀確認 |
|---|---|---|
| ① | ch5 J3 不驗平方關係 | **實跑 `[99,1,99]` → `ok=true, evidence=J3`**,原樣復現。測試只鎖「<3 筆要錯」與正資料路徑,**沒有把錯誤行為鎖成契約**——修 J3 不會撞既有測試 |
| ② | ch3 提交前印標準斷言 | 確認在 `renderShipDossierCandidates`:`h4` 直接渲染 `○ + 完整答案`,**且下兩行的說明文字寫著「提示不會預先標出答案」**——介面自己打自己的臉,比報告寫的更難看,建議 CR-A3-PREANSWER 排最前 |
| ③ | ch4 行星封存+揭露同動作 | `predictPlanet` 單次呼叫寫入 `actual/residualPct/pass` 再立即 `openedAt`。**加一個對 Sol 有利的發現**:同檔的 `scalePrediction`(line 1035/1055)已是正確的兩段式(`openedAt: null` → 獨立揭露),修法有現成的同倉範本 |
| ④ | 三柱寫死 | `narrative.js` 4 處 `idx>=3 / slice(0,3)` 原樣 |
| ⑤ | ch2 提交前判正誤 | `updateLawHint` 的「✓ 這組數據可引用」原樣 |
| ⑥ | 未知章退回 ch1 | `chapter-ui.js:113` sanitizer 分派的最後 else 落到 ch1;`narrative.js:1039` 章別鏈的 else 落到 `"lab."`。原樣——**與 A-4 的「未修,列為 ch6 門檻」宣稱一致,沒有冒充已修** |

---

## 六、CR-031 逐項(範圍 2)

| 宣稱 | 實測 |
|---|---|
| 45 心聲全保留,17 active + 28 legacyOnly | ✅ 45/17/28 逐節點數出 |
| 17 個 active 皆有合法 `osPurpose` | ✅ 0 缺 |
| 現行圖無入邊指向 legacyOnly | ✅ 0 條(活躍節點→legacyOnly 掃描) |
| 不刪不改既有 node ID | ✅ 對 HEAD 刪除/改 id = 0;對施工前 218 → 221(+3:`c_meet` 與兩個回應節點,即早期玩家選擇) |
| 裸 `旅人` = 0 | ✅ |
| C0-1 伽桑狄首問前移玩家選擇 | ✅ `c_meet` 兩選項(「我在看自己的圖…」/「可以讓我看看嗎?」),都是關係定調不是答題 |
| 名牌統一 `旅人(你)`、讀屏分「旅人說/旅人心裡想」 | ✅ `03-busts.part.js:49-54` + `chapter-ui.js:272-276/3833-3842`,視覺與 aria 同源 |
| 負向控制會紅 | ✅ 實拔:`osPurpose→vibes` = 134/2 紅,還原 136/0 |
| ch3 敘事診斷歸零 | ✅ 0 warnings |

---

## 七、程序聲明

**我讀的是 repo 候選包**(297 行 v3.4.1-candidate),非我的生效版。我的生效版是 v3.1.0-candidate(276 行,總監 07-29 指示同步);Codex 生效版 61 行舊版。**activation 裁決通過後,兩邊生效版都需重新同步**——屆時請保留我生效版 §5 步驟 0 的 SHA 漂移自檢,或由 Sol 把它收進 canonical。

**本輪我犯的錯,已當場抓回**:第一次跑敘事診斷漏給 `--scenes`,argparse 吐 usage error、我的 grep 數到 0,差點把「五章 0 warnings」寫進報告——靠與 Sol 宣稱(1/1/0/5/6)不符才回頭看原始輸出。**對照組再次救人**。exit code 全程 `> file; echo $?` 取碼,無管線。

**我沒做的**:桌機/手機橫屏真機驗收(總監保留);285 髒檔的逐一審查(任務書排除);ch6 的 6A 內容判斷(H1 案件資格是設計決策,待總監 GO)。

**檔案影響**:repo 零修改(本報告除外)。所有變異在 /tmp 副本;repo 內跑測試產生的兩個暫存目錄已刪,工作樹 285 如初。

---

## 八、放行建議

**candidate 包 A=0/B=0,放行。**接下來依序:

1. 總監裁決三件事(承重報告 §九-3):主類型定名「歷史情境科學探究遊戲」、ch3 程序承諾 vs 新增預測(C-2 案)、修復成本差異立法(C-3 案)。
2. 總監裁 activation:41 個 error 的大宗是「candidate 狀態 + 檔案未由 Git 追蹤」,**需要您授權提交 skill 套件與法源檔**——這一步只有您能解。
3. 裁 H1 立案,開 6A(史料與案件資格,純紙上,不碰 runtime)。
4. 六條紅線依 Sol 的分批表(§8.2)各立 CR;②(A-2)建議排最前,理由見 §五。

四條 C 都不擋路,列後續即可。
