# v3.4.2 candidate activation 修正|Claude 定點覆核回覆

**審查**:Claude|**日期**:2026-07-30|**真相模式**:CONFORMANCE|**lane**:R0
**主審對象**:commit `b3008cb` + 工作樹的 v3.4.2-candidate
**範圍**:依交付報告 §六,只做三項定點;不重開 v3.4.1 全面審
**判定**:**A=0、B=0 —— v3.4.2 lifecycle 定點通過。**

---

## 定點 1|未追蹤來源的 activation 分級:通過,含危險方向

不只抽查,我把 registry 68 個 source 全掃了一遍,與 activation 輸出交叉比對:

| 檢查 | 結果 |
|---|---|
| active + untracked → ERROR | **15/15 全升,漏網 0** |
| WARN 裡的未追蹤(6 條)誤含 active? | **0** |
| candidate + `blocks_skill_activation=true`(5 個)是否被 tracking 降級洗白 | **否,5/5 仍 ERROR**(workflow-v3 / historical-inquiry / series-bible / workflow-safe-default / ch3.current-cr) |
| 22 errors 分解 | **15 條未追蹤(13 unique paths,與報告 §四-B 逐路徑吻合)+ 7 條狀態/blocker,與 §四-A 吻合** |

與 SKILL §9 的一致性:§9-3 只要求「**active** 必讀來源已由 Git 追蹤」,原驗證器把 candidate/superseded/historical 也升 error 反而是超出條文;v3.4.2 的分級才是條文的忠實實作。**這不是降標,是對齊。**同意報告 §一的自我辯護。

## 定點 2|鏡像 lifecycle 檔頭:三態全對

| 態 | 實測 |
|---|---|
| registry=candidate(現況) | 檔頭「⚠ 這是候選鏡像,不是 agent 生效版」✅ |
| /tmp 把 registry 翻 active → `sync-mirror` | 檔頭變「✅ 這是 active 治理鏡像」,且 lifecycle 句仍要求「各 agent 仍須明示同步並 read-back,不得把 repo 更新冒充已生效」✅ |
| stale:registry 還原 candidate、檔頭留 active | `validate` **exit 1**:「治理鏡像 lifecycle 檔頭與 registry 狀態不同步」✅ |

## 定點 3|測試與反向變異:通過

| 項 | 結果 |
|---|---|
| `test_skill_guard.py` | **50/50 OK**(/tmp 副本;repo 內因我沙盒 teardown 權限限制不可跑,同 v3.4.1 輪已知環境問題) |
| `test_mechanics_guard.py` | **20/20 OK** |
| runtime | **136/0** |
| 變異 1(危險方向):`untracked_source_blocks_activation` 改恆 `return False` | **FAILED(failures=1)**,`test_only_untracked_active_sources_block_activation` 轉紅 ✅ |
| 變異 2:`build_mirror_header` 的 active 分支改為永不觸發 | **FAILED(failures=1)**,`test_mirror_header_tracks_registry_lifecycle` 轉紅 ✅ |
| 還原後 | 50/50 OK ✅ |

變異方向說明:Sol 的變異 1 是「全部狀態都升紅」(寬鬆→嚴格,4 failures);我改打**反方向**「active 也不擋」(嚴格→寬鬆),這才是會放行壞 activation 的那一面,同樣被抓。兩面都紅,防護是雙向的。

## commit 精確性(§五宣稱抽驗)

- `b3008cb` 共 **12 個路徑**,全部在 `tools/skill/before-discovery-dev/` + 治理鏡像,零 runtime/劇本/美術。
- 原三個 staged 檔(CH3-CR-020/收官基線/對抗審委託)**仍在 index**。
- SKILL.md 版本字串 = v3.4.2-candidate。
- `.git/index.lock` 移除一事無法事後獨立驗證,但 index 現況健康、staged 檔完好,與宣稱一致。

## 誠實記錄:本輪我自己的兩次空變異

變異注入兩度失敗而測試「全綠」——第一次 regex 沒吃到 `-> bool` 回傳註記,第二次注入了一個未使用的區域變數。**兩次都被我腳本裡的 assert 與「先看函式本體」擋下,沒有寫成「防護失效」的假發現。**空變異的綠燈與真變異的綠燈長得一模一樣,這是變異測試最陰的坑:變異後必須先確認變異真的改變了行為,才能解讀測試結果。

repo 零修改(本報告除外);所有變異在 /tmp 副本,已還原並驗證 50/50。

---

## 結論

**v3.4.2 lifecycle 定點通過。**依報告 §六的約定條件(三項 A=0/B=0)成立。

真正 active 前還剩:總監三項內容裁決的**狀態回寫**(您已裁「照建議繼續」,但 registry 裡 workflow-v3 等 5 個 candidate 狀態要等各自覆核/裁決流程走完才能翻)、13 個 active source 的精確納管(含 ch4 migration 獨立 runtime 包,Sol 已在授權內進行)、以及最後的 registry/SKILL/鏡像**原子切換**。切換完成後,Claude 生效版由我以 `save_skill` 同步並 read-back 回報。
