# 《發現之前》v3.4.4、第三／四章、信譽與旅人聲域｜最終乾淨鏈交 Claude 對抗審

- 交付者：Sol／Codex
- 獨立審查者：Claude
- 裁決與真人試玩：總監（陳育詮）
- 日期：2026-07-30
- 真相模式：CONFORMANCE
- 審查 lane：R0（唯讀覆核）
- 審查目標：`31fa0dd9e019064c87959500c52b8aca98e6aa47`
- 判定前提：本文件不宣稱 Claude 已對最終鏈給出 `A=0、B=0`

## 一、結論先行

這份提交鏈已完成：

1. 第三章試玩回報的舊紙圖、落點幅度、辯論證據桌、問題指向與表格精簡。
2. 第四章 v0.8 runtime、schema 1→2 遷移、互動狀態與美術接線。
3. 一至五章信譽語意與可驗證事件帳。
4. 第三章旅人心聲減量，以及五章公開發言／心聲的共用可見名牌。
5. `before-discovery-dev` v3.4.4-candidate 的 HEAD-backed 啟用守衛、信譽路由與來源收斂。
6. P-6、P-9 與 archive add-only 治理裁決。

乾淨 clone 的 runtime 與守門測試全綠；skill 的一般驗證也已全綠。啟用驗證仍故意保留兩個 error，表示「尚待本輪獨立審查」，不是工程故障：

- `ch3.cr-026` 仍為 candidate。
- `registry_status` 仍為 candidate。

因此本輪請 Claude 做最後一次整合對抗審。通過前不得把 registry／skill 翻成 active，也不得把 candidate 同步成 Claude 或 Sol 的正式生效版。

## 二、唯一有效提交鏈

由舊主線 `ee330c3` 起，本輪有效鏈依序為：

| Commit | 內容 |
|---|---|
| `b3008cb` | before-discovery guard v3.4.2 candidate 基線 |
| `180652b` | 納管已核准設計法源 |
| `8aacca5` | superseded archive add-only 納管 |
| `2543e99` | 接受 P-6／P-9，收斂審查與 archive 治理 |
| `84ed6e1` | 納管第四章 spike 閉包 |
| `43ec7a4` | 啟用工作流與歷史科學探究法源 |
| `5dabfe0` | 納管章節收官基線 |
| `b600b06` | 啟用只能由 HEAD 追蹤內容成立 |
| `38e8e63` | 納管 skill provenance fixtures |
| `5c80c91` | 納管信譽治理與既有審查鏈 |
| `5e144ea` | 第四章 v0.8、schema 2 與遷移器 |
| `ec3b993` | 第二章 canonical 台詞同步 |
| `8ebcf64` | 五章證據取得儀式一致化 |
| `95d87dc` | v3.4.4-candidate：信譽任務路由至 GB-ADR-030 |
| `b0f3172` | 第三章 209 節點主線與試玩修正 |
| `0ce2bac` | 一至五章信譽狀態機 |
| `90a6cec` | 第三章旅人聲域與心聲減量 |
| `31fa0dd` | 補齊 active 法源正文、README 索引與 ch5 候選小說稿 |

曾在早期乾淨包探索中出現的 synthetic prerequisite `8449a5a` 判定 **VOID**：

- 不在上述祖先鏈。
- 主 repo 物件庫不存在該物件。
- 本輪沒有以它作為測試、提交或放行依據。

## 三、精確驗證結果

驗證環境：由 `31fa0dd` 建立的全新 local clone；測試前後 `git status --short` 皆為空。

### 3.1 Runtime

```text
npm test
136 passed / 0 failed
```

- Chapter 4 migration：19 groups／205 legacy cursors。
- 第三章：11 scenes（10 主線＋1 修復場）／221 inventory nodes。
- 第四章：13 scenes（12 主線＋1 修復場）／294 nodes。
- Chapter 4 art、Chapter 5 art、series home 與正式根入口均通過。
- canonical／JS 鏡像、stage parts／生成物、5 組 cache hash 均一致。

### 3.2 Skill 與 mechanics

```text
test_skill_guard.py       54 / 54
test_mechanics_guard.py   20 / 20
skill_guard.py validate   0 errors / 14 warnings
quick_validate.py         Skill is valid!
```

啟用檢查的預期結果：

```text
skill_guard.py validate --activation
2 errors / 13 warnings
```

兩個 error 精確為：

1. `ch3.cr-026` 仍為 candidate。
2. `registry_status` 為 candidate，尚非 active。

### 3.3 已做的反向控制

- 第四章遷移刪除一筆真實 cursor 後，精準失敗為 `204 !== 205`。
- 信譽狀態機六種變異均精準轉紅：
  - 增量白名單；
  - 本輪修復進場；
  - 正向來源去重；
  - 前置操作順序；
  - 第四章退出作者欄的相鄰唯一事件；
  - UI 儲存前修復重導。
- 旅人聲域將 `osPurpose` 改成非法值後轉紅。
- 將 `C0-1/x3` 重新接入 legacy 心聲後，兩條 legacy ingress 契約轉紅。
- skill 對 staged-only 偽啟用、active 未追蹤與狀態漂移均會轉紅。

## 四、請 Claude 集中攻擊的五個面

### A. 第四章 v0.8 與存檔遷移

1. 205 個舊 cursor 是否逐一映射到正確 `scene + node`。
2. 進行中的拖曳、封蠟、刻痕、蓋章、借條與第六槽狀態是否都能遷移。
3. 借條是否一定由玩家操作產生，系統不能憑常數代報。
4. 玩家未蓋章、未封蠟、未退出作者欄時，敘事與 state 是否都不能越級。
5. 新接入的第四章圖是否只承載質感／情緒，物理數字與軌跡仍由 SVG／引擎負責。

### B. 跨五章信譽狀態機

請以
`05_審核/發現之前_P3跨五章信譽狀態機_乾淨包交Claude定點覆核_Sol_20260730.md`
為攻擊清單，重點驗：

1. 五章能否由正式玩家操作自然歸零，而非測試直接改 `rep`。
2. 一般答錯、概念誤判與實驗失敗是否維持信譽中性。
3. 同一失信不重複扣、同一正向行動不重複加。
4. 正向事件是否可能只靠 sourceId／reason／最終狀態偽造。
5. ch3–ch5 修復後，跨序列化是否回到原 scene＋node。
6. 修復後再犯的第二輪是否仍須重新完成歸零、鎖定、進場與補正。
7. 第四章退出作者欄、第五章 J4 是否缺任一前置事件就會拒絕。

### C. 旅人聲域與第三章心聲

1. 第三章 221 inventory nodes 中，45 個原心聲節點仍保留；現行 17、`legacyOnly` 28。
2. 現行圖不得再進入 `legacyOnly`；舊存檔停在舊節點仍可續讀。
3. 公開發言與心聲的可見名牌都只顯示 `旅人（你）`。
4. 不靠括號文字，畫面仍以排版、肖像狀態與讀屏文案區分兩個聲域。
5. 讀屏仍分別為「旅人說」與「旅人心裡想」。
6. ch1／ch2 既有裸 `旅人` 只在呈現邊界正規化；ch3–ch5 不新增裸 speaker。
7. 17 個現行心聲不得偷渡答案、替 NPC 診斷、或重說畫面已演出的事。
8. `C0-1` 的早期玩家發言是否讓「旅人為何參與實驗」在動機上成立。

### D. 第三章試玩修正與「拆掉台詞仍能玩」

1. 舊紙、落點與時間流動圖是否在正確節點出現，不搶走資料判讀。
2. 加速船況的石頭落點是否是可辨識的些微偏移，而非掉出船外。
3. 辯論是否攤開所有可用原紙，並標明船況、觀察位置、變因與可支持範圍。
4. 每一題是否同步亮出「現在問的是哪張紙」。
5. 證據柱數是否由論證需要決定，不被硬鎖成三柱。
6. 誘答項是否都必須靠某張原紙／某個欄位才能否掉；不能靠常識一眼刪除。
7. 拿掉台詞與角色表演後，核心操作是否仍形成：
   `觀察 → 承諾 → 紀錄 → 比較 → 有範圍的主張 → 公開支付證據`。

### E. Skill／法源／髒工作樹邊界

1. `--task reputation` 是否精確載入 GB-ADR-030 與章別個案，且不誤載無關工作台規格。
2. staged-only、untracked 或工作樹較新內容是否都不能假裝成 active 法源。
3. normal validate 是否可復現 0 errors；activation 是否只剩本報告列出的兩個 errors。
4. `ch3.cr-026` 與 registry 翻 active 是否必須是同一原子切換。
5. 原本由使用者暫存的兩檔是否維持原 blob：
   - CH3-CR-020：`087902e1be2f46ba96ec3341345aef631d16b3a4`
   - 第一至第四章收官基線委託：`8312e0846bfca08292a491d8748c604c441290b2`
6. 本輪不得把共享工作樹的其他 modified／untracked WIP 誤認為本提交鏈內容。

## 五、已知非阻斷警告

以下不是本輪偷藏的已知問題；normal validate 會明示為 warning：

- root README 含易過期的章節狀態與測試數，不能單獨當現況證據。
- 尚缺全系列逐件美術來源／母版／生成參數 ledger 與完整視覺授權索引。
- browser checklist G9 仍保留第四章 14 場舊描述。
- 尚缺獨立的跨章存檔相容與遷移法源。
- 第三章功能規格落後最新 CR／runtime，審查必須同讀。
- 第三、四、五章尚缺章別美術製作附錄。
- 第四章施工基線保留舊盤點文字；AS-IS 必須由 runtime 即時計算。
- 第四章 D1-2 spike 尚有舊 4.9 公尺文字，不得反向覆蓋現行 runtime。
- 第五章可操作小說稿仍為 candidate reference，不是 runtime 真相。

另有兩個 candidate 狀態：

- `ch3.cr-026`：本輪放行閘門，需 Claude 覆核。
- ch5 novel：`blocks_skill_activation=false`，只作參考，不冒充 active runtime。

## 六、工作樹與交付層級

- 主工作樹仍有大量既有 modified／untracked WIP；本輪沒有「清掉」或刪除它們。
- 本輪提交均由獨立乾淨包產生，再以精確 parent／manifest 接回主線。
- 沒有使用 `git add -A`、`git reset --hard` 或廣域 checkout。
- 原有兩個 user-staged 檔案 blob 前後一致。
- 未 push、未 deploy。
- 未執行總監保留的桌機與手機橫屏真人試玩。
- 尚未同步任何正式生效 skill。

## 七、放行條件與第六章

請 Claude 對本文件 §四給出新報告：

- `A=0、B=0`：可將 `ch3.cr-026` 與 registry 原子切換為 active，再同步正式 skill。
- 有 A 或 B：只修對應問題，不重開無關範圍。

Claude 放行後，由總監做第三、四章桌機／手機橫屏真人試玩。真人試玩通過後：

- **可開始第六章 6A：歷史問題、玩家證據鏈、合作動機、互動骨架與史實查核。**
- **不可直接跳進 scenes6／engine6 runtime。**
- 第六章在設計閘門通過前，先用「去台詞決策表」驗證核心操作本身是否好玩，再寫完整台詞與美術清單。

## 八、請回覆的最小格式

```text
判定：A=?、B=?、C=?

A/B 問題：
- 位置
- 可重現證據
- 最小修法
- 修後驗證條件

若 A=0、B=0：
- 明示同意 ch3.cr-026 + registry 原子翻 active
- 明示是否同意同步正式生效 skill
- 列出仍須總監真人試玩的項目
```
