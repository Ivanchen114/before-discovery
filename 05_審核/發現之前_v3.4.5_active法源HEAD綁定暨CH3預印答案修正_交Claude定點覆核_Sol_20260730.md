# 《發現之前》v3.4.5 active 法源 HEAD 綁定暨 CH3 預印答案修正

**交付者**：Sol／Codex  
**覆核者**：Claude  
**日期**：2026-07-30  
**覆核範圍**：只驗前輪 B-1 與 C-2；不重開全面審  
**候選 HEAD**：`977942a`

---

## 一、請 Claude 只驗兩件事

### 1. B-1｜審的、玩的、路由的是否同一份

修正 commit：`fce7ebc fix: bind active sources to HEAD`

`validate --activation` 現在對 registry 全部 `active` 來源檢查：

- 路徑必須存在於 HEAD。
- 檔案的 worktree 內容必須等於 HEAD。
- 目錄來源內若有未追蹤且未忽略的子檔，也必須轉紅。
- 即使來源標成 `tracked_required:false`，只要狀態是 `active`，activation 仍必須做 HEAD 等價檢查。
- 同一路徑被多個 source 共用時只報一次，不以重複錯誤灌水。

新增反向測試：

- 修改一份 active 檔案，activation 必須轉紅。
- 在 active 目錄內新增未追蹤子檔，activation 必須轉紅。

前輪查出的 active 漂移已分兩包精確提交：

- `666a0ef docs: reconcile active chapter sources`
- `977942a fix: reconcile reviewed runtime sources`

第四章開場曾有一份未提交版本，把 `D0-2/a1…a6` 搬成
`D0-1/n6…n11`。本輪沒有把它硬提交；保留原本已審過的節點 ID，
只同步 v0.8 核准台詞與史實措辭，避免既有 schema 2 存檔斷游標。

### 2. C-2｜第三章作答前是否還會印出完整正解

修正位置：`greybox/src/chapter-ui.js`

原本題卡直接顯示：

> ○ 隔開甲板風後，停泊和平駛各做三回，艙內結果仍然相近

現在先顯示中性問題，例如：

> ○ 問題：這六張艙內原紙，最多能比較哪些船況？

五個取證題均由 `ship3DossierQuestionText()` 提供中性問題；
完整斷言只在玩家完成「選紙＋選斷言」後，沿既有
`ship3DossierAssertionText()` 揭露。

常駐測試已加入兩層守衛：

- 五題的中性問題 helper 與題型覆蓋必須存在。
- 舊的「題卡 h4 直接呼叫完整斷言」字串一旦回歸，測試必須轉紅。

---

## 二、順手清掉的三個舊殘留

這三項不是擴張功能，而是收斂 active 漂移時發現的舊版回歸：

1. 恢復 `importCurrentChapter()` 最後一道 `sanitizeLoaded()` 匯入淨化。
2. 恢復信譽 MutationObserver 首次同步只建立基準值，不把載入畫面誤報成信譽變動。
3. 第四章遷移文件保留「287 migration core＋SC4-R1 後現況 294」的雙層描述，不退回舊 287 現況。

三項均由既有測試直接鎖住。

---

## 三、乾淨 clone 復現

從候選 HEAD `977942a` 另建乾淨 clone，結果：

| 驗證 | 結果 |
|---|---:|
| `greybox/npm test` | **136/0** |
| `test_skill_guard.py` | **56/56** |
| `test_mechanics_guard.py` | **20/20** |
| `skill_guard.py validate` | **0 errors / 14 warnings** |
| `skill_guard.py validate --activation` | **2 errors / 13 warnings** |

activation 的兩個 error 精確為：

1. `ch3.cr-026` 仍是 candidate。
2. registry status 仍是 candidate。

沒有 active source 漂移錯誤。

---

## 四、反向控制

### B-1 反向控制

在乾淨 clone 只修改 active 的 `README.md`，activation 由 2 errors
變成 3 errors，新增訊息逐字為：

> `ERROR core.root-index: active source 本地內容與 HEAD 不同 — README.md`

### C-2 反向控制

在乾淨 clone 把題卡改回：

```js
ship3El("h4", "○ " + ship3DossierAssertionText(id), card);
```

`run-node.mjs` 立即轉紅，指定失敗為：

> `第三章選題缺中性問題或題型覆蓋:ship3El("h4", "○ 問題：" + ship3DossierQuestionText(id), card);`

同時舊答案直印字串的負向守衛也會命中。

---

## 五、工作樹所有權與未宣稱事項

- 總監原本 staged 的兩份文件仍在 staged，blob 未變：
  - `087902e1be2f46ba96ec3341345aef631d16b3a4`
  - `8312e0846bfca08292a491d8748c604c441290b2`
- 共享工作樹仍有其他非 active、未納入本包的歷史文件與美術 WIP；
  本輪沒有清除、覆寫或順手提交。
- 尚未把 `ch3.cr-026` 或 registry 翻 active。
- 尚未宣稱總監桌機／手機橫屏完整試玩、發布、推送或線上同步。

---

## 六、覆核判準

請只回覆：

1. active 來源任一 worktree／未追蹤子檔漂移是否確實使 activation 轉紅。
2. CH3 題卡是否只印中性問題，完整斷言是否延後至玩家操作後。

若兩項皆過，請依前輪報告 §五將 B-1 降為 0；其餘 C 項沿用前輪定性，
再由總監決定 candidate → active 的原子切換，不需重開全面審。
