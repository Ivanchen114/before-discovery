# v3.4.4・CH3/CH4・信譽聲域|最終乾淨鏈 Claude 對抗審回覆

**審查**:Claude|**日期**:2026-07-30|**真相模式**:CONFORMANCE|**lane**:R0
**審查目標**:`31fa0dd`(於全新 clone 檢出,`git status` 空)|**報告 commit**:`6d4dceb`
**判定:A=0、B=1、C=3 —— 鏈本身乾淨,但 activation 現場有一個綁定缺口,修一處即可放行。**

---

## 一、鏈與數字:全部獨立復現

| 項 | 結果 |
|---|---|
| 提交鏈 ee330c3→31fa0dd | ✅ 18 commits,與報告 §二逐條吻合;HEAD=`6d4dceb` |
| `8449a5a` VOID | ✅ 物件庫不存在 + 不在祖先鏈,雙重確認 |
| user-staged 兩檔 blob | ✅ `087902e1…`/`8312e084…` 與宣稱一字不差(第三檔已由 `5dabfe0` 合法入鏈) |
| 乾淨 clone runtime | ✅ **136/0**;ch4 遷移 **19 groups/205 cursors** |
| skill / mechanics | ✅ **54/54**、**20/20** |
| validate / activation | ✅ **0 errors/14 warnings**;activation **恰好 2 errors**(`ch3.cr-026` candidate + `registry_status` candidate),與宣稱完全一致 |

## 二、五面攻擊結果

### A 面|第四章與遷移:通過

官方逐筆迴圈的斷言邏輯我逐行讀過:205 游標各自映射 `V1_TO_V2_SCENE` → 落在 `V2_STARTS` 起點,**合成狀態(無真證據)被 milestone 守衛拉回 D1-2 而非硬保游標位置**,且每筆都走 `migrateText` + `sanitizeImport4` 往返。這個「不為保位置繞過里程碑」的設計是對的。反向控制(刪一筆 golden cursor → `204 !== 205`)前輪已實測。`removeTravelerFromAuthorField` 的 `hooke-scope-required`/`shell-page-placement-required` 雙前置與 `travelerRemoved` 早退在先前輪已驗,本鏈內容一致。

### B 面|信譽狀態機:通過,含我前輪 B-1 的回歸驗證

在乾淨 clone 重打我上輪打穿的洞:

| 偽造 | 結果 |
|---|---|
| (對照組)乾淨初始檔 | ACCEPT ✅ |
| 憑空一筆修復 +1(`SC-R1/n3`,無歸零無鎖) | ✅ **REJECT**「信譽修復加分缺少本輪歸零與進場」 |
| 憑空兩筆疊到 5 | ✅ REJECT 同上 |
| 憑空一筆一般獎勵 +1(`P0-2/nB3`) | ✅ **REJECT**「信譽事件缺少依序的玩家操作」 |

**修得比我要求的更深**:連一般正向事件都要求事件帳裡有依序的玩家操作,不只修復場。§3.3 宣稱的六種變異我採信(其中兩種即為此洞的兩面)。

### C 面|旅人聲域:通過

乾淨 clone 實數:**11 場/221 節點、心聲 45 = active 17 + legacyOnly 28**,active 全有合法 `osPurpose`,活躍圖 → legacyOnly 入邊 = 0,ch3 裸 `旅人` = 0。`C0-1/x3` 現為 active 心聲(Sol 的反向控制是「把它改接進 legacy」的變異,現狀正確)。

### D 面|第三章試玩修正與去台詞:鏈內項目通過,兩個已知債見 C 級

柱列資料驅動(`SHIP3_DOSSIER_PILLARS`)、「現在判讀的紙」、證據全攤開皆在 HEAD ✅。**但 D-6(誘答可否證)與提交前印答案未在本鏈內**,見 C-1/C-2——它們是文件在案的延後 CR,不是本鏈的回歸。

### E 面|skill/法源/髒樹邊界:一過一破

- `--task reputation` → GB-ADR-030 3 命中、`design.workbench` 0 命中 ✅
- worktree 把 registry+cr-026 翻 active(不 commit)→ **兩條精準 error**:鏡像 lifecycle 不同步 + 「skill package 尚未進入 HEAD commit,或本地內容與 HEAD 不同」✅ **staged-only 偽啟用擋死**
- **但第三種威脅沒擋**,見 B-1。

---

## 三、B-1|activation 只驗 skill 套件的 HEAD 等價性,不驗 active 法源——而 activation 現場有 11 個 active 法源正處於語意分歧

**位置**:`skill_guard.py` 的 activation 檢查(HEAD-equality 僅覆蓋 skill 套件 12 路徑)+ 主工作樹現況。

**可重現證據(兩層)**:

1. **機制層**:在乾淨 clone 對 active 法源(`雙人共用工作守則`)做 worktree 修改(未 commit)→ `validate --activation` 仍然**恰好 2 errors/13 warnings,零新增**。「工作樹較新內容不能假裝成 active 法源」(§四-E-2 第三問)對法源層**不成立**。
2. **現場層**:主工作樹掃描 59 條 active 法源路徑,**11 條是髒的**,且不是重排版而是語意分歧:
   - `scenes.json`:worktree 比 HEAD **多 2 節點**(`A1-4/n1b`、`A2-2/n4b`)
   - `scenes2.json`:worktree 比 HEAD **多 2 個心聲**(`B0-2/n1c_os`、`B3-1/n3_os`——E-2b 的成果,鏈未收)
   - `scenes4.json`:節點數同為 294 但 **12 個節點 id 互有差異**(HEAD 有 `D0-2/a1-a4` 而 worktree 沒有;worktree 有 `D0-1/n6-n11` 而 HEAD 沒有)——**雙向分歧,不是超集**
   - `run-node.mjs`:worktree 測試名含 **WB-CR-025b**(第四章工作台對話),HEAD 是舊版 WB-CR-025
   - 另含 `README.md`、`decisions.md`、`chapter-ui.js`、`stage.html`、`assets.json`、`histfacts4.json`、stage parts

**因果問題**:依 Sol 的順序,我放行後立即(a)原子切換 active、(b)同步生效版、(c)總監試玩三、四章。但**瀏覽器跑的是 worktree,鏈證明的是 HEAD**——總監試玩到的 ch4 開場結構、ch2 心聲、工作台對話化,有一部分不在我審過的鏈裡;router 讀的 active 法源也是 worktree 版本。「activation 全綠才算 active」(ADR-013)與「啟用只能由 HEAD 追蹤內容成立」(`b600b06`)的保證,在法源層有一扇沒上閂的窗——與我前輪抓的信譽 B-1 同構:三道門鎖了,第四道沒有。

**要說公道話**:那些 worktree 差異多半是**已審過但未入鏈**的工作(ch2 心聲是我寫的 E-2b、WB-CR-025b 是掛帳已久的待辦),不是野內容。問題不在內容危險,在**綁定不誠實**:審的、玩的、路由的三者不是同一份。

**最小修法(擇一,甲較徹底)**:

- **甲**:activation 檢查擴到所有 `status=active` 來源——worktree blob ≠ HEAD blob 即 error 並列出路徑。以現況會正確地擋住切換,直到那 11 路徑走精確 commit(內容多半已審過,補幾個小包即可)或還原。約十餘行 + 一個雙態測試。
- **乙**:不改碼,程序解:ADR-013 補一句「activation 綁 HEAD;真人試玩以乾淨 clone 起服務」,總監試玩改跑 `31fa0dd` 的 clone。但這讓主工作樹永久處於「active 法源 ≠ 生效內容」的狀態,治標。

**會失敗的驗證條件**:任一 active 法源 worktree ≠ HEAD 時 `--activation` 必須 FAIL 並逐路徑列名;全數一致時恰好剩 2 個預期 error。負向:把守衛退回「只驗 skill 套件」,新測試必須紅。

---

## 四、C 級(3 條,不阻擋修 B 後放行)

**C-1|D-6 誘答可否證未落 runtime**:`engine3.js:2408/2441` 仍有「石頭會自己追著桅杆」「石頭會突然變重」等稻草人;ch3 辯論資料無 `refutedBy`。法源(§3.7)與機檢(mechanics guard)已備,缺的是五章 contract 實檔與 ch3 四處改寫——已在 承重報告 C-1 與 §8.2 在案,非本鏈回歸。列此避免「A=0/B=0 → 以為誘答已修」的誤讀。

**C-2|A-2 提交前印答案仍在 HEAD**(`chapter-ui.js:2793`,`h4` 直接渲染完整斷言,下兩行還寫著「提示不會預先標出答案」)。它是文件在案的 CR-A3-PREANSWER,**但總監下一步就要試玩 ch3**——會親眼撞上這條 A 級代理權紅線。建議:修 B-1 的同一輪順手把這條收掉(改中性題目一處,DOM 契約一條),讓總監試玩的是不自打臉的版本;或至少讓總監知情進場。

**C-3|`ch3.cr-026` 翻 active 時同步改檔頭**:該檔 `IMPLEMENTATION GATE:待獨立覆核` 是 required heading。我本輪已覆核其正向路徑與失準修復路徑(runtime 行為在信譽輪已實測:C0-2 提議、C0-3 誘惑、伽桑狄兩理由交權)。翻 active 時檔頭應同步記錄覆核結論,否則 active 文件自述「待覆核」,又是一個小型狀態矛盾。

---

## 五、依 §八 格式的回覆

```text
判定:A=0、B=1、C=3

B-1:
- 位置:skill_guard.py activation 檢查(HEAD-equality 只覆蓋 skill 套件)+ 主工作樹 11 條髒 active 法源
- 可重現證據:乾淨 clone 修改 active 法源→activation 零反應;主工作樹 scenes/scenes2/scenes4/run-node 與 HEAD 語意分歧(雙向)
- 最小修法:activation 擴驗所有 status=active 來源的 worktree==HEAD;分歧路徑走精確 commit 或還原
- 修後驗證:髒 active 法源→FAIL 逐路徑列名;乾淨→恰 2 預期 error;守衛退化→測試紅

B=0 之後(預先聲明,免再一輪往返):
- ch3.cr-026 + registry 原子翻 active:同意(附 C-3 檔頭同步)
- 同步正式生效 skill:同意;Claude 生效版由我以 save_skill 同步並 read-back 回報
- 總監真人試玩保留項:桌機+844×390 橫屏、ch3 舊紙/落點/辯論桌/心聲密度體感、
  ch4 紙桌/軌道/作者欄、選項誘惑力與「結論是我自己做出來的」體感(承重報告 §十一之 1-7)
```

---

## 六、程序聲明

我犯過的坑本輪的迴避紀錄:遷移手工探針回 `bad-v2-seed`——這正是我在 ch3/ch4 輪誤報過的同一形狀(遷移需引擎產的 schema-2 種子)。這次先讀了官方 harness 的呼叫方式與斷言邏輯再下結論,**未誤報**;A 面採官方逐筆迴圈的綠燈 + 斷言邏輯人工審讀為證據。

repo 零修改(本報告除外);全部變異在 `/tmp/clean`(31fa0dd 的獨立 clone)與 /tmp 副本,已還原或即棄。主工作樹的 `git status` 與我開工前一致。
