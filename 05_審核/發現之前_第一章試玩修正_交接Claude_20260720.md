# 《發現之前》第一章試玩修正｜Sol → Claude verification 交接(2026-07-20)

## 一、任務性質

總監已授權 Sol 直接完成本輪試玩修正；此件請 Claude 只做 **verification pass**，不另開設計討論輪。若發現程式缺陷，按 A/B 級附可重現路徑；純偏好列 C 級，不阻擋本次基線。

## 二、已修問題

1. **實驗失敗訊息誤導**：「選集內部不一致」已撤除，改為兩道獨立亮牌：前四段形狀偏差、第五段預測偏差；兩項門檻皆為 12%。
2. **器材差異不透明**：計時器選單公開天數與性格；首次選擇可聽到伽利略的器材評論。評論只說器材脾氣，不替玩家判答案。
3. **卡關無診斷**：連續失敗第 2 次顯示中性觀察；第 3 次探索模式自動給診斷問句，學者模式改為玩家主動按「與伽利略討論」。
4. **反覆進出實驗室**：A2-2→A2-3 的規律、換球、換傾角三段視為同一工作階段；只在主實驗首次進場設確認閘。SC-R1 信譽修復仍有獨立進場。
5. **辯論像後台表單**：改成三支柱戰況、證詞卡、證據手牌與自然語言行動句；不再用證據／子項／目標三層下拉選單。
6. **追問沒有玩法產物**：改名「問到底——讓他把前提說滿」；每句首次問清後產生並保存一條 `insight`，顯示在證詞卡；重複追問拒絕且不扣分。
7. **辯論失敗回實驗室沒有意義**：說服力歸零改為與伽利略複盤本輪證據—證詞錯配，不新增實驗；信譽歸零才進 SC-R1 做乾淨紀錄。雙歸零時兩段依序發生。
8. **模式錯把玩家按學段分類**：標題改以玩法偏好描述探索／學者模式，不再寫「國中基準／高中以上」。

## 三、不變邊界

- 實驗 fixture、12% 判定線、216 判定矩陣不變。
- 證據取得條件、三支柱弱點、說服力／信譽扣值不變。
- 凍結劇情的科學結論與章末結果不變；A3-F 僅修正失敗迴路與對應台詞。
- `chapter.html` 與 `stage.html` 仍共用同一引擎、資料與存檔。

## 四、自動驗證

- `cd greybox && npm test`：**57/57 通過**。
- `node --check`：`chapter-ui.js`、`stage-ui.js`、`narrative.js`、`ui.js`、資料鏡像均通過。
- 新增常駐測試：失敗提示連續數、追問洞見與重複拒絕、A3-F debrief 分流、雙歸零只在 SC-R1 新增一次實驗、辯論桌 DOM 契約、非學段模式文案。

## 五、請實機驗證的最短路徑

依 `greybox/docs/browser-checklist.md` G7：

1. `stage.html` 新遊戲 → 對話播完後才出現「前往實驗台」，鍵盤焦點落在按鈕。
2. 陡坡＋水鐘故意失敗 → 兩道偏差各自顯示；連續失敗 2／3 次的提示分層正確。
3. 完成 E3.a → E3.b → E3.c，確認三段之間沒有重複進場閘。
4. 辯論先「問到底」看洞見，再以錯證據把說服力扣到 0 → 進伽利略複盤而非實驗台 → 重返後已破支柱保留。
5. trap 走說謊使信譽歸零 → SC-R1 必須新增一筆乾淨紀錄；若同時說服力歸零，之後仍須再走一次論證複盤。
6. 1280×720 與 844×390 橫屏各看一次證詞卡／證據手牌是否可操作、是否溢位。

通過後回覆「verification pass」即可；不要改 Sol 本輪檔案，若有缺陷請列路徑交回修正。
