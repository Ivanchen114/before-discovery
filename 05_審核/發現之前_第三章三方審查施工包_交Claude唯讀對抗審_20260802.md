# 《發現之前》第三章三方審查施工包｜交 Claude 唯讀對抗審

- 日期：2026-08-02
- 創作・設計：臺北市立松山高中物理教師 陳育詮
- 審查性質：唯讀、對抗式；不得代為修改、提交、推送或發布
- 施工依據：`05_審核/發現之前_第三章台詞審查_三方交叉驗證彙整報告_20260802.md`
- 工作樹狀態：共享 FOREIGN-WIP。第四章與共用 UI 另有並行施工；請只判本文件列出的節點與契約，不以整份 `git diff` 歸責。

## 一、先給結論，不請你背書

三方報告原判 **A=0、B=7、C=8**。獨立施工前複查將其中兩條升級，因此本包採 **A=2、B=5、C=8**：

1. `C0-1/xcard` 在第一次實驗前直接說出穩速落點，屬答案提前洩漏兼史實／合理重建界線混淆。
2. `C0-3` 的 `fake + f1 + 舊 f2` 依序交付 R「紙載桅後」、M「船速缺欄」、S「不能代表其他船況」，玩家重選前已拿到完整正解。

兩項 A 均已修，但這份文件不自行宣告放行；請用下列攻擊面獨立判定。

## 二、送審範圍與 SHA-256

| 檔案 | SHA-256 | 用途 |
|---|---|---|
| `greybox/data/scenes3.json` | `2ae101d0d1c1fbe3bf4b6eebf7ec7e33315073b70aa3dad1f58bcd9c540c14f6` | 第三章 canonical 正文 |
| `greybox/data/scenes3.js` | `855c57703607309f704761be0b8fa14ab9ce50b6f3f91ece0dcbfd581ce5c188` | canonical 生成鏡像 |
| `greybox/data/scenes5.json` | `b37fe12f1ce9e774320732d2286f6d21c21b7f12c01ccb649c01802bed931d0c` | 第五章跨章引句一處 |
| `greybox/data/scenes5.js` | `fb93ac0f9487de59a831653e819d11336f62fb36f165ee4ac9de3faa346f5de4` | 第五章生成鏡像 |
| `greybox/src/chapter-ui.js` | `febb103c30b41639651566d7eadf9b7d71b64fda4e55d0ecd2d4852b8c97ae51` | 只審 `ship3SayAssertionBeat` 的 A3 旅人句 |
| `greybox/tests/run-node.mjs` | `cd42b9987a8ba73e93ddf3b784faf83167165a75dc523794911b9baf77866636` | 常駐契約與反向控制 |
| `04_劇本/第三章台詞稿_v0.9_展開版_Claude_20260728.md` | `bd2fbbf160b35821309c930ffffbcd36c3f9ea39e2f71cae481218f4bd7376ec` | 逐拍對照稿同步 |
| `04_劇本/發現之前_第五章E-3展開稿_v0.1_Claude_20260731.md` | `4e2ba40b58dcdb7764e1c0790850e3226085892845562c4d9a9998cbb6532b41` | 第五章回呼同步 |

## 三、實際施工清單

### A 級修復

1. `C0-1/xcard`：改成「馬賽槳帆船落石實驗可由史料確認；1640 年依書信線索採用；借船、船長、舊紙與卷宗為合理重建」，並把結果留到章末。不得出現「落在桅腳／桅杆下／跟船一起走／不落後」等同義洩漏。
2. `C0-3/f2`：只請玩家回頭檢查欄位，不再替玩家說出 S「不能代表其他船況」。`fake` 已含 M，`f1` 只交付反駁錯誤在邏輯上必須說的 R。

### B 級修復

1. `C3-1/x19`：明示「少了一欄的這張舊紙」是保留對象，避免讀成回應上一句心聲；不把不完整舊紙稱作「錯的」。
2. `C3-2/x4`：取消「只能一次取對」，改為題名不得替卷宗說過頭；三個題名都不帶信譽正誤。
3. `CE-1/x18`：由「替他量完」收限為「在這艘船上量了一回」。
4. `CE-1/x11`、`CE-2/x3–x5`：撤除捏造的伽利略引句「先別急著相信我」，分別回到第一章 canonical 的贈冊口說與首頁題字。
5. `CE-2/n2` 改為 `旅人・心聲` 並標 `osPurpose:naming`；`CE-2/n4` 改為 `stage`，讓親筆書寫不再像公開發言。

### C 級與報告外補修

- `C0-2/x3` 問號、`C0-3/n2` 紙面紀錄／船況真相界線、`C1-1/e1.hint` 船務白話、`INT-C2/x7` 《對話》的問答結構、`CE-2/s1` 移除「第三章」製作術語。
- `CE-1/n6` 採「五十多年前」，沒有採報告的「五十二年前」：事件只確知 1590 年，沒有月日；精確到 52 年是假精確。
- `C0-3/xok`、`x7`、`a1` 與 `SC3-R1/w2` 一併清掉會替未驗證主張背書的「答案」語彙。
- 報告漏查第五章 `EE-2/x5`；已同步回到第一章題字，並更新第五章 E-3 對照稿。
- 動態工作台 A3 旅人句由「所以它只能說不知道」改為「所以船況那一欄只能寫不知道」，避免把舊紙已記下的落點也一併抹除。
- 第三章 v0.9 文件頭更新為 11 場、194 active、29 legacyOnly、223 total；歷史算式保留但明標快照。

## 四、結構與相容性證明

以 HEAD 的 `scenes3.json` 與送審版投影下列欄位後逐位元組比較：

`scene.id / node.id / node.type / next / require / legacyOnly / option.id / option.next / option.require / option.effects`

結果：`shapeEqual = true`。

- 場景：11
- 全節點：223
- active：194
- legacyOnly：29
- 本輪沒有新增、刪除或重接節點，也沒有改信譽效果、證據、條件或存檔游標。

## 五、常駐契約與反向控制

`greybox/tests/run-node.mjs` 新增「第三章三方台詞審查」契約，並用同一驗證器執行八組變異；任何一組未轉紅，測試本身就失敗：

1. 合理重建卡提前寫出穩速結果。
2. 重試句重新交付 R／M／S 完整正解。
3. 題名恢復成唯一正解。
4. 親筆書寫退回公開發言。
5. 第三章重新捏造伽利略引句。
6. 第五章再次沿用捏造引句。
7. 題字換回後，前句仍稱「空白頁」。
8. 題字換回後，後句仍回答「我到現在都還沒有」。

重試契約不逐字鎖死 `f2`，而是把 `fake + f1 + f2 + xf` 拆成 R／M／S；三塊在重選前同時出現即失敗。這是本輪最重要的方法論產物。

## 六、已執行驗證

- `npm test`：**139 通過、0 失敗**。
- 第四章存檔遷移：**19 groups passed；205 legacy cursors covered**。
- `git diff --check`：exit 0。
- 第三、第五章 JS 鏡像與 canonical JSON 深等契約：通過。
- 正式正文／現行劇本（排除審查報告、封存與負向測試）搜尋「先別急著相信我」：0。
- 瀏覽器定點抽查：標題頁作者署名可見；第三章正式入口可啟動；844×390 下開場台詞 24px、兩行完整落在 viewport 內；console error/warning 0。

這不是總監完整通關，也不是讀屏、鍵盤、真機觸控或全章真人節奏簽核。

## 七、請 Claude 實攻，不要只重跑綠燈

請逐項回報 A／B／C，且每一項必須附「位置、可重現攻擊、最小修法、轉綠條件」。至少攻以下八面：

1. **第一個玩家實驗前的所有 active 可見文字**，是否仍以任何同義句提前交付穩速結果。
2. `C0-3/fake → f1 → f2 → xf → c1` 實際順序，重選前是否仍能拼出 R／M／S。
3. 第一章 `P0-3/n2` 口說與 `P0-3/n3` 題字，是否被第三、第五章正確區分；不得把題字說成口說。
4. `CE-2/n2–n4` 的可見名牌、立繪、語音／讀屏媒介是否符合「心聲→動作→書寫」，不是只看 JSON speaker。
5. `CE-2/x3–x5` 換回真正題字後，前後語意是否自然、有指涉。
6. `ship3SayAssertionBeat` A3 是否準確限定「船況欄未知」，且不抹掉落點紀錄。
7. HEAD→送審版第三章結構投影是否真的完全相等；舊游標是否仍可續讀。
8. 八組反向控制是否各自因宣稱的契約轉紅，而非語法錯誤、未定義變數或別層副作用。

若你認為原三方報告的 A=0 應維持，請直接反駁本包升列 A=2 的判準；不得只沿用原分級。

## 八、請回傳的判定格式

1. `A = ? / B = ? / C = ?`
2. `可放行／有條件放行／不得放行`
3. 每項問題：`位置 → 攻擊證據 → 直接修法 → 可機械驗證條件`
4. 另列 `未驗`：真人閱讀節奏、完整瀏覽器通關、無障礙、844×390 真機、聲音。
