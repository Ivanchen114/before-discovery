# 第四章 v0.8 存檔遷移｜乾淨可重現包施工交接

**日期**：2026-07-30  
**執行**：Sol／Codex  
**真相模式**：CONFORMANCE  
**風險**：R4  
**狀態**：施工計畫與乾淨暫存重建已驗證；共享 repo runtime 尚未修改  
**不變量**：只重建 pre-reputation 的 ch4 v0.8 migration core，不夾帶 ch3、ch5、
跨章信譽、`SC4-R1` 或其他共享工作樹 WIP

## 一、結論

四個未追蹤 migration 檔不能單獨構成可重現交付。從 HEAD 的乾淨暫存 clone，
依本檔列出的完整檔與精確 hunk 重建後，已得到：

- `scenes4`：12 場、287 節點；
- `node tests/run-ch4-migration.mjs`：19 groups passed、205 legacy cursors covered；
- 反向控制：從 golden cursors 刪除任一游標後 exit 1，報
  `204 !== 205`；還原後再綠。

因此本包已可零歧義施工，但不能在目前 dirty shared worktree 對共用檔做 whole-file
stage。若本輪禁止 runtime 修改，只能保持 `TRACKED CANDIDATE / BLOCKED`，不得稱為
完整可獨立驗證。

## 二、可完整納入的檔案

以下五檔使用目前 working tree 原文：

1. `03_規格/發現之前_第四章v0.8存檔遷移表與契約測試_Claude_20260728.md`
2. `greybox/src/ch4-migration.js`
3. `greybox/tests/run-ch4-migration.mjs`
4. `greybox/tests/fixtures/ch4-v1-base.json`
5. `greybox/tests/fixtures/ch4-v1-cursors.json`

## 三、由現況機械衍生的章別檔

### 3.1 `greybox/data/scenes4.json` 與鏡像

從目前 `scenes4.json` 只移除 `scene.id === "SC4-R1"`，結果必須正好為 12 場、
287 節點；不得另改任何節點或欄位。再執行：

```bash
cd greybox
node tools/build-ch4-data.mjs
```

驗證：

```bash
node -e 'const s=require("./data/scenes4.js");if(s.scenes.length!==12||s.scenes.reduce((n,x)=>n+x.nodes.length,0)!==287)process.exit(1)'
rg -n 'SC4-R1|"rep"' data/scenes4.json data/scenes4.js
```

最後一個命令預期零命中。`histfacts4.*` 不變，不納入。

### 3.2 `greybox/src/engine4.js`

以目前整檔為基礎，只移除後加的三組信譽碼：

1. `removeTravelerFromAuthorField` 的 `repDelta`／`repReason`，並修正前一欄尾逗號；
2. `setHookeScope` 的 `firstScopeAttempt` 與兩個信譽分支；
3. `setBoundary` 的 `firstBoundaryAttempt` 與兩個信譽分支。

驗證：

```bash
rg -n 'repDelta|repReason' src/engine4.js
```

預期零命中。

### 3.3 `greybox/src/sanitize.js`

以 HEAD 為底，只將 `/* 第四章白名單 */` 至 `/* 第五章白名單 */` 前的
`sanitizeImport4` 整段替換為目前版本，並將其中三行 reputation lifecycle 呼叫
精確改為：

```js
var milestoneCursor = state.cursor;
```

禁止納入目前檔案前 702 行的 `sanitizeReputationLifecycle`，也不得帶入
`sanitizeImport1/2/3/5` 變更。

## 四、共用檔只取精確 hunk

### 4.1 `greybox/src/narrative.js`

只納入：

1. `SAVE_SCHEMA` 的 ch4 = 2；
2. `until.orbit` 的 `source-k0`；
3. `chapterEndReady` 與 advance／histfacts／end 三處守衛；
4. `labAction` 的第四章 dispatcher：
   - tangent／orbit：`sealTangentPrediction`、`sealOrbitRule`、
     `nudgeOrbitAim`、`commitOrbitBeat`、`resetOrbitBeats`、
     `continueOrbitRule`；
   - 四個 scale actions；
   - 五個 ledger actions；
   - 三個 shell／author actions；
5. D3-1 opening flag 擴成 `D3-1 || D4-1`；
6. K1–K5 state-truth revoke；
7. lab event 改為物件，ch4 附 `sequence`，`assertK1–K4` 附 `args`；
8. embed opening scene 的 `D3-1 || D4-1`。

排除：

- `REPAIR_SCENE` map；
- `applyRep`／`applyRepOnce`／reason／`repSource`；
- `redirectIfLocked`；
- ch3／ch5 hunk。

### 4.2 `greybox/src/chapter-ui.js`

只納入：

1. 目前的 `migrateLegacyCh4` 與 `preserveCh4MigrationBackup`；
2. 目前 `inspectSaveText` body；
3. `restoreBackup`／`tryLoad` 的 backup 與 notice；
4. `SPOKEN_RE`、`parseSpokenLine`、`sayIntoDialogue` 三段；
5. HEAD 的 `/* 第四章軌道… */` 至 `/* 第五章… */` 前整段，替換為目前
   第四章段落；
6. `startGame` 重置 `orbit4CoachSpokenKey`／`orbit4PendingCoach`；
7. 目前 `importCurrentChapter`；
8. 匯入成功時的 preserve backup 與 notice。

排除：

- `DEBATE_SAY_ACTIONS`／`sayDebateBeat`／`doShip`；
- 旅人心聲顯示與 `renderStatus` 信譽 reason；
- 全部 ship／ch3／ch5 hunk。

### 4.3 `greybox/stage.html`

只納入：

1. queue-active selector 增加
   `body[data-view="orbit"].queue-active #panelWrap`；
2. `/* 第四章：牛頓的紙桌… */` 至 `/* 第五章… */` 前的第四章 CSS；
3. `scenes4` 後、Narrative／UI 前新增 `ch4-migration.js` script；
4. 僅更新 `sanitize`、`engine4`、`scenes4`、`ch4-migration`、`narrative`、
   `chapter-ui` 六個 cache key；檔案定稿後用各檔 SHA-256 前 12 碼。

排除全站 asset-key rewrite、rep tooltip、ch3／ch5 CSS。

### 4.4 `greybox/tests/run-node.mjs`

只納入：

1. 前端語法清單加入 `ch4-migration.js`；
2. HEAD 的「第四章資料鏡像」至第五章 fixture 前舊 ch4 測試群，替換為目前
   「第四章 v0.8 場景圖」至「第四章 UI 契約」；
3. pre-reputation 修正：
   - `expectedScenes` 移除 `SC4-R1`；
   - 294 改 287，名稱改為 12 主線；
   - visited pending 移除 repair start；
   - 刪除 full-walk 中「K5 作者欄加分與退出操作被其他事件插隊」的
     `repIndex` 測試。

長局測試目前與 HEAD 相同，不改。

### 4.5 `greybox/package.json`

`test` script 只加一個 hunk：在 `run-node.mjs` 後加入
`node tests/run-ch4-migration.mjs &&`。

## 五、整體排除清單

不得納入：

- `histfacts4.*`、`run-ch4-art.mjs`、`narrative-suite.js`；
- `assets*`、`stage-ui`、stage parts、`build-stage`；
- ch3／ch5 data、engine、UI 與 tests；
- 全部 reputation lifecycle、`SC4-R1`、rep reason WIP。

## 六、乾淨重建與驗證順序

只在乾淨暫存 clone／worktree 施工；不在 shared worktree 整檔 stage。

```bash
node -e 'const fs=require("fs");const p="greybox/data/scenes4.json";const d=JSON.parse(fs.readFileSync(p));d.scenes=d.scenes.filter(s=>s.id!=="SC4-R1");fs.writeFileSync(p,JSON.stringify(d,null,1)+"\n")'
cd greybox
node tools/build-ch4-data.mjs
node tests/run-ch4-migration.mjs
node tests/run-node.mjs
npm test
git diff --check
```

驗收：

1. migration：19 groups／205 cursors；
2. 刪除一筆 golden cursor：exit 1、`204 !== 205`；
3. 還原後再綠；
4. `run-node` 新 ch4 群全綠；
5. `npm test` 0 failures。乾淨 HEAD 若因原本未追蹤的美術／法源失敗，須列為
   foreign baseline，不得誤歸因 migration；
6. 最後按本檔 manifest 精確 stage，禁止 `git add -A`／`git add .`。

## 七、暫存重建雜湊

| 檔案 | SHA-256 |
|---|---|
| `data/scenes4.json` | `851133898fe632dd84b6fa57b2f3406d17f73216a8fc36f79262415705bedce6` |
| `data/scenes4.js` | `57ae334a27672b49d26e8ae814a7992540ca3d9785abf7d233ff3939de889940` |
| `src/engine4.js` | `c835d851f05b843c583d80a54a081b5d1ffca3054cad2e762996a04adacffb22` |
| `src/sanitize.js` | `ba2947d8137a0d32e97d06cdb7e021738bc7d4f380ea79238f79412b5ff088dc` |
| `src/ch4-migration.js` | `7b6d538d54251c18b8a68fdf3e4528d377b8e5b011ec3b529de86e11ef4812dc` |
| `tests/run-ch4-migration.mjs` | `98b29568ffa969ee6edbc19a60b02763c42906f06e051d36bc9c3c369b63a203` |
| `tests/fixtures/ch4-v1-base.json` | `f1ab8470ea2972617bb6dc3b7974fe34666333e9d0fa8825a8b240f573645c42` |
| `tests/fixtures/ch4-v1-cursors.json` | `7a5e68688ceedc59f1072e4600c0d1fd6b3ac906eb0af5027f38b937c0cb83ea` |

雜湊只證明本次暫存重建內容；正式施工若修正任何 hunk，必須重算，不能用本表冒充
最後 commit。
