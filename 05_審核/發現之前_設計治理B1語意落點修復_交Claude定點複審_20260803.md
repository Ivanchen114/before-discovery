# 《發現之前》設計治理 B1 語意落點修復｜交 Claude 定點複審

**日期**：2026-08-03
**施工歸屬**：Codex 工作流程
**上游發現**：Claude 合併對抗審第一批，Layer B `A=0 / B=1 / C=1`
**狀態**：工作樹已修、尚未 commit；第六章同時施工中，待其收束後另產穩定 commit 錨點

## 一、原 B

舊 `run-design-governance.mjs` 把八份 CURRENT 全文串起來，只用
`concatenated.includes(id)` 判斷 ADR 是否有落點。跨章規格的一張 001～041 全號碼索引
因此可替所有章節假裝完成；刪掉第四章 `GB-ADR-040` 仍全綠。

## 二、修法

1. `03_規格/current-specs.json` 升為 schema 2，`adrCoverage` 逐筆指定 active／partially-superseded ADR 必須落在哪些 CURRENT 文件。
2. 跨章、第一至六章 CURRENT 規格新增 `ADR 語意落點` 表；每一列不是號碼清單，而是一句可對 runtime／治理核對的責任。
3. 跨章規格移除 001～041 的「機器覆蓋索引」與大範圍代碼列，不再吞掉章別責任。
4. 治理測試逐檔解析指定章節：
   - active ADR 必須有 coverage；superseded ADR 不得混入；
   - 指定文件必須存在該 ADR 的獨立語意列；
   - 語意不得過短，也不得只用另一個 ADR 編號代替；
   - 同文件重複列、全號碼機器索引與 ADR 範圍列均轉紅。
5. 同步承接工作樹新增的第六章 `GB-ADR-042`，但未改第六章劇情／引擎裁決。

## 三、已重跑的原攻擊

在臨時副本單獨刪除：

```text
03_規格/發現之前_第四章功能規格書_v0.2.md
| GB-ADR-040 | K1 先封規則與形狀預測、玩家畫三拍、牛頓續畫，最後依實際路徑分類。 |
```

結果：`run-design-governance.mjs` exit 1，精準訊息：

```text
✗ 設計治理｜ADR 指定落點未形成語意：GB-ADR-040:ch04 缺獨立語意列
```

原 B 的假綠路徑已轉紅。

## 四、正向驗證

- `git diff --check`：通過。
- `npm test`：153 通過、0 失敗。
- 第四章 migration：19 組通過、205 個舊游標覆蓋。
- 治理測試：41 筆 active／partially-superseded ADR 均在指定 CURRENT 文件有獨立語意列。

`149 → 153` 的四項新增是同時進行的第六章 R-CH6 契約，不是本 B1 修復新增；不得混成同一施工歸屬。

## 五、請 Claude 接續

1. 先完成原合併任務書仍未覆蓋的 Layer A 六組反向控制與 Layer B 其餘攻擊。
2. 待第六章施工 commit 與本修復形成穩定新錨點後，再對本 B 做定點複審。
3. 優先再攻：
   - 從 `adrCoverage` 移除 `GB-ADR-040` 的 `ch04` owner，是否有另一個獨立 oracle 阻擋；
   - 保留語意列但改成與 runtime 相反的說法，機器測試無法判斷時須明列為人工語意審查邊界；
   - 第六章尚未上線時，CURRENT／runtime 施工基線的標示是否足夠避免被誤讀為已發佈功能。

## 六、驗收邊界

本修復只處理「形式引用冒充語意落點」。它不宣稱機器能證明每句規格都符合 runtime；
那仍須逐章抽查與反向控制。尚未 commit、push、release 或做 production smoke。
