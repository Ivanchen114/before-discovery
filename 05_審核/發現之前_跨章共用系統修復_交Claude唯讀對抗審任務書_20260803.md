# 《發現之前》跨章共用系統修復｜交 Claude 唯讀對抗審任務書

日期：2026-08-03
施工流程：Codex
審查方式：唯讀；不得修改工作樹
施工依據：`05_審核/發現之前_跨章共用系統_三方交叉驗證最終報告_20260803.md`

## 一、送審邊界

這一輪只主張完成下列三組修復：

1. 第一、二、五章辯論改成「先對證據，再由玩家自己判斷證據能推出什麼」；缺少或寫壞推論資料時 fail closed。
2. 第一、三、五章存檔清洗不再相信完成旗標與自稱結論；改由原始紀錄、斷言與事件鏈重新推導。
3. 維持非拖曳操作；補第二章 44px 觸控目標與第五章 844×390 橫屏版面。

本輪**沒有**導入跨章 append-only action ledger，也沒有宣稱真人 iPhone、讀屏器或五章完整人工遊玩已驗收。

工作樹另有第四章、設計治理與第六章既有施工；請勿把那些差異歸入本輪結論。

送審 HEAD：`0a5425af23c02fa85cea501bbc87306097aa04b5`

## 二、送審檔案 SHA-256

| 檔案 | SHA-256 |
|---|---|
| `greybox/data/debate.json` | `724188c83048e2f1ceb60702f27380334330195b6a9febddc583feeee4b0d77d` |
| `greybox/data/debate.js` | `078769d397613637c5c08ee400e30507ffe72b8fee0124e9ea9555dac47a91fd` |
| `greybox/data/debate2.json` | `cdf4bb66aac3beda4eaa003caf68fa6d31285f66f28c8cdf74927797a71b3362` |
| `greybox/data/debate2.js` | `95645c9fb8f42cd6eac824453641c8162b23561c318f8edb72237b2aa0da6bca` |
| `greybox/data/debate5.json` | `fb9719db90ebe397a0165cf2da29f361e6935543f00a976f85dc78485fe6e3e4` |
| `greybox/data/debate5.js` | `d2271e46933b6dfc1fb206488a6749019ae55449046df02cb718e5fe394400cd` |
| `greybox/src/narrative.js` | `4f8ef879ababede636ce973d6d77db55e55d04886ab2a9dc4daf0c384f38d58a` |
| `greybox/src/sanitize.js` | `90e1bc396ff9f57f304ee4e68639a4e0c43a744ffdc7c02af477d423f349fc2d` |
| `greybox/src/chapter-ui.js` | `b230d33a8751495099939350cda2f8301625d917e76002bfd643a6fa4477692f` |
| `greybox/stage.html` | `8d01c893d3071cce362b6f98fccc65fa29341306080a8c095723731613757275` |
| `greybox/tests/narrative-suite.js` | `b4346c462baf85574a36bbf8cdaebb47c6164f195bdfc25da7202eced90492fa` |
| `greybox/tests/run-node.mjs` | `840a8503d36f1fc0b36ddbbb2ad48b9290894378bc7b1385e8cc5258674a2c94` |

如任一 SHA 不符，請停止把不同版本混成同一輪判定；先回報漂移檔案。

## 三、請實攻的宣稱

### A. 玩家推論代理權

1. ch1 P1/P2、ch2 P1/P2/P3、ch5 P1/P2/P3 都必須明示 `reasonMode: "player"`，每柱恰有三個互異選項且恰一個正解。
2. ch1 P3 是刻意的自動收束，必須明示 `reasonMode: "auto"` 且不得夾帶玩家選項。
3. 缺 `reasonMode`、未知 mode、player 缺 prompt、選項不是三個、零或多個正解、錯項無 reply，都不得默默退回自動結論。
4. 選對證據只應進入 `pendingReason`；答對推論後才真正擊破支柱。答錯可重試，不得把正解寫進存檔。

### B. 存檔投影與防偽

1. 第一章原始讀值必須由 patterns、器材配置與輪次重算；run id、seq、patternIndex、runSeq 不得跳號或互相矛盾。
2. 第一章 claim 的 `observedFifth/predHit/consistent/ok/predDev/maxDev` 必須由引用原紙重算；跨配置 assertion 與 E3.a/b/c 也須由 claim 重算。
3. 第三章故事證據 G1–G5 必須逐項等於工作台 g1–g5；只改故事旗標不得通過。
4. 第五章碰撞與黏土原紙必須由 fixture 重算；J1–J3 須對應真實 assertions，J4 須有完整辯論勝利事件鏈。
5. ch5 舊存檔遷移不得再從 `evidence` 布林反向補出玩家 judgments；只可從原紙與已完成斷言推導。

請特別攻：合法舊存檔相容、重複 ID、引用不存在的原紙、只改 `claim.ok`、只改 assertion、只改章節 evidence、偽造 eventLog 順序、缺 judgments 再塞完成旗標。

### C. 非拖曳與手機

1. 玩家主線不得新增 `pointerdown/pointermove/dragstart/draggable` 依賴；按鈕／轉鍵路徑必須可完成。
2. 第二章斜面工作台在低高度橫屏的 select、校準、測量與門檻操作目標不得小於 44px。
3. 第五章 844×390 必須進單欄可捲動版面；設定、判讀與送出控制不得被裁掉。

## 四、指定反向控制

每組都請在隔離副本執行，記錄是否轉紅及精確訊息；完成後還原。

1. 刪除 ch1 P1 的 `reasonMode`：必須因 `reason-mode-missing:P1` 類具名原因失敗。
2. 把 player 柱改成兩個選項或兩個 `correct:true`：必須 fail closed。
3. 在初始章節狀態直接塞入 E5、G5、J4：三章都必須拒絕。
4. 合法 ch1 run 的某個 reading 改成 `999`：必須因封閉資料源不一致拒絕。
5. 保持原始 run 不動，只把 ch1 `claim.ok` 或 `assertion.ok` 改成 true：必須拒絕。
6. 合法 ch5 碰撞原紙只改 `after.a=99`：必須拒絕。
7. 刪除 ch5 judgments，再偽造 J1–J4 evidence：不得由遷移程式補成已完成。
8. 將第二章任一指定控制高度退回 28px：常駐契約必須轉紅。
9. 將 ch5 橫屏 breakpoint 由 900px 退回 840px：844×390 契約必須轉紅。

另請自行設計至少兩組**不使用被測欄位自身作探針**的旁路變異。

## 五、已執行驗證

- `cd greybox && npm test`
  - 核心：155 通過、0 失敗
  - ch4 migration：19 groups、205 legacy cursors
  - ch4/ch5/ch6 art、系列入口、SEO／作者、設計治理：全通過
- `git diff --check`：exit 0
- in-app Browser，844×390：
  - 根標題頁 `scrollWidth === innerWidth`，無水平溢出
  - 第二章開場 `scrollWidth === innerWidth`，章名正確
  - 正確 server root 下本輪請求無 404

瀏覽器只驗到第二章開場，沒有人工走到第二章與第五章每一座工作台。自動 CSS 契約不能替代真人觸控與讀屏驗收。

## 六、請回報格式

請給一份唯讀對抗審結論：

1. `A / B / C` 數量與是否放行。
2. 每一條缺陷的最短可重現路徑、影響、精確修法。
3. 九組指定反向控制逐組結果；不得以「測試全綠」代替。
4. 清楚分開：靜態查讀、程式反向控制、瀏覽器驗收、真人實機未驗。
5. 不要修改任何檔案；若發現問題，只回報給施工端。
