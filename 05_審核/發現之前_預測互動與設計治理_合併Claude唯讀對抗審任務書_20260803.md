# 《發現之前》預測互動與設計治理｜合併 Claude 唯讀對抗審任務書

**日期**：2026-08-03
**施工歸屬**：Codex 工作流程
**審查模式**：唯讀；不得修改、commit、push 或發佈
**目的**：一次交付兩個尚未送 Claude 的包，但必須分層取快照、分層判定，不得混版。

## 一、先講清楚：這不是一個大而模糊的「全專案再審一次」

本輪包含兩層：

| 層 | 固定快照 | 性質 | 審查重點 |
|---|---|---|---|
| A｜預測與玩家代理權 | `7ae79946f52d6906377b5855f7be4c0fa6b49f2f` | runtime／敘事／測試，回溯審 | 預測是否有依據、先封後揭、第四章 K1 是否真的讀玩家路徑 |
| B｜設計文件治理 | `0a5425af23c02fa85cea501bbc87306097aa04b5` | 文件／ADR／治理測試，發佈前審 | CURRENT 是否真能代表 runtime、ADR 是否唯一可追、治理測試是否自我循環 |

兩層必須分別回報 `A=n / B=n / C=n` 與放行判定，最後才能給整體結論。

- 不得以 B 層文件寫得完整，替 A 層 runtime 背書。
- 不得以 A 層 `npm test` 全綠，替 B 層文件真實性背書。
- 目前主工作樹另有第六章草稿與審查文件差異，**完全不在本輪範圍**。

## 二、可重現錨點

兩個 commit 與 tree 都存在於本 repo：

```text
Layer A parent  16c1ce96f4ac847f1b9dfd7cf8607b29e9618220
Layer A commit  7ae79946f52d6906377b5855f7be4c0fa6b49f2f
Layer A tree    97392199b5962f075c31c4635a58c2f4bcffb8cd

Layer B parent  7ae79946f52d6906377b5855f7be4c0fa6b49f2f
Layer B commit  0a5425af23c02fa85cea501bbc87306097aa04b5
Layer B tree    4bb9d465e4730f8f4a4402346b86b012d587099c
```

請用 detached worktree、`git archive` 或等價唯讀快照各自審查。不要 checkout 主工作樹，也不要拿 HEAD 檔案去核對 A 層舊 SHA。兩個 diff 的 `git diff --check` 均已通過。

---

# Layer A｜預測機制敘事動機與第四章 K1

## 三、A 層先讀

在 `7ae7994` 快照中完整讀：

1. `05_審核/發現之前_預測機制敘事動機_全五章三方交叉驗證最終報告_20260803.md`
2. `05_審核/發現之前_預測機制敘事動機_施工後Claude唯讀對抗審包_20260803.md`
3. `greybox/decisions.md` 的 `GB-ADR-040`
4. 實作：`greybox/data/scenes.json`、`scenes3.json`、`scenes4.json`、`greybox/src/engine4.js`、`chapter-ui.js`、`sanitize.js`
5. 契約：`greybox/tests/run-node.mjs`

舊送審包的逐檔 SHA 只適用 `7ae7994` 快照；若在該快照仍不符，才算錨點失敗。

## 四、A 層必攻宣稱

### A1｜第一章第五段不是憑空押答案

- 押注前只能看到前四段；第五段須先封存後揭曉。
- 押注動機、實測揭曉與 `1,3,5,7,9 → 1,4,9,16,25` 兩次玩家判讀都要存在。
- NPC 不得先替玩家完成增量與累加的關係。

### A2｜第三章預測有直覺依據，也回到人物反應

- 木板與墨水瓶只能提供「誰改了速度」的直覺，不得直接交付偏頭／偏尾答案。
- 實驗後須保留預測紙，猜錯不擦除。
- 第三柱的錯誤對時／錯誤縮放須有具名人物接住，而非只有 UI 判錯。

### A3｜第四章 K1 規則紙與預測紙真有不同功能

- 指向、初速、箭長會改路徑；形狀押注不改路徑。
- 玩家要知道自己押的是「同一規矩跑三十拍後會撞回、逃走或繞住」。
- 特別判定「四項先封再畫三拍」是否仍讓押注太早。若是，請判 B 並明說這需要 schema／migration 工程，不能偽裝成一句文案可修。

### A4｜K1 分類真的來自玩家實際路徑

- 新 run 必須以 `classificationSource: "simulated-path-v1"` 標示。
- 玩家前三拍必須進入續畫；形狀名稱不得退回設定查表。
- 舊 run 無欄位可依明示相容規則讀取；未知來源須遭匯入稽核拒絕。

### A5｜第四章 K2／K3 先押後看

- K2 的六十秒換一秒先算再翻，錯誤 `4.9 ÷ 60 = 81.7 mm` 要指出「時間應平方」而非距離平方。
- K3 火星與木星兩張都封存後才一起揭曉；第一張封好不得從 DOM、alt、提示、存檔或錯誤訊息偷看 actual。
- 猜偏不擋證據，但錯紙必須留痕。

## 五、A 層反向控制

至少實做以下六組；變異只在臨時快照：

1. 把 `orbitShapeFromSimulation()` 改回 canonical 查表，契約必須轉紅。
2. 讓續畫忽略玩家前三拍，狀態／匯入契約必須轉紅。
3. 刪掉或偽造 `classificationSource`：未知值拒絕、合法舊缺欄位仍可讀。
4. 移除第一章「第五段先蓋住」或揭曉後並排，敘事契約必須轉紅。
5. 移除第三章木板類比、錯圖回應或結果回應，契約必須轉紅。
6. 讓 K3 只封一顆即可揭曉，契約必須轉紅。

若能操作瀏覽器，再於桌機與 844×390 實走 K1：檢查兩張紙可區分、焦點與 Enter 不會一次落兩拍、錯路徑保留。不能操作就標 `NOT RUN`，不得以自動測試代替。

---

# Layer B｜五章 CURRENT 規格與 ADR 治理

## 六、B 層先讀

在 `0a5425a` 快照中完整讀：

1. `05_審核/發現之前_設計文件完善度_三方交叉驗證最終報告_20260803.md`
2. `03_規格/README.md`
3. `03_規格/current-specs.json`
4. `03_規格/發現之前_跨章runtime與治理規格_v1.0.md`
5. 五份 CURRENT 章規格
6. `02_設計/發現之前_第三章現行設計基線_v1.0.md`
7. `02_設計/發現之前_第四章書桌探究現行設計定案_v1.0.md`
8. `greybox/decisions.md` 的 supersession metadata、GB-ADR-030、GB-ADR-041
9. `01_治理/發現之前_GOV-ADR-001_信譽作為研究誠實與授權階梯_20260729.md` 與退役別名
10. `greybox/tests/run-design-governance.mjs`
11. `tools/skill/before-discovery-dev/references/source-registry.json`

## 七、B 層必攻宣稱

### B1｜GB-ADR-030 撞號真的解除

- `greybox/decisions.md` 的 GB-ADR-030 只能是第四章 K2。
- 全系列信譽決策只能以 GOV-ADR-001 為現行名稱。
- 舊 GB-ADR-030 信譽檔必須明確退役，且 router／CURRENT 文件不能再把它當現行法源。

### B2｜「論證對位」改名沒有破壞存檔

- 玩家可見名稱是「論證對位」。
- 內部 `persuasion` 欄位、schema 與舊存檔相容不改。
- 「信譽」與「論證對位」的失敗、扣除、修復語意不得合併。
- GB-ADR-041、GOV-ADR-001、runtime 與測試必須說同一件事。

### B3｜CURRENT 真的是唯一現行入口

- `current-specs.json`、`03_規格/README.md`、檔名、H1 與版號一致。
- 舊版需以 HISTORICAL／AUDIT ONLY 清楚降權；新協作者循 README／router 不會先讀到錯版。
- 不只確認檔案存在，要實走 source registry 的任務路由。

### B4｜章規格描述的是現況，不是理想稿

逐章抽查 runtime；第三、四章要深查：

- 第三章五輪因果、第三柱逐拍換尺、雙視角與存檔狀態是否和 v1.0／v0.2 一致。
- 第四章 K1 玩家三拍＋牛頓續畫、K2 分紙、K3 雙封後揭、K4 借條、K5 出版欄位是否與 v1.0／v0.2 一致。
- 找出任何「規格寫成已完成，實際只是一項理想／未驗收」的句子。

### B5｜supersession 鏈不是裝飾

- GB-ADR-012、013、024、029 的 active／partially-superseded／superseded 與 `supersededBy` 必須符合實際演進。
- 被取代的部分不能藉由 CURRENT 文件重新生效；未被取代的部分也不能整筆消失。

### B6｜治理測試不是自我循環

`run-design-governance.mjs` 目前檢查 ID、CURRENT、版本、引用與名詞。請找出：

- 測試與被測資料是否共用同一個錯字／同一份名單，導致一起錯仍全綠。
- `active ADR 被 CURRENT 引用` 是否只靠一張集中列出 001～041 的「機器覆蓋索引」而形成形式合規，卻沒有章別語意落點。
- 跳過 `05_審核/`、`archive/` 是否合理；是否還有其他可被誤認成現行的路徑未掃。

## 八、B 層反向控制

至少實做以下五組；探針文字須由審查者獨立書寫，不得從被測欄位複製：

1. 在唯一流水帳外新增一個 active `GB-ADR-030` 標題，測試必須轉紅。
2. 刪掉一筆 active ADR 的真正 CURRENT 語意落點，只留下集中「機器覆蓋索引」；判斷測試是否仍假綠。
3. 讓 registry 版號與檔名或 H1 不同，測試必須轉紅。
4. 把任一 CURRENT 章規格的玩家可見名稱改回「說服力」，測試必須轉紅；同時不得誤殺解釋舊稱的相容段落。
5. 讓舊 GB-ADR-030 信譽別名拿掉 RETIRED 標記，或讓 router 指回舊檔，測試必須轉紅。

若第 2 或第 5 組不紅，請直接判治理契約缺口；不要因現行內容恰好正確就放過。

## 九、跨層一致性必查

1. A 層的 `GB-ADR-040` 必須在 B 層 CURRENT 規格中有**語意性**落點，不只列出 ID。
2. 文件不得把「149/0」寫成瀏覽器、真人通關或 production 驗收。
3. runtime 的玩家可見名詞、內部欄位、migration 與文件必須一致。
4. A 層若抓到 K1 四項先封過早，B 層文件應把它列成取捨／未驗風險，不能把它寫成已完全解決。

## 十、施工方已驗與未驗

現行 `0a5425a` 工作樹重跑：

- `cd greybox && npm test`：149 通過、0 失敗。
- 第四章 migration：19 組通過、205 個舊游標覆蓋。
- 設計治理：ID 唯一、001～041 連續、CURRENT 8 份、40 筆 active／partially-superseded ADR 有引用、名詞邊界均通過。
- 兩個 commit range 的 `git diff --check` 均通過。

未驗／不得冒充：本輪桌機與 844×390 全流程、鍵盤／VoiceOver、真人完整通關、B 層 push／release／production smoke。

另有 activation guard 的既存 13 warnings。請只在本輪使其惡化或證明它們遮蔽 CURRENT 真相時列入 A/B/C；不要把既存欠單灌進本輪數字。

## 十一、回報格式

請依序回報：

1. 錨點是否可重現；兩個 commit／tree 是否相符。
2. Layer A：`A=n / B=n / C=n`＋放行判定。
3. Layer B：`A=n / B=n / C=n`＋放行判定。
4. 跨層：是否有 runtime／文件互相背書的循環。
5. 每條問題須附檔案、函式或章節、可重現攻擊路徑、為何重要、最小修法、驗收條件。
6. 列出每一組反向控制的結果；若變異本身寫壞，須撤回，不得算產品問題。
7. 把靜態、引擎、migration、瀏覽器、真人閱讀、commit、release 分開陳述。

整體放行門檻：**兩層都 A=0、B=0**。C 可列後續改善，但不能偷換成已驗收。
