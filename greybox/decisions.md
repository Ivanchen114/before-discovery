# greybox/decisions.md — 實作級架構決策紀錄(GB-ADR)

**範疇**:僅灰盒程式實作層。專案級裁決(分工、流程、凍結)與**待總監裁決區**在根目錄 `decisions.md`。
編號採 `GB-ADR-###`,與根目錄專案級 `ADR-###` 分流,避免撞號。

---

## GB-ADR-001|資料載入策略:.js 全域賦值 + .json 規範鏡像

**背景**:R-ENV-01 要求雙擊 `index.html` 可玩;R-DATA-02 要求機器可讀 JSON 於 `/data`。file:// 之 opaque origin 使 fetch/XHR/ES module 載入一律被現代瀏覽器阻擋(Sol 審稿 #3)。
**選項**:①改用 localhost 指令啟動;②建置時 bundle;③資料以傳統 script 全域賦值載入,JSON 為規範鏡像。
**裁決**:採③。`/data/*.js` 為執行載體,`/data/*.json` 為規範鏡像,兩者深度相等性入自動測試(R-DATA-05)。
**理由**:①違反「雙擊可玩」對非技術試玩者的意義;②違反「無建置步驟」;③零依賴、零指令,漂移風險由測試封殺。

## GB-ADR-002|fixture 全面重校準與誤差語義修正

**背景**:v0.1 附錄 A 經枚舉驗算有 12+ 處矩陣違規(Claude 審核 A-1;Sol 審核 #2 同時指出)。根因:樣式為絕對偏移、容差為相對百分比——傾角越陡基準越大,相對衝擊縮水,一切變得可判定。另 v0.1「水鐘=對稱抖動」與矩陣「陡+水鐘三次平均不通過」邏輯矛盾:零均值抖動經平均必然收斂通過。
**裁決**:各樣式按「嚴重度×樣式÷基準」之相對衝擊重校;水鐘誤差改為「首段系統性偏高+微抖」(平均不可消除);脈搏改為「大幅抖動+輕偏移」(平均可大幅消除=廉價工具×多次路徑);原木後段流失遞增且三 run 形狀相異;木球突刺逐 run 游走。
**理由**:憲章四(不得製造錯誤的物理模型或虛假歷史因果)+R-EXP-04(矩陣為規範)。驗證:54 配置×216 判定點全符,與 12% 線最小距離 0.5pp,測試常駐 `/tests`。
**修訂(2026-07-19,依 Sol 第 2 輪審稿 A-1)**:撤除原裁決文中「此即伽利略需要緩斜面的史實原因」之宣稱。計時誤差樣式定位為**表現短時測量不確定性的教學代理**,非伽利略水鐘實測誤差之重建——本模型無操作程序與量測方程,無從導出該歷史因果;伽利略之實際程序(全程/指定距離集水稱重)與本切片逐段增量讀值不同。規格 v0.1.2 R-EXP-06 已同步代理聲明;史實頁揭露義務入 `docs/debt.md`。數值與矩陣不受影響。

## GB-ADR-003|天數成本機制化(最小版)

**背景**:v0.1 宣稱路徑「成本結構相異」但天數僅顯示、無差異(Sol #4:機制化或撤宣稱)。
**裁決**:天數成本=計時工具差異化(脈搏 1/水鐘 2/音格 3),run 與 claim 紀錄天數,結束畫面揭示總耗天數;v0 不設上限、不觸發事件,並於規格明文此邊界。
**理由**:三條過關路徑天數結構立即相異(最少天數、最少次數、最廉工具互不重合);上限與事件掛鉤屬第一章整合範疇,現在做=範疇膨脹。

## GB-ADR-004|選集配置指紋為四維(含計時工具)

**背景**:v0.1 R-JUD-01 漏「計時」維(Sol #5),混水鐘+脈搏數據可通過「變因受控」檢查。
**裁決**:選集守衛與斷言驗證一律比對四維(球/槽面/傾角/計時);斷言 b 之「僅球相異」=其餘三維全同。
**理由**:憲章三(變因結構可見);計時工具本身就是本切片的主要教學變因。

## GB-ADR-006|M1 敘事層架構(2026-07-19)

**背景**:章規格 v0.1 M1=敘事引擎+序幕+第一幕。
**裁決**:①章節入口為獨立 `chapter.html`,切片 `index.html` 原封不動(trunk 穩定、切片測試不受影響;M2 於章節內 embed 實驗台後再議整併);②`src/narrative.js` 為純函式敘事引擎,場景資料 `data/scenes.js/json` 雙載體(R-DATA-06 鏡像測試),authoring 便利工具 `tools/gen_scenes.py` 重跑覆寫兩載體、非遊戲建置步驟;③不可見之 choice 節點必須攜帶 `next` 跳過路徑,引擎強制擲錯;④存檔鍵 `bd_ch1_save`,壞檔備份至 `bd_ch1_save_corrupt`(R-SAV-02)。
**R-NAR-05 實作偏差(呈章規格審稿裁定)**:白名單條目增設 `class` 欄——`pre-discovery`(劇中人自己的錯誤/部分理論,位於玩家操作前,如 A1-1 密度理論之「同材質落得一樣快」)與 `post-reveal`(玩家指認後之覆述);「必須位於玩家操作節點之後」僅約束 post-reveal 類。理由:規格原文無法容納「史實立場呈現」與「結論揭曉」之語義差;若 Sol 審稿否決此偏差,改寫 A1-1 台詞為不含禁詞版本。

## GB-ADR-007|M2 第二幕架構(2026-07-19)

**裁決**:①新增節點型別 `embed`(內嵌實驗台,`until` 條件門:e3.a/b/c/established、repairRun)與 `return`(SC-R1 返回原游標);②切片引擎全狀態內嵌為 `state.lab`——章節與實驗台合為單一可序列化物件,存檔 schema 升 2(M1 存檔不相容,由 R-SAV-02 容錯處理,pre-release 可接受);③`labAction` 為唯一 lab 變更通道,負責同步:E3 確立(a∧b)→章節證據 E3、失敗判定→`hadFailure` 旗標(死路 B 回述之觸發條件——失敗回述僅於玩家真的失敗過才出現,不預先教學);④SC-R1 由 `redirectIfLocked` 引擎導入(非 goto 邊),可達性測試將其列為引擎入口。
**劇本實作偏差(呈 Sol 劇本審稿)**:劇本 A2-2 之「指認選項(1:3:5:7/平方數)」由實驗台**預測制**實現,不重複做成敘事選項——同一認知動作不做兩次,刪台詞測試由 lint+預測制雙重把關;A2-2 死路 B 診斷對話改為條件式回述(hadFailure),避免預先標籤化失敗(憲章五防消毒條款)。

## GB-ADR-008|M3 辯論層架構(2026-07-19)

**裁決**:①全章辯論資料入 `debate.json` 新欄位 `chapter`(legacy 欄位與切片測試原封不動,R-DATA-04);②辯論引擎入 narrative.js(debatePress/PressChoice/Present/Fr/ExitSuspended),辯論為 `embed(system:"debate", until:{debateWon})`,中止經 `suspendNext` 出口至 A3-F(自由補實驗、無門檻)再經 `{"debate":"reenter"}` 效果重返——量表重置、已破支柱與 FR 進度保留(R-DEB-13);③trap 說謊之 rep−1 與修復迴路(SC-R1)自然互通:辯論中歸零→修復→返回辯論,零特例碼;④review(R-UI-02 兩題)與 histfacts(R-END-02 資料驅動)為新節點型別;⑤存檔 schema 升 3。
**與劇本之對齊註**:P2 追問 2-3 之「憑誰立論」選項、E1「又是幾乎」特殊回應、FR 兩步引導/三槽組鏈/干擾項、trap 雙重代價與強制轉誠實、判定場「夾數據紙」全數入資料;辛普里奧台詞=老派書面語(ADR-005)、道具=評注本(ADR-008 專案級)。

## GB-ADR-005|文件版次治理

**背景**:規格 v0.1 引用不存在於資產庫的「架構 v0.3.1」;資料夾平鋪三版架構書。
**裁決**:法源引用更正為 v0.3;舊版文件移入 `archive/`,現行版唯一露出;專案以 git 管理;2026-07-18 起文件依 01_治理/02_設計/03_規格/04_劇本/05_審核 分類。
**理由**:組織原則 3(未寫入資產庫=不存在);隱藏驗收測試錨定條號,法源飄移直接汙染測試效力。
